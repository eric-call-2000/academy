/* Database Mastery — Unit 4: Transactions & Concurrency */
window.CODELAB.addUnit("db", {
  id: "db-u4",
  title: "Transactions & Concurrency",
  icon: "🔄",
  blurb: "ACID transactions, isolation levels, locking, and handling concurrent database operations safely.",
  cheat: [
    { h: "ACID properties", lang: "text", code: "A - Atomicity: All operations succeed or none do\nC - Consistency: Database moves from valid state to valid state\nI - Isolation: Concurrent transactions don't interfere\nD - Durability: Committed transactions survive failures", note: "ACID guarantees data integrity. Most databases provide ACID by default for single-statement transactions." },
    { h: "Transaction basics", lang: "sql", code: "BEGIN TRANSACTION;\n\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\n\nCOMMIT; -- or ROLLBACK if error", note: "Wrap related operations in a transaction. COMMIT saves changes, ROLLBACK undoes them." },
    { h: "Isolation levels", lang: "sql", code: "-- Read Uncommitted: dirty reads possible\n-- Read Committed: no dirty reads (default)\n-- Repeatable Read: no non-repeatable reads\n-- Serializable: no phantom reads\n\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;", note: "Higher isolation = more safety but less concurrency. Most apps use Read Committed or Repeatable Read." },
    { h: "Handling deadlocks", lang: "sql", code: "-- Deadlock occurs when transactions wait circularly\n-- Solution: retry with backoff\n\nBEGIN;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\n-- If deadlock, retry after delay\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\nCOMMIT;", note: "Deadlocks happen in high concurrency. Handle with retry logic and consistent lock ordering." }
  ],
  lessons: [

    {
      id: "db-u4-1",
      title: "ACID transactions: atomicity",
      kind: "js", chip: "DB", xp: 15, mins: 12,
      brief: "**Atomicity** means all operations in a transaction succeed together or fail together. No partial states.\n\n**ACID properties**:\n- **A**tomicity: all or nothing\n- **C**onsistency: valid state transitions\n- **I**solation: concurrent transactions don't interfere\n- **D**urability: committed changes persist\n\nYou'll implement a transaction system with rollback capability.",
      example: { lang: "js", code: "// Transaction with rollback\n\nconst transaction = db.beginTransaction();\n\ntry {\n  transaction.update('accounts', 1, { balance: -100 });\n  transaction.update('accounts', 2, { balance: +100 });\n  transaction.commit();\n} catch (error) {\n  transaction.rollback(); // Undo all changes\n}" },
      steps: [
        { text: "Create a transaction system with beginTransaction(), commit(), and rollback().",
          test: "T.expect(typeof db.beginTransaction === 'function', 'Create beginTransaction method');\nconst tx = db.beginTransaction();\nT.expect(typeof tx.commit === 'function', 'Transaction should have commit');\nT.expect(typeof tx.rollback === 'function', 'Transaction should have rollback');" },
        { text: "Implement rollback to undo all changes in transaction.",
          test: "const tx2 = db.beginTransaction();\ntx2.update('accounts', 1, { balance: 500 });\nconst beforeRollback = db.data.accounts[0].balance;\ntx2.rollback();\nT.expect(db.data.accounts[0].balance !== beforeRollback, 'Rollback should undo changes');" },
        { text: "Implement commit to apply all changes permanently.",
          test: "const tx3 = db.beginTransaction();\ntx3.update('accounts', 1, { balance: 600 });\ntx3.commit();\nT.expect(db.data.accounts[0].balance === 600, 'Commit should apply changes');" },
        { text: "Ensure atomicity: rollback undoes all operations, not just last one.",
          test: "const tx4 = db.beginTransaction();\ntx4.update('accounts', 1, { balance: 700 });\ntx4.update('accounts', 2, { balance: 800 });\ntx4.rollback();\nT.expect(db.data.accounts[0].balance !== 700 && db.data.accounts[1].balance !== 800, 'Rollback should undo all changes');" }
      ],
      files: [
        { name: "script.js", content: "// Transaction system with ACID atomicity\n\nconst db = {\n  data: {\n    accounts: [\n      { id: 1, name: 'Alice', balance: 1000 },\n      { id: 2, name: 'Bob', balance: 500 }\n    ]\n  },\n  \n  beginTransaction: function() {\n    // Start a new transaction\n    // Return transaction object with update, commit, rollback\n  }\n};\n\n// Test atomicity: transfer money\nconsole.log('=== Initial State ===');\nconsole.log('Accounts:', JSON.stringify(db.data.accounts, null, 2));\n\nconsole.log('\\n=== Transaction: Transfer $100 from Alice to Bob ===');\nconst tx = db.beginTransaction();\n\ntry {\n  // Alice loses $100\n  tx.update('accounts', 1, { balance: -100 });\n  console.log('After debit Alice:', tx.getState().accounts[0]);\n  \n  // Bob gains $100\n  tx.update('accounts', 2, { balance: +100 });\n  console.log('After credit Bob:', tx.getState().accounts[1]);\n  \n  // Commit transaction\n  tx.commit();\n  console.log('\\n=== Transaction Committed ===');\n  console.log('Accounts:', JSON.stringify(db.data.accounts, null, 2));\n  \n} catch (error) {\n  console.error('Error:', error.message);\n  tx.rollback();\n  console.log('\\n=== Transaction Rolled Back ===');\n  console.log('Accounts:', JSON.stringify(db.data.accounts, null, 2));\n}\n\n// Test rollback\nconsole.log('\\n=== Test Rollback ===');\nconst tx2 = db.beginTransaction();\ntx2.update('accounts', 1, { balance: -50 });\nconsole.log('Before rollback:', tx2.getState().accounts[0]);\ntx2.rollback();\nconsole.log('After rollback:', db.data.accounts[0]);\n\n// Test failed transaction (simulate error)\nconsole.log('\\n=== Test Failed Transaction ===');\nconst tx3 = db.beginTransaction();\n\ntry {\n  tx3.update('accounts', 1, { balance: -200 });\n  // Simulate error (insufficient funds check would fail here)\n  throw new Error('Insufficient funds');\n  tx3.commit();\n} catch (error) {\n  console.error('Transaction failed:', error.message);\n  tx3.rollback();\n  console.log('After rollback:', db.data.accounts[0]);\n}\n" }
      ],
      hints: [
        "For beginTransaction: create a transaction object with a copy of the current database state",
        "For update: modify the transaction's state copy, not the actual database",
        "For commit: replace the actual database state with the transaction's state",
        "For rollback: discard the transaction's state, do nothing to the actual database"
      ],
      solution: {
        "script.js": "// Transaction system with ACID atomicity\n\nconst db = {\n  data: {\n    accounts: [\n      { id: 1, name: 'Alice', balance: 1000 },\n      { id: 2, name: 'Bob', balance: 500 }\n    ]\n  },\n  \n  beginTransaction: function() {\n    // Deep copy of current state\n    const initialState = JSON.parse(JSON.stringify(this.data));\n    \n    return {\n      state: initialState,\n      \n      getState: function() {\n        return this.state;\n      },\n      \n      update: function(table, id, changes) {\n        const row = this.state[table].find(r => r.id === id);\n        if (row) {\n          Object.assign(row, changes);\n        }\n      },\n      \n      commit: function() {\n        db.data = this.state;\n        console.log('Transaction committed successfully');\n      },\n      \n      rollback: function() {\n        console.log('Transaction rolled back');\n        // Changes are discarded (state is not applied to db.data)\n      }\n    };\n  }\n};\n\n// Test atomicity: transfer money\nconsole.log('=== Initial State ===');\nconsole.log('Accounts:', JSON.stringify(db.data.accounts, null, 2));\n\nconsole.log('\\n=== Transaction: Transfer $100 from Alice to Bob ===');\nconst tx = db.beginTransaction();\n\ntry {\n  // Alice loses $100\n  tx.update('accounts', 1, { balance: -100 });\n  console.log('After debit Alice:', tx.getState().accounts[0]);\n  \n  // Bob gains $100\n  tx.update('accounts', 2, { balance: +100 });\n  console.log('After credit Bob:', tx.getState().accounts[1]);\n  \n  // Commit transaction\n  tx.commit();\n  console.log('\\n=== Transaction Committed ===');\n  console.log('Accounts:', JSON.stringify(db.data.accounts, null, 2));\n  \n} catch (error) {\n  console.error('Error:', error.message);\n  tx.rollback();\n  console.log('\\n=== Transaction Rolled Back ===');\n  console.log('Accounts:', JSON.stringify(db.data.accounts, null, 2));\n}\n\n// Test rollback\nconsole.log('\\n=== Test Rollback ===');\nconst tx2 = db.beginTransaction();\ntx2.update('accounts', 1, { balance: -50 });\nconsole.log('Before rollback:', tx2.getState().accounts[0]);\ntx2.rollback();\nconsole.log('After rollback:', db.data.accounts[0]);\n\n// Test failed transaction (simulate error)\nconsole.log('\\n=== Test Failed Transaction ===');\nconst tx3 = db.beginTransaction();\n\ntry {\n  tx3.update('accounts', 1, { balance: -200 });\n  // Simulate error (insufficient funds check would fail here)\n  throw new Error('Insufficient funds');\n  tx3.commit();\n} catch (error) {\n  console.error('Transaction failed:', error.message);\n  tx3.rollback();\n  console.log('After rollback:', db.data.accounts[0]);\n}\n"
      }
    },

    {
      id: "db-u4-2",
      title: "Isolation levels: preventing interference",
      kind: "js", chip: "DB", xp: 15, mins: 12,
      brief: "**Isolation levels** control how transactions see each other's changes. Higher isolation = more safety but less concurrency.\n\n**Levels** (low to high):\n- **Read Uncommitted**: dirty reads possible\n- **Read Committed**: no dirty reads (default)\n- **Repeatable Read**: no non-repeatable reads\n- **Serializable**: no phantom reads\n\nYou'll simulate different isolation levels.",
      example: { lang: "js", code: "// Isolation level simulation\n\n// Read Uncommitted: see uncommitted changes\nconst tx1 = db.beginTransaction('READ_UNCOMMITTED');\nconst tx2 = db.beginTransaction('READ_UNCOMMITTED');\ntx1.update('accounts', 1, { balance: 900 });\nconsole.log(tx2.read('accounts', 1)); // Sees uncommitted change!\n\n// Read Committed: only see committed changes\nconst tx3 = db.beginTransaction('READ_COMMITTED');\nconst tx4 = db.beginTransaction('READ_COMMITTED');\ntx3.update('accounts', 1, { balance: 800 });\nconsole.log(tx4.read('accounts', 1)); // Doesn't see uncommitted change" },
      steps: [
        { text: "Create transactions with isolation level parameter.",
          test: "T.expect(typeof db.beginTransaction === 'function', 'Create beginTransaction with isolation level');\nconst tx = db.beginTransaction('READ_COMMITTED');\nT.expect(tx.isolationLevel === 'READ_COMMITTED', 'Should set isolation level');" },
        { text: "Implement READ_UNCOMMITTED: transactions see uncommitted changes.",
          test: "const tx1 = db.beginTransaction('READ_UNCOMMITTED');\nconst tx2 = db.beginTransaction('READ_UNCOMMITTED');\ntx1.update('accounts', 1, { balance: 900 });\nconst read = tx2.read('accounts', 1);\nT.expect(read.balance === 900, 'READ_UNCOMMITTED should see uncommitted changes');" },
        { text: "Implement READ_COMMITTED: only see committed changes.",
          test: "const tx3 = db.beginTransaction('READ_COMMITTED');\nconst tx4 = db.beginTransaction('READ_COMMITTED');\ntx3.update('accounts', 1, { balance: 800 });\nconst read2 = tx4.read('accounts', 1);\nT.expect(read2.balance !== 800, 'READ_COMMITTED should not see uncommitted changes');" },
        { text: "Implement REPEATABLE READ: consistent snapshot within transaction.",
          test: "const tx5 = db.beginTransaction('REPEATABLE_READ');\nconst firstRead = tx5.read('accounts', 1);\n// Simulate another transaction committing\ndb.data.accounts[0].balance = 750;\nconst secondRead = tx5.read('accounts', 1);\nT.expect(firstRead.balance === secondRead.balance, 'REPEATABLE_READ should see consistent snapshot');" }
      ],
      files: [
        { name: "script.js", content: "// Isolation level simulation\n\nconst db = {\n  data: {\n    accounts: [\n      { id: 1, name: 'Alice', balance: 1000 },\n      { id: 2, name: 'Bob', balance: 500 }\n    ]\n  },\n  transactions: [],\n  \n  beginTransaction: function(isolationLevel = 'READ_COMMITTED') {\n    const tx = {\n      id: Date.now(),\n      isolationLevel: isolationLevel,\n      state: JSON.parse(JSON.stringify(this.data)),\n      snapshot: null, // For REPEATABLE_READ\n      \n      read: function(table, id) {\n        // Read based on isolation level\n      },\n      \n      update: function(table, id, changes) {\n        const row = this.state[table].find(r => r.id === id);\n        if (row) Object.assign(row, changes);\n      },\n      \n      commit: function() {\n        db.data = this.state;\n      },\n      \n      rollback: function() {\n        // Discard changes\n      }\n    };\n    \n    this.transactions.push(tx);\n    return tx;\n  }\n};\n\nconsole.log('=== Initial State ===');\nconsole.log('Alice balance:', db.data.accounts[0].balance);\n\n// Test READ_UNCOMMITTED\nconsole.log('\\n=== READ_UNCOMMITTED ===');\nconst tx1 = db.beginTransaction('READ_UNCOMMITTED');\nconst tx2 = db.beginTransaction('READ_UNCOMMITTED');\n\ntx1.update('accounts', 1, { balance: 900 });\nconsole.log('Tx1 updates Alice to 900 (uncommitted)');\n\nconst tx2Read = tx2.read('accounts', 1);\nconsole.log('Tx2 reads Alice:', tx2Read.balance, '(sees uncommitted change!)');\n\ntx1.rollback();\n\n// Test READ_COMMITTED\nconsole.log('\\n=== READ_COMMITTED ===');\nconst tx3 = db.beginTransaction('READ_COMMITTED');\nconst tx4 = db.beginTransaction('READ_COMMITTED');\n\ntx3.update('accounts', 1, { balance: 800 });\nconsole.log('Tx3 updates Alice to 800 (uncommitted)');\n\nconst tx4Read = tx4.read('accounts', 1);\nconsole.log('Tx4 reads Alice:', tx4Read.balance, '(does not see uncommitted change)');\n\ntx3.commit();\nconst tx4ReadAfter = tx4.read('accounts', 1);\nconsole.log('Tx4 reads Alice after commit:', tx4ReadAfter.balance, '(now sees committed change)');\n\n// Test REPEATABLE_READ\nconsole.log('\\n=== REPEATABLE_READ ===');\nconst tx5 = db.beginTransaction('REPEATABLE_READ');\n\nconst firstRead = tx5.read('accounts', 1);\nconsole.log('Tx5 first read Alice:', firstRead.balance);\n\n// Simulate external commit\ndb.data.accounts[0].balance = 750;\nconsole.log('External commit changes Alice to 750');\n\nconst secondRead = tx5.read('accounts', 1);\nconsole.log('Tx5 second read Alice:', secondRead.balance, '(still sees original value!)');\n" }
      ],
      hints: [
        "For READ_UNCOMMITTED: read from the transaction's state (which includes uncommitted changes from other transactions)",
        "For READ_COMMITTED: read from the actual database data (only committed changes)",
        "For REPEATABLE_READ: take a snapshot at first read, always return from snapshot",
        "Store committed transactions and check if other transactions have committed when reading"
      ],
      solution: {
        "script.js": "// Isolation level simulation\n\nconst db = {\n  data: {\n    accounts: [\n      { id: 1, name: 'Alice', balance: 1000 },\n      { id: 2, name: 'Bob', balance: 500 }\n    ]\n  },\n  transactions: [],\n  \n  beginTransaction: function(isolationLevel = 'READ_COMMITTED') {\n    const tx = {\n      id: Date.now(),\n      isolationLevel: isolationLevel,\n      state: JSON.parse(JSON.stringify(this.data)),\n      snapshot: null,\n      \n      read: function(table, id) {\n        if (this.isolationLevel === 'READ_UNCOMMITTED') {\n          // Read from transaction state (includes uncommitted changes)\n          return this.state[table].find(r => r.id === id);\n        } else if (this.isolationLevel === 'READ_COMMITTED') {\n          // Read from actual database (only committed changes)\n          return db.data[table].find(r => r.id === id);\n        } else if (this.isolationLevel === 'REPEATABLE_READ') {\n          // Use snapshot if exists, otherwise create one\n          if (!this.snapshot) {\n            this.snapshot = JSON.parse(JSON.stringify(db.data));\n          }\n          return this.snapshot[table].find(r => r.id === id);\n        }\n        return this.state[table].find(r => r.id === id);\n      },\n      \n      update: function(table, id, changes) {\n        const row = this.state[table].find(r => r.id === id);\n        if (row) Object.assign(row, changes);\n      },\n      \n      commit: function() {\n        db.data = this.state;\n        console.log('Transaction committed');\n      },\n      \n      rollback: function() {\n        console.log('Transaction rolled back');\n      }\n    };\n    \n    this.transactions.push(tx);\n    return tx;\n  }\n};\n\nconsole.log('=== Initial State ===');\nconsole.log('Alice balance:', db.data.accounts[0].balance);\n\n// Test READ_UNCOMMITTED\nconsole.log('\\n=== READ_UNCOMMITTED ===');\nconst tx1 = db.beginTransaction('READ_UNCOMMITTED');\nconst tx2 = db.beginTransaction('READ_UNCOMMITTED');\n\ntx1.update('accounts', 1, { balance: 900 });\nconsole.log('Tx1 updates Alice to 900 (uncommitted)');\n\nconst tx2Read = tx2.read('accounts', 1);\nconsole.log('Tx2 reads Alice:', tx2Read.balance, '(sees uncommitted change!)');\n\ntx1.rollback();\n\n// Test READ_COMMITTED\nconsole.log('\\n=== READ_COMMITTED ===');\nconst tx3 = db.beginTransaction('READ_COMMITTED');\nconst tx4 = db.beginTransaction('READ_COMMITTED');\n\ntx3.update('accounts', 1, { balance: 800 });\nconsole.log('Tx3 updates Alice to 800 (uncommitted)');\n\nconst tx4Read = tx4.read('accounts', 1);\nconsole.log('Tx4 reads Alice:', tx4Read.balance, '(does not see uncommitted change)');\n\ntx3.commit();\nconst tx4ReadAfter = tx4.read('accounts', 1);\nconsole.log('Tx4 reads Alice after commit:', tx4ReadAfter.balance, '(now sees committed change)');\n\n// Test REPEATABLE_READ\nconsole.log('\\n=== REPEATABLE_READ ===');\nconst tx5 = db.beginTransaction('REPEATABLE_READ');\n\nconst firstRead = tx5.read('accounts', 1);\nconsole.log('Tx5 first read Alice:', firstRead.balance);\n\n// Simulate external commit\ndb.data.accounts[0].balance = 750;\nconsole.log('External commit changes Alice to 750');\n\nconst secondRead = tx5.read('accounts', 1);\nconsole.log('Tx5 second read Alice:', secondRead.balance, '(still sees original value!)');\n"
      }
    },

    {
      id: "db-u4-3",
      title: "Pessimistic locking: prevent conflicts",
      kind: "js", chip: "DB", xp: 15, mins: 12,
      brief: "**Pessimistic locking** assumes conflicts will happen and locks resources before accessing them. Prevents concurrent modifications.\n\n**Lock types**:\n- **Shared lock**: multiple readers, no writers\n- **Exclusive lock**: single writer, no readers or writers\n\n**Use when**: high contention, critical operations, data integrity is paramount.\n\nYou'll implement a locking system.",
      example: { lang: "js", code: "// Pessimistic locking\n\n// Acquire exclusive lock\nconst lock = db.acquireLock('accounts', 1, 'EXCLUSIVE');\n\nif (lock) {\n  try {\n    // Safe to modify\n    db.update('accounts', 1, { balance: 900 });\n  } finally {\n    // Always release lock\n    db.releaseLock(lock);\n  }\n} else {\n  // Lock already held, wait or retry\n  console.log('Resource locked, retrying...');\n}" },
      steps: [
        { text: "Create a lock manager with acquireLock() and releaseLock().",
          test: "T.expect(typeof db.acquireLock === 'function', 'Create acquireLock method');\nT.expect(typeof db.releaseLock === 'function', 'Create releaseLock method');\nconst lock = db.acquireLock('accounts', 1, 'EXCLUSIVE');\nT.expect(lock, 'Should acquire lock');" },
        { text: "Implement exclusive lock: only one transaction can hold it.",
          test: "const lock1 = db.acquireLock('accounts', 1, 'EXCLUSIVE');\nconst lock2 = db.acquireLock('accounts', 1, 'EXCLUSIVE');\nT.expect(lock1 && !lock2, 'Second acquire should fail when lock held');" },
        { text: "Implement shared lock: multiple readers can hold it.",
          test: "const shared1 = db.acquireLock('accounts', 2, 'SHARED');\nconst shared2 = db.acquireLock('accounts', 2, 'SHARED');\nT.expect(shared1 && shared2, 'Multiple shared locks should be allowed');" },
        { text: "Shared lock should block exclusive lock.",
          test: "const shared3 = db.acquireLock('accounts', 3, 'SHARED');\nconst exclusive = db.acquireLock('accounts', 3, 'EXCLUSIVE');\nT.expect(shared3 && !exclusive, 'Exclusive should fail when shared lock held');" }
      ],
      files: [
        { name: "script.js", content: "// Pessimistic locking system\n\nconst db = {\n  locks: new Map(), // key -> { type, holders }\n  \n  acquireLock: function(resource, type, transactionId) {\n    // Try to acquire lock on resource\n    // type: 'SHARED' or 'EXCLUSIVE'\n    // Return lock object if successful, null if failed\n  },\n  \n  releaseLock: function(lock) {\n    // Release lock\n  },\n  \n  isLocked: function(resource) {\n    // Check if resource is locked\n  }\n};\n\n// Test exclusive lock\nconsole.log('=== Exclusive Lock Test ===');\nconst tx1 = 'tx1';\nconst tx2 = 'tx2';\n\nconst lock1 = db.acquireLock('accounts', 1, 'EXCLUSIVE', tx1);\nconsole.log('Tx1 acquire exclusive on accounts[1]:', lock1 ? 'SUCCESS' : 'FAILED');\n\nconst lock2 = db.acquireLock('accounts', 1, 'EXCLUSIVE', tx2);\nconsole.log('Tx2 acquire exclusive on accounts[1]:', lock2 ? 'SUCCESS' : 'FAILED (expected)');\n\ndb.releaseLock(lock1);\nconsole.log('Tx1 released lock');\n\nconst lock3 = db.acquireLock('accounts', 1, 'EXCLUSIVE', tx2);\nconsole.log('Tx2 acquire exclusive after release:', lock3 ? 'SUCCESS' : 'FAILED');\n\ndb.releaseLock(lock3);\n\n// Test shared lock\nconsole.log('\\n=== Shared Lock Test ===');\nconst shared1 = db.acquireLock('accounts', 2, 'SHARED', tx1);\nconsole.log('Tx1 acquire shared on accounts[2]:', shared1 ? 'SUCCESS' : 'FAILED');\n\nconst shared2 = db.acquireLock('accounts', 2, 'SHARED', tx2);\nconsole.log('Tx2 acquire shared on accounts[2]:', shared2 ? 'SUCCESS' : 'FAILED');\n\nconst exclusiveBlocked = db.acquireLock('accounts', 2, 'EXCLUSIVE', 'tx3');\nconsole.log('Tx3 acquire exclusive on accounts[2]:', exclusiveBlocked ? 'SUCCESS' : 'FAILED (expected)');\n\ndb.releaseLock(shared1);\ndb.releaseLock(shared2);\n\nconst exclusiveAfter = db.acquireLock('accounts', 2, 'EXCLUSIVE', 'tx3');\nconsole.log('Tx3 acquire exclusive after shared released:', exclusiveAfter ? 'SUCCESS' : 'FAILED');\n\ndb.releaseLock(exclusiveAfter);\n\nconsole.log('\\n=== Lock Status ===');\nconsole.log('Active locks:', db.locks.size);\n" }
      ],
      hints: [
        "For acquireLock: check if resource is locked. For EXCLUSIVE, fail if any lock exists. For SHARED, fail if EXCLUSIVE lock exists",
        "Track lock holders in an array. For SHARED, add to holders array. For EXCLUSIVE, replace holders array",
        "For releaseLock: remove holder from array. If holders array is empty, delete the lock entry",
        "Lock key can be composite: 'table:id' or just 'table' depending on granularity"
      ],
      solution: {
        "script.js": "// Pessimistic locking system\n\nconst db = {\n  locks: new Map(),\n  \n  acquireLock: function(resource, type, transactionId) {\n    const key = `${resource}`;\n    const existing = this.locks.get(key);\n    \n    if (!existing) {\n      // No existing lock, acquire it\n      this.locks.set(key, {\n        type: type,\n        holders: [transactionId]\n      });\n      return { resource, type, transactionId };\n    }\n    \n    if (type === 'EXCLUSIVE') {\n      // Exclusive lock requires no existing locks\n      return null;\n    }\n    \n    if (type === 'SHARED' && existing.type === 'SHARED') {\n      // Can share if existing is also shared\n      existing.holders.push(transactionId);\n      return { resource, type, transactionId };\n    }\n    \n    // Can't acquire\n    return null;\n  },\n  \n  releaseLock: function(lock) {\n    const key = lock.resource;\n    const existing = this.locks.get(key);\n    \n    if (existing) {\n      const index = existing.holders.indexOf(lock.transactionId);\n      if (index > -1) {\n        existing.holders.splice(index, 1);\n      }\n      \n      if (existing.holders.length === 0) {\n        this.locks.delete(key);\n      }\n    }\n  },\n  \n  isLocked: function(resource) {\n    return this.locks.has(`${resource}`);\n  }\n};\n\n// Test exclusive lock\nconsole.log('=== Exclusive Lock Test ===');\nconst tx1 = 'tx1';\nconst tx2 = 'tx2';\n\nconst lock1 = db.acquireLock('accounts', 1, 'EXCLUSIVE', tx1);\nconsole.log('Tx1 acquire exclusive on accounts[1]:', lock1 ? 'SUCCESS' : 'FAILED');\n\nconst lock2 = db.acquireLock('accounts', 1, 'EXCLUSIVE', tx2);\nconsole.log('Tx2 acquire exclusive on accounts[1]:', lock2 ? 'SUCCESS' : 'FAILED (expected)');\n\ndb.releaseLock(lock1);\nconsole.log('Tx1 released lock');\n\nconst lock3 = db.acquireLock('accounts', 1, 'EXCLUSIVE', tx2);\nconsole.log('Tx2 acquire exclusive after release:', lock3 ? 'SUCCESS' : 'FAILED');\n\ndb.releaseLock(lock3);\n\n// Test shared lock\nconsole.log('\\n=== Shared Lock Test ===');\nconst shared1 = db.acquireLock('accounts', 2, 'SHARED', tx1);\nconsole.log('Tx1 acquire shared on accounts[2]:', shared1 ? 'SUCCESS' : 'FAILED');\n\nconst shared2 = db.acquireLock('accounts', 2, 'SHARED', tx2);\nconsole.log('Tx2 acquire shared on accounts[2]:', shared2 ? 'SUCCESS' : 'FAILED');\n\nconst exclusiveBlocked = db.acquireLock('accounts', 2, 'EXCLUSIVE', 'tx3');\nconsole.log('Tx3 acquire exclusive on accounts[2]:', exclusiveBlocked ? 'SUCCESS' : 'FAILED (expected)');\n\ndb.releaseLock(shared1);\ndb.releaseLock(shared2);\n\nconst exclusiveAfter = db.acquireLock('accounts', 2, 'EXCLUSIVE', 'tx3');\nconsole.log('Tx3 acquire exclusive after shared released:', exclusiveAfter ? 'SUCCESS' : 'FAILED');\n\ndb.releaseLock(exclusiveAfter);\n\nconsole.log('\\n=== Lock Status ===');\nconsole.log('Active locks:', db.locks.size);\n"
      }
    },

    {
      id: "db-u4-4",
      title: "Optimistic locking: version control",
      kind: "js", chip: "DB", xp: 15, mins: 12,
      brief: "**Optimistic locking** assumes conflicts are rare. Check for conflicts before committing, retry if conflict occurs.\n\n**How it works**:\n1. Read data with version number\n2. Modify data\n3. On write, check version hasn't changed\n4. If changed, retry; if not, update and increment version\n\n**Use when**: low contention, high throughput needed.\n\nYou'll implement optimistic concurrency control.",
      example: { lang: "js", code: "// Optimistic locking with version\n\nconst account = db.read('accounts', 1);\nconst originalVersion = account.version;\n\n// ... do work ...\n\ntry {\n  db.updateWithCheck('accounts', 1, {\n    balance: 900,\n    version: originalVersion + 1\n  }, originalVersion);\n} catch (error) {\n  // Version changed, retry\n  console.log('Conflict detected, retrying...');\n}" },
      steps: [
        { text: "Add version field to records and auto-increment on updates.",
          test: "T.expect(db.data.accounts[0].version !== undefined, 'Records should have version field');\nconst oldVersion = db.data.accounts[0].version;\ndb.update('accounts', 1, { balance: 900 });\nT.expect(db.data.accounts[0].version > oldVersion, 'Version should increment on update');" },
        { text: "Implement updateWithCheck() that verifies version before updating.",
          test: "T.expect(typeof db.updateWithCheck === 'function', 'Create updateWithCheck method');\nconst account = db.read('accounts', 1);\ndb.updateWithCheck('accounts', 1, { balance: 950 }, account.version);\nT.expect(db.data.accounts[0].balance === 950, 'Update should succeed with correct version');" },
        { text: "Throw error if version check fails (conflict detected).",
          test: "const staleVersion = db.data.accounts[0].version - 1;\ntry {\n  db.updateWithCheck('accounts', 1, { balance: 1000 }, staleVersion);\n  T.expect(false, 'Should throw error for stale version');\n} catch (e) {\n  T.expect(true, 'Should throw error for stale version');\n}" },
        { text: "Implement retry logic for conflicts.",
          test: "T.expect(typeof db.updateWithRetry === 'function', 'Create updateWithRetry method');\n// Simulate conflict by updating externally\ndb.data.accounts[0].version += 1;\ndb.updateWithRetry('accounts', 1, { balance: 1100 }, 3);\nT.expect(db.data.accounts[0].balance === 1100, 'Retry should succeed after conflict');" }
      ],
      files: [
        { name: "script.js", content: "// Optimistic locking with version control\n\nconst db = {\n  data: {\n    accounts: [\n      { id: 1, name: 'Alice', balance: 1000, version: 1 },\n      { id: 2, name: 'Bob', balance: 500, version: 1 }\n    ]\n  },\n  \n  read: function(table, id) {\n    return this.data[table].find(r => r.id === id);\n  },\n  \n  update: function(table, id, changes) {\n    const row = this.data[table].find(r => r.id === id);\n    if (row) {\n      Object.assign(row, changes);\n      row.version = (row.version || 0) + 1;\n    }\n  },\n  \n  updateWithCheck: function(table, id, changes, expectedVersion) {\n    // Update only if version matches expected\n    // Throw error if version mismatch (conflict)\n  },\n  \n  updateWithRetry: function(table, id, changes, expectedVersion, maxRetries = 3) {\n    // Retry updateWithCheck on conflict\n    // Return true if successful, false if max retries exceeded\n  }\n};\n\nconsole.log('=== Initial State ===');\nconsole.log('Alice:', db.read('accounts', 1));\n\n// Test successful update with version check\nconsole.log('\\n=== Test Successful Update ===');\nconst account1 = db.read('accounts', 1);\nconsole.log('Read Alice, version:', account1.version);\n\ndb.updateWithCheck('accounts', 1, { balance: 900 }, account1.version);\nconsole.log('Updated Alice:', db.read('accounts', 1));\n\n// Test conflict detection\nconsole.log('\\n=== Test Conflict Detection ===');\nconst account2 = db.read('accounts', 1);\nconst staleVersion = account2.version - 1; // Simulate stale read\n\ntry {\n  db.updateWithCheck('accounts', 1, { balance: 800 }, staleVersion);\n  console.log('Update succeeded (unexpected)');\n} catch (error) {\n  console.log('Update failed as expected:', error.message);\n}\n\n// Test retry logic\nconsole.log('\\n=== Test Retry Logic ===');\nconst account3 = db.read('accounts', 1);\nconst originalVersion = account3.version;\n\n// Simulate external update\ndb.update('accounts', 1, { balance: 850 });\nconsole.log('External update changed version to:', db.read('accounts', 1).version);\n\nconst success = db.updateWithRetry('accounts', 1, { balance: 950 }, originalVersion);\nconsole.log('Retry result:', success ? 'SUCCESS' : 'FAILED');\nconsole.log('Final Alice:', db.read('accounts', 1));\n" }
      ],
      hints: [
        "For updateWithCheck: compare row.version with expectedVersion. If match, update and increment version. If not match, throw error",
        "For updateWithRetry: loop up to maxRetries. On each iteration, read current version, call updateWithCheck with current version. If success, return true. If max retries exceeded, return false",
        "Version should be incremented on every successful update",
        "Optimistic locking is good for low-conflict scenarios as it doesn't block reads"
      ],
      solution: {
        "script.js": "// Optimistic locking with version control\n\nconst db = {\n  data: {\n    accounts: [\n      { id: 1, name: 'Alice', balance: 1000, version: 1 },\n      { id: 2, name: 'Bob', balance: 500, version: 1 }\n    ]\n  },\n  \n  read: function(table, id) {\n    return this.data[table].find(r => r.id === id);\n  },\n  \n  update: function(table, id, changes) {\n    const row = this.data[table].find(r => r.id === id);\n    if (row) {\n      Object.assign(row, changes);\n      row.version = (row.version || 0) + 1;\n    }\n  },\n  \n  updateWithCheck: function(table, id, changes, expectedVersion) {\n    const row = this.data[table].find(r => r.id === id);\n    if (!row) {\n      throw new Error('Row not found');\n    }\n    \n    if (row.version !== expectedVersion) {\n      throw new Error(`Conflict: expected version ${expectedVersion}, actual ${row.version}`);\n    }\n    \n    Object.assign(row, changes);\n    row.version = row.version + 1;\n  },\n  \n  updateWithRetry: function(table, id, changes, expectedVersion, maxRetries = 3) {\n    for (let attempt = 0; attempt < maxRetries; attempt++) {\n      try {\n        const current = this.read(table, id);\n        this.updateWithCheck(table, id, changes, current.version);\n        return true;\n      } catch (error) {\n        if (attempt === maxRetries - 1) {\n          console.log(`Max retries (${maxRetries}) exceeded`);\n          return false;\n        }\n        console.log(`Retry ${attempt + 1}/${maxRetries}: ${error.message}`);\n        // Small delay before retry (in real app, use exponential backoff)\n      }\n    }\n    return false;\n  }\n};\n\nconsole.log('=== Initial State ===');\nconsole.log('Alice:', db.read('accounts', 1));\n\n// Test successful update with version check\nconsole.log('\\n=== Test Successful Update ===');\nconst account1 = db.read('accounts', 1);\nconsole.log('Read Alice, version:', account1.version);\n\ndb.updateWithCheck('accounts', 1, { balance: 900 }, account1.version);\nconsole.log('Updated Alice:', db.read('accounts', 1));\n\n// Test conflict detection\nconsole.log('\\n=== Test Conflict Detection ===');\nconst account2 = db.read('accounts', 1);\nconst staleVersion = account2.version - 1; // Simulate stale read\n\ntry {\n  db.updateWithCheck('accounts', 1, { balance: 800 }, staleVersion);\n  console.log('Update succeeded (unexpected)');\n} catch (error) {\n  console.log('Update failed as expected:', error.message);\n}\n\n// Test retry logic\nconsole.log('\\n=== Test Retry Logic ===');\nconst account3 = db.read('accounts', 1);\nconst originalVersion = account3.version;\n\n// Simulate external update\ndb.update('accounts', 1, { balance: 850 });\nconsole.log('External update changed version to:', db.read('accounts', 1).version);\n\nconst success = db.updateWithRetry('accounts', 1, { balance: 950 }, originalVersion);\nconsole.log('Retry result:', success ? 'SUCCESS' : 'FAILED');\nconsole.log('Final Alice:', db.read('accounts', 1));\n"
      }
    },

    {
      id: "db-quiz-4",
      title: "Unit 4 quiz: Transactions & Concurrency",
      kind: "quiz", xp: 10,
      brief: "ACID properties, isolation levels, pessimistic locking, optimistic locking, and handling concurrent operations. 80% to pass.",
      questions: [
        { q: "What does the 'A' in ACID stand for?",
          choices: ["Availability", "Atomicity", "Authentication", "Authorization"],
          answer: 1, explain: "Atomicity means all operations in a transaction succeed together or fail together — no partial states." },
        { q: "What's the difference between READ_COMMITTED and READ_UNCOMMITTED?",
          choices: ["READ_COMMITTED is faster", "READ_UNCOMMITTED sees uncommitted changes", "READ_COMMITTED allows phantom reads", "No difference"],
          answer: 1, explain: "READ_UNCOMMITTED allows dirty reads (seeing uncommitted changes). READ_COMMITTED only shows committed changes." },
        { q: "What's pessimistic locking?",
          choices: ["Assume conflicts won't happen", "Lock resources before accessing them", "Use version numbers to detect conflicts", "Retry on conflict automatically"],
          answer: 1, explain: "Pessimistic locking assumes conflicts will happen and locks resources upfront to prevent concurrent modifications." },
        { q: "What's optimistic locking?",
          choices: ["Lock resources before accessing", "Check for conflicts before committing", "Use exclusive locks for all operations", "Prevent all concurrent access"],
          answer: 1, explain: "Optimistic locking assumes conflicts are rare. It checks for version conflicts before committing and retries if conflicts occur." },
        { q: "When should you use pessimistic locking?",
          choices: ["Low contention, high throughput", "High contention, critical operations", "Read-heavy workloads", "When conflicts are rare"],
          answer: 1, explain: "Use pessimistic locking when there's high contention and data integrity is critical, even though it reduces concurrency." },
        { q: "What's a deadlock?",
          choices: ["A transaction that never commits", "Circular wait where transactions block each other", "A lock that never releases", "A read of uncommitted data"],
          answer: 1, explain: "Deadlock occurs when two or more transactions wait for each other in a cycle, preventing any from proceeding. Handle with retry logic." }
      ]
    }
  ]
});
