/* Database Mastery — Unit 3: NoSQL Basics */
window.CODELAB.addUnit("db", {
  id: "db-u3",
  title: "NoSQL Basics",
  icon: "📊",
  blurb: "Beyond SQL: document databases, key-value stores, graph databases, and when to use each.",
  cheat: [
    { h: "Document DB (MongoDB)", lang: "js", code: "// Insert document\ndb.users.insertOne({\n  name: 'Alice',\n  email: 'alice@example.com',\n  tags: ['developer', 'nodejs'],\n  address: { city: 'NYC', zip: '10001' }\n});\n\n// Query documents\ndb.users.find({ tags: 'nodejs' });\ndb.users.find({ 'address.city': 'NYC' });", note: "Document databases store flexible JSON-like documents. Great for hierarchical data and rapid iteration." },
    { h: "Key-Value Store (Redis)", lang: "js", code: "// Set key-value\nSET user:1 '{\"name\":\"Alice\"}'\nSET user:1:name 'Alice'\n\n// Get value\nGET user:1\nGET user:1:name\n\n// Set with expiration\nSETEX session:abc123 3600 '{\"user_id\":1}'\n\n// Lists and hashes\nLPUSH queue:tasks 'task1'\nHSET user:1 email 'alice@example.com'", note: "Key-value stores are the simplest NoSQL type. Great for caching, sessions, and simple lookups. Extremely fast." },
    { h: "Graph DB (Neo4j)", lang: "cypher", code: "// Create nodes\nCREATE (alice:Person {name: 'Alice'})\nCREATE (bob:Person {name: 'Bob'})\n\n// Create relationship\nCREATE (alice)-[:FRIEND]->(bob)\n\n// Query relationships\nMATCH (a:Person)-[:FRIEND]->(b:Person)\nWHERE a.name = 'Alice'\nRETURN b.name\n\n// Find friends of friends\nMATCH (a:Person)-[:FRIEND]->(friend)-[:FRIEND]->(fof)\nWHERE a.name = 'Alice'\nRETURN fof.name", note: "Graph databases excel at relationships and connections. Best for social networks, recommendations, and complex hierarchies." },
    { h: "SQL vs NoSQL decision", lang: "text", code: "Choose SQL when:\n- Data is structured and consistent\n- Complex queries and joins needed\n- ACID transactions are critical\n- Schema stability is important\n\nChoose NoSQL when:\n- Data is unstructured or evolving\n- Massive scale and horizontal scaling needed\n- Flexible schema is required\n- Specific use case (caching, graph, documents)", note: "SQL isn't obsolete — it's better for structured data and complex queries. NoSQL shines at scale and flexibility." }
  ],
  lessons: [

    {
      id: "db-u3-1",
      title: "Document databases: flexible schema",
      kind: "js", chip: "NOSQL", xp: 15, mins: 12,
      brief: "**Document databases** (MongoDB, CouchDB) store data as flexible JSON-like documents. Each document can have different fields.\n\n**Key features**:\n- Flexible schema (add fields without migrations)\n- Nested documents and arrays\n- Rich query language for nested data\n- Horizontal scaling via sharding\n\nYou'll build a mock document database.",
      example: { lang: "js", code: "// Document database operations\n\n// Insert document\ndb.users.insert({\n  name: 'Alice',\n  email: 'alice@example.com',\n  tags: ['developer', 'nodejs'],\n  address: { city: 'NYC', zip: '10001' }\n});\n\n// Query by nested field\ndb.users.find({ 'address.city': 'NYC' });\n\n// Query by array element\ndb.users.find({ tags: 'nodejs' });\n\n// Update nested field\ndb.users.update(\n  { name: 'Alice' },\n  { $set: { 'address.city': 'LA' } }\n);" },
      steps: [
        { text: "Create a document database with insert() method.",
          test: "T.expect(typeof db === 'object', 'Create db object');\nT.expect(typeof db.users.insert === 'function', 'Create insert method');\ndb.users.insert({ name: 'Alice', email: 'alice@example.com' });\nT.expect(db.users.data.length > 0, 'Should insert document');" },
        { text: "Implement find() for querying documents.",
          test: "T.expect(typeof db.users.find === 'function', 'Create find method');\nconst result = db.users.find({ name: 'Alice' });\nT.expect(result.length > 0, 'Should find matching documents');" },
        { text: "Support nested document queries (dot notation).",
          test: "db.users.insert({ name: 'Bob', address: { city: 'NYC' } });\nconst nested = db.users.find({ 'address.city': 'NYC' });\nT.expect(nested.length > 0, 'Should query nested fields');" },
        { text: "Support array element queries.",
          test: "db.users.insert({ name: 'Charlie', tags: ['developer', 'nodejs'] });\nconst arrayQuery = db.users.find({ tags: 'nodejs' });\nT.expect(arrayQuery.length > 0, 'Should query array elements');" }
      ],
      files: [
        { name: "script.js", content: "// Mock document database\n\nconst db = {\n  users: {\n    data: [],\n    insert: function(doc) {\n      // Insert document with auto-generated _id\n    },\n    find: function(query) {\n      // Find documents matching query\n      // Support: exact match, nested fields (dot notation), array elements\n    },\n    update: function(query, updates) {\n      // Update documents matching query\n      // Support: $set operator\n    },\n    remove: function(query) {\n      // Remove documents matching query\n    }\n  }\n};\n\n// Test insert\nconsole.log('=== Test Insert ===');\ndb.users.insert({\n  name: 'Alice',\n  email: 'alice@example.com',\n  tags: ['developer', 'nodejs'],\n  address: { city: 'NYC', zip: '10001' }\n});\n\ndb.users.insert({\n  name: 'Bob',\n  email: 'bob@example.com',\n  tags: ['designer'],\n  address: { city: 'LA', zip: '90210' }\n});\n\ndb.users.insert({\n  name: 'Charlie',\n  email: 'charlie@example.com',\n  tags: ['developer', 'python'],\n  address: { city: 'NYC', zip: '10002' }\n});\n\nconsole.log('All documents:', db.users.data);\n\n// Test find\nconsole.log('\\n=== Test Find ===');\nconsole.log('Find Alice:', db.users.find({ name: 'Alice' }));\nconsole.log('Find developers:', db.users.find({ tags: 'developer' }));\nconsole.log('Find NYC users:', db.users.find({ 'address.city': 'NYC' }));\n\n// Test update\nconsole.log('\\n=== Test Update ===');\ndb.users.update({ name: 'Alice' }, { $set: { 'address.city': 'Chicago' } });\nconsole.log('After update:', db.users.find({ name: 'Alice' }));\n\n// Test remove\nconsole.log('\\n=== Test Remove ===');\ndb.users.remove({ name: 'Bob' });\nconsole.log('After remove:', db.users.data);\n" }
      ],
      hints: [
        "For insert: add a _id field (incrementing integer) and push to data array",
        "For find: recursively check nested properties using dot notation (e.g., 'address.city' splits to ['address', 'city'])",
        "For array queries: check if the query value exists in the array",
        "For update with $set: find matching documents and merge the $set object into them"
      ],
      solution: {
        "script.js": "// Mock document database\n\nconst db = {\n  users: {\n    data: [],\n    nextId: 1,\n    insert: function(doc) {\n      const newDoc = { _id: this.nextId++, ...doc };\n      this.data.push(newDoc);\n      return newDoc;\n    },\n    find: function(query) {\n      return this.data.filter(doc => this.matchesQuery(doc, query));\n    },\n    matchesQuery: function(doc, query) {\n      for (const [key, value] of Object.entries(query)) {\n        if (key.includes('.')) {\n          // Nested query\n          const keys = key.split('.');\n          let current = doc;\n          for (const k of keys) {\n            if (!current || current[k] === undefined) return false;\n            current = current[k];\n          }\n          if (current !== value) return false;\n        } else if (Array.isArray(doc[key])) {\n          // Array query\n          if (!doc[key].includes(value)) return false;\n        } else {\n          // Simple query\n          if (doc[key] !== value) return false;\n        }\n      }\n      return true;\n    },\n    update: function(query, updates) {\n      const matches = this.find(query);\n      matches.forEach(doc => {\n        if (updates.$set) {\n          for (const [key, value] of Object.entries(updates.$set)) {\n            if (key.includes('.')) {\n              const keys = key.split('.');\n              let current = doc;\n              for (let i = 0; i < keys.length - 1; i++) {\n                if (!current[keys[i]]) current[keys[i]] = {};\n                current = current[keys[i]];\n              }\n              current[keys[keys.length - 1]] = value;\n            } else {\n              doc[key] = value;\n            }\n          }\n        }\n      });\n      return matches.length;\n    },\n    remove: function(query) {\n      const matches = this.find(query);\n      matches.forEach(doc => {\n        const index = this.data.indexOf(doc);\n        if (index > -1) this.data.splice(index, 1);\n      });\n      return matches.length;\n    }\n  }\n};\n\n// Test insert\nconsole.log('=== Test Insert ===');\ndb.users.insert({\n  name: 'Alice',\n  email: 'alice@example.com',\n  tags: ['developer', 'nodejs'],\n  address: { city: 'NYC', zip: '10001' }\n});\n\ndb.users.insert({\n  name: 'Bob',\n  email: 'bob@example.com',\n  tags: ['designer'],\n  address: { city: 'LA', zip: '90210' }\n});\n\ndb.users.insert({\n  name: 'Charlie',\n  email: 'charlie@example.com',\n  tags: ['developer', 'python'],\n  address: { city: 'NYC', zip: '10002' }\n});\n\nconsole.log('All documents:', db.users.data);\n\n// Test find\nconsole.log('\\n=== Test Find ===');\nconsole.log('Find Alice:', db.users.find({ name: 'Alice' }));\nconsole.log('Find developers:', db.users.find({ tags: 'developer' }));\nconsole.log('Find NYC users:', db.users.find({ 'address.city': 'NYC' }));\n\n// Test update\nconsole.log('\\n=== Test Update ===');\ndb.users.update({ name: 'Alice' }, { $set: { 'address.city': 'Chicago' } });\nconsole.log('After update:', db.users.find({ name: 'Alice' }));\n\n// Test remove\nconsole.log('\\n=== Test Remove ===');\ndb.users.remove({ name: 'Bob' });\nconsole.log('After remove:', db.users.data);\n"
      }
    },

    {
      id: "db-u3-2",
      title: "Key-value stores: simple and fast",
      kind: "js", chip: "NOSQL", xp: 15, mins: 12,
      brief: "**Key-value stores** (Redis, Memcached) are the simplest NoSQL type: store values by unique keys. Extremely fast O(1) lookups.\n\n**Use cases**:\n- Caching (frequently accessed data)\n- Session storage\n- Real-time leaderboards\n- Rate limiting\n- Message queues\n\nYou'll build a mock key-value store with TTL (time-to-live).",
      example: { lang: "js", code: "// Key-value store operations\n\n// Set key-value\nkv.set('user:1', { name: 'Alice', email: 'alice@example.com' });\nkv.set('user:1:name', 'Alice');\n\n// Get value\nkv.get('user:1');\nkv.get('user:1:name');\n\n// Set with expiration (TTL)\nkv.setex('session:abc123', 3600, { user_id: 1 });\n\n// Check if key exists\nkv.exists('user:1');\n\n// Delete key\nkv.del('user:1');\n\n// Increment counter\nkv.incr('counter:views');\nkv.incrby('counter:views', 10);" },
      steps: [
        { text: "Create a key-value store with set() and get() methods.",
          test: "T.expect(typeof kv === 'object', 'Create kv object');\nT.expect(typeof kv.set === 'function', 'Create set method');\nT.expect(typeof kv.get === 'function', 'Create get method');\nkv.set('test', 'value');\nT.expect(kv.get('test') === 'value', 'Should get set value');" },
        { text: "Implement setex() for keys with expiration (TTL).",
          test: "T.expect(typeof kv.setex === 'function', 'Create setex method');\nkv.setex('temp', 1, 'data');\nT.expect(kv.get('temp') === 'data', 'Should get value before expiration');" },
        { text: "Auto-expire keys after TTL expires.",
          test: "kv.setex('temp2', 0.1, 'data');\nsetTimeout(() => {\n  T.expect(kv.get('temp2') === null, 'Should expire after TTL');\n}, 150);" },
        { text: "Implement incr() and incrby() for counters.",
          test: "kv.set('counter', 5);\nkv.incr('counter');\nT.expect(kv.get('counter') === 6, 'incr should increment');\nkv.incrby('counter', 10);\nT.expect(kv.get('counter') === 16, 'incrby should increment by amount');" }
      ],
      files: [
        { name: "script.js", content: "// Mock key-value store with TTL\n\nconst kv = {\n  store: new Map(),\n  ttls: new Map(),\n  \n  set: function(key, value) {\n    // Set key-value without expiration\n  },\n  \n  get: function(key) {\n    // Get value, return null if expired or not found\n  },\n  \n  setex: function(key, ttl, value) {\n    // Set key-value with expiration (ttl in seconds)\n  },\n  \n  exists: function(key) {\n    // Check if key exists and not expired\n  },\n  \n  del: function(key) {\n    // Delete key\n  },\n  \n  incr: function(key) {\n    // Increment key by 1, create if doesn't exist\n  },\n  \n  incrby: function(key, amount) {\n    // Increment key by amount\n  },\n  \n  // Cleanup expired keys\n  cleanup: function() {\n    // Remove expired keys\n  }\n};\n\n// Test basic set/get\nconsole.log('=== Test Set/Get ===');\nkv.set('user:1', { name: 'Alice', email: 'alice@example.com' });\nkv.set('user:1:name', 'Alice');\nconsole.log('user:1:', kv.get('user:1'));\nconsole.log('user:1:name:', kv.get('user:1:name'));\n\n// Test exists\nconsole.log('\\n=== Test Exists ===');\nconsole.log('user:1 exists:', kv.exists('user:1'));\nconsole.log('nonexistent exists:', kv.exists('nonexistent'));\n\n// Test setex with TTL\nconsole.log('\\n=== Test Setex (TTL) ===');\nkv.setex('session:abc123', 2, { user_id: 1, created: Date.now() });\nconsole.log('Session:', kv.get('session:abc123'));\n\n// Wait for expiration\nsetTimeout(() => {\n  console.log('After 2 seconds:', kv.get('session:abc123'));\n  console.log('Session exists:', kv.exists('session:abc123'));\n}, 2100);\n\n// Test counters\nconsole.log('\\n=== Test Counters ===');\nkv.set('counter:views', 0);\nkv.incr('counter:views');\nkv.incr('counter:views');\nkv.incr('counter:views');\nconsole.log('Counter after 3 increments:', kv.get('counter:views'));\n\nkv.incrby('counter:views', 10);\nconsole.log('Counter after +10:', kv.get('counter:views'));\n\n// Test del\nconsole.log('\\n=== Test Del ===');\nkv.del('user:1:name');\nconsole.log('After del user:1:name:', kv.get('user:1:name'));\n\n// Run cleanup periodically\nsetInterval(() => kv.cleanup(), 1000);\n" }
      ],
      hints: [
        "For set: store value in Map, remove any existing TTL",
        "For get: check if key exists and not expired (compare current time with TTL), return null if expired",
        "For setex: store value and set expiration timestamp (current time + ttl * 1000)",
        "For incr: if key doesn't exist, start at 0, then increment. Parse as number",
        "For cleanup: iterate through all keys, remove those where TTL < current time"
      ],
      solution: {
        "script.js": "// Mock key-value store with TTL\n\nconst kv = {\n  store: new Map(),\n  ttls: new Map(),\n  \n  set: function(key, value) {\n    this.store.set(key, value);\n    this.ttls.delete(key);\n  },\n  \n  get: function(key) {\n    if (!this.store.has(key)) return null;\n    \n    const ttl = this.ttls.get(key);\n    if (ttl && ttl < Date.now()) {\n      this.store.delete(key);\n      this.ttls.delete(key);\n      return null;\n    }\n    \n    return this.store.get(key);\n  },\n  \n  setex: function(key, ttl, value) {\n    this.store.set(key, value);\n    this.ttls.set(key, Date.now() + (ttl * 1000));\n  },\n  \n  exists: function(key) {\n    return this.get(key) !== null;\n  },\n  \n  del: function(key) {\n    this.store.delete(key);\n    this.ttls.delete(key);\n  },\n  \n  incr: function(key) {\n    const current = this.get(key);\n    const value = current === null ? 0 : parseInt(current) || 0;\n    const newValue = value + 1;\n    this.set(key, newValue);\n    return newValue;\n  },\n  \n  incrby: function(key, amount) {\n    const current = this.get(key);\n    const value = current === null ? 0 : parseInt(current) || 0;\n    const newValue = value + amount;\n    this.set(key, newValue);\n    return newValue;\n  },\n  \n  cleanup: function() {\n    const now = Date.now();\n    for (const [key, ttl] of this.ttls.entries()) {\n      if (ttl < now) {\n        this.store.delete(key);\n        this.ttls.delete(key);\n      }\n    }\n  }\n};\n\n// Test basic set/get\nconsole.log('=== Test Set/Get ===');\nkv.set('user:1', { name: 'Alice', email: 'alice@example.com' });\nkv.set('user:1:name', 'Alice');\nconsole.log('user:1:', kv.get('user:1'));\nconsole.log('user:1:name:', kv.get('user:1:name'));\n\n// Test exists\nconsole.log('\\n=== Test Exists ===');\nconsole.log('user:1 exists:', kv.exists('user:1'));\nconsole.log('nonexistent exists:', kv.exists('nonexistent'));\n\n// Test setex with TTL\nconsole.log('\\n=== Test Setex (TTL) ===');\nkv.setex('session:abc123', 2, { user_id: 1, created: Date.now() });\nconsole.log('Session:', kv.get('session:abc123'));\n\n// Wait for expiration\nsetTimeout(() => {\n  console.log('After 2 seconds:', kv.get('session:abc123'));\n  console.log('Session exists:', kv.exists('session:abc123'));\n}, 2100);\n\n// Test counters\nconsole.log('\\n=== Test Counters ===');\nkv.set('counter:views', 0);\nkv.incr('counter:views');\nkv.incr('counter:views');\nkv.incr('counter:views');\nconsole.log('Counter after 3 increments:', kv.get('counter:views'));\n\nkv.incrby('counter:views', 10);\nconsole.log('Counter after +10:', kv.get('counter:views'));\n\n// Test del\nconsole.log('\\n=== Test Del ===');\nkv.del('user:1:name');\nconsole.log('After del user:1:name:', kv.get('user:1:name'));\n\n// Run cleanup periodically\nsetInterval(() => kv.cleanup(), 1000);\n"
      }
    },

    {
      id: "db-u3-3",
      title: "Graph databases: relationships",
      kind: "js", chip: "NOSQL", xp: 15, mins: 13,
      brief: "**Graph databases** (Neo4j, ArangoDB) specialize in relationships. Data is stored as nodes (entities) and edges (relationships).\n\n**Key concepts**:\n- **Nodes**: entities (Person, Company)\n- **Edges**: relationships (FRIEND, WORKS_AT)\n- **Properties**: data on nodes and edges\n- **Traversals**: following relationships (friends of friends)\n\nYou'll build a mock graph database with relationship queries.",
      example: { lang: "js", code: "// Graph database operations\n\n// Create nodes\ngraph.addNode('Person', { name: 'Alice', age: 28 });\ngraph.addNode('Person', { name: 'Bob', age: 30 });\ngraph.addNode('Company', { name: 'TechCorp' });\n\n// Create relationships\ngraph.addEdge(1, 'FRIEND', 2);\ngraph.addEdge(1, 'WORKS_AT', 3);\n\n// Query: find friends of Alice\ngraph.findRelated(1, 'FRIEND');\n\n// Query: find friends of friends\ngraph.findRelated(1, 'FRIEND', 2); // depth 2\n\n// Query: find path between nodes\ngraph.findPath(1, 2);" },
      steps: [
        { text: "Create a graph database with nodes and edges.",
          test: "T.expect(typeof graph === 'object', 'Create graph object');\nT.expect(typeof graph.addNode === 'function', 'Create addNode method');\nT.expect(typeof graph.addEdge === 'function', 'Create addEdge method');\nconst aliceId = graph.addNode('Person', { name: 'Alice' });\nT.expect(aliceId, 'Should return node id');" },
        { text: "Implement findRelated() to find nodes connected by relationship.",
          test: "const bobId = graph.addNode('Person', { name: 'Bob' });\ngraph.addEdge(aliceId, 'FRIEND', bobId);\nconst friends = graph.findRelated(aliceId, 'FRIEND');\nT.expect(friends.length > 0, 'Should find related nodes');" },
        { text: "Support multi-depth traversals (friends of friends).",
          test: "const charlieId = graph.addNode('Person', { name: 'Charlie' });\ngraph.addEdge(bobId, 'FRIEND', charlieId);\nconst fof = graph.findRelated(aliceId, 'FRIEND', 2);\nT.expect(fof.length > 0, 'Should find friends of friends');" },
        { text: "Implement findPath() to find shortest path between nodes.",
          test: "const path = graph.findPath(aliceId, charlieId);\nT.expect(path.length > 0, 'Should find path between nodes');" }
      ],
      files: [
        { name: "script.js", content: "// Mock graph database\n\nconst graph = {\n  nodes: new Map(),\n  edges: [],\n  nextNodeId: 1,\n  \n  addNode: function(label, properties) {\n    // Add node with auto-generated id\n  },\n  \n  addEdge: function(fromId, relationship, toId, properties) {\n    // Add directed edge between nodes\n  },\n  \n  findRelated: function(nodeId, relationship, depth = 1) {\n    // Find nodes connected by relationship at specified depth\n    // depth=1: direct friends, depth=2: friends of friends\n  },\n  \n  findPath: function(fromId, toId) {\n    // Find shortest path between nodes using BFS\n    // Return array of node ids\n  },\n  \n  getNode: function(nodeId) {\n    // Get node by id\n  },\n  \n  getNeighbors: function(nodeId, relationship) {\n    // Get all neighbors connected by specific relationship\n  }\n};\n\n// Create nodes\nconsole.log('=== Creating Nodes ===');\nconst alice = graph.addNode('Person', { name: 'Alice', age: 28 });\nconst bob = graph.addNode('Person', { name: 'Bob', age: 30 });\nconst charlie = graph.addNode('Person', { name: 'Charlie', age: 25 });\nconst diana = graph.addNode('Person', { name: 'Diana', age: 32 });\nconst techcorp = graph.addNode('Company', { name: 'TechCorp' });\n\nconsole.log('Nodes:', Array.from(graph.nodes.values()));\n\n// Create relationships\nconsole.log('\\n=== Creating Relationships ===');\ngraph.addEdge(alice, 'FRIEND', bob);\ngraph.addEdge(bob, 'FRIEND', charlie);\ngraph.addEdge(charlie, 'FRIEND', diana);\ngraph.addEdge(alice, 'WORKS_AT', techcorp);\ngraph.addEdge(bob, 'WORKS_AT', techcorp);\n\nconsole.log('Edges:', graph.edges);\n\n// Query: direct friends\nconsole.log('\\n=== Direct Friends of Alice ===');\nconst aliceFriends = graph.findRelated(alice, 'FRIEND');\nconsole.log('Friends:', aliceFriends.map(id => graph.getNode(id)));\n\n// Query: friends of friends\nconsole.log('\\n=== Friends of Friends of Alice ===');\nconst aliceFoF = graph.findRelated(alice, 'FRIEND', 2);\nconsole.log('Friends of friends:', aliceFoF.map(id => graph.getNode(id)));\n\n// Query: coworkers\nconsole.log('\\n=== Coworkers of Alice ===');\nconst aliceCoworkers = graph.findRelated(alice, 'WORKS_AT');\nconsole.log('Coworkers:', aliceCoworkers.map(id => graph.getNode(id)));\n\n// Query: find path\nconsole.log('\\n=== Path from Alice to Diana ===');\nconst path = graph.findPath(alice, diana);\nconsole.log('Path:', path.map(id => graph.getNode(id).name));\n" }
      ],
      hints: [
        "For addNode: create node object with id, label, properties, store in Map",
        "For addEdge: create edge object with from, relationship, to, properties, add to edges array",
        "For findRelated with depth=1: find all edges where from matches nodeId and relationship matches, return to nodes",
        "For findRelated with depth>1: recursively traverse, tracking visited nodes to avoid cycles",
        "For findPath: use BFS (breadth-first search) to find shortest path, track visited nodes and parent pointers to reconstruct path"
      ],
      solution: {
        "script.js": "// Mock graph database\n\nconst graph = {\n  nodes: new Map(),\n  edges: [],\n  nextNodeId: 1,\n  \n  addNode: function(label, properties) {\n    const node = {\n      id: this.nextNodeId++,\n      label,\n      properties: { ...properties }\n    };\n    this.nodes.set(node.id, node);\n    return node.id;\n  },\n  \n  addEdge: function(fromId, relationship, toId, properties = {}) {\n    const edge = {\n      from: fromId,\n      relationship,\n      to: toId,\n      properties: { ...properties }\n    };\n    this.edges.push(edge);\n  },\n  \n  findRelated: function(nodeId, relationship, depth = 1) {\n    if (depth === 0) return [];\n    \n    const results = new Set();\n    const visited = new Set();\n    \n    const traverse = (currentId, currentDepth) => {\n      if (currentDepth > depth) return;\n      if (visited.has(currentId)) return;\n      visited.add(currentId);\n      \n      if (currentDepth > 0) {\n        results.add(currentId);\n      }\n      \n      const neighbors = this.getNeighbors(currentId, relationship);\n      neighbors.forEach(neighborId => {\n        traverse(neighborId, currentDepth + 1);\n      });\n    };\n    \n    traverse(nodeId, 0);\n    results.delete(nodeId); // Remove starting node\n    return Array.from(results);\n  },\n  \n  findPath: function(fromId, toId) {\n    if (fromId === toId) return [fromId];\n    \n    const queue = [[fromId]];\n    const visited = new Set([fromId]);\n    \n    while (queue.length > 0) {\n      const path = queue.shift();\n      const currentId = path[path.length - 1];\n      \n      const neighbors = this.getNeighbors(currentId, null); // All relationships\n      for (const neighborId of neighbors) {\n        if (neighborId === toId) {\n          return [...path, neighborId];\n        }\n        if (!visited.has(neighborId)) {\n          visited.add(neighborId);\n          queue.push([...path, neighborId]);\n        }\n      }\n    }\n    \n    return []; // No path found\n  },\n  \n  getNode: function(nodeId) {\n    return this.nodes.get(nodeId);\n  },\n  \n  getNeighbors: function(nodeId, relationship) {\n    const neighbors = [];\n    for (const edge of this.edges) {\n      if (edge.from === nodeId) {\n        if (relationship === null || edge.relationship === relationship) {\n          neighbors.push(edge.to);\n        }\n      }\n    }\n    return neighbors;\n  }\n};\n\n// Create nodes\nconsole.log('=== Creating Nodes ===');\nconst alice = graph.addNode('Person', { name: 'Alice', age: 28 });\nconst bob = graph.addNode('Person', { name: 'Bob', age: 30 });\nconst charlie = graph.addNode('Person', { name: 'Charlie', age: 25 });\nconst diana = graph.addNode('Person', { name: 'Diana', age: 32 });\nconst techcorp = graph.addNode('Company', { name: 'TechCorp' });\n\nconsole.log('Nodes:', Array.from(graph.nodes.values()));\n\n// Create relationships\nconsole.log('\\n=== Creating Relationships ===');\ngraph.addEdge(alice, 'FRIEND', bob);\ngraph.addEdge(bob, 'FRIEND', charlie);\ngraph.addEdge(charlie, 'FRIEND', diana);\ngraph.addEdge(alice, 'WORKS_AT', techcorp);\ngraph.addEdge(bob, 'WORKS_AT', techcorp);\n\nconsole.log('Edges:', graph.edges);\n\n// Query: direct friends\nconsole.log('\\n=== Direct Friends of Alice ===');\nconst aliceFriends = graph.findRelated(alice, 'FRIEND');\nconsole.log('Friends:', aliceFriends.map(id => graph.getNode(id)));\n\n// Query: friends of friends\nconsole.log('\\n=== Friends of Friends of Alice ===');\nconst aliceFoF = graph.findRelated(alice, 'FRIEND', 2);\nconsole.log('Friends of friends:', aliceFoF.map(id => graph.getNode(id)));\n\n// Query: coworkers\nconsole.log('\\n=== Coworkers of Alice ===');\nconst aliceCoworkers = graph.findRelated(alice, 'WORKS_AT');\nconsole.log('Coworkers:', aliceCoworkers.map(id => graph.getNode(id)));\n\n// Query: find path\nconsole.log('\\n=== Path from Alice to Diana ===');\nconst path = graph.findPath(alice, diana);\nconsole.log('Path:', path.map(id => graph.getNode(id).name));\n"
      }
    },

    {
      id: "db-u3-4",
      title: "SQL vs NoSQL: choosing the right database",
      kind: "js", chip: "NOSQL", xp: 15, mins: 12,
      brief: "**Choosing between SQL and NoSQL** depends on your data structure, scale requirements, and query patterns.\n\n**SQL (PostgreSQL, MySQL)**:\n- Structured, consistent data\n- Complex queries and joins\n- ACID transactions critical\n- Schema stability important\n\n**NoSQL (MongoDB, Redis, Neo4j)**:\n- Unstructured or evolving schema\n- Massive horizontal scale\n- Specific use case (caching, graph, documents)\n- Rapid iteration needed\n\nYou'll analyze scenarios and recommend the right database.",
      example: { lang: "js", code: "// Database decision helper\n\nfunction recommendDatabase(scenario) {\n  const { dataStructure, scale, queryComplexity, transactionCritical, schemaStability } = scenario;\n  \n  if (transactionCritical && queryComplexity === 'high') {\n    return 'SQL (PostgreSQL)';\n  }\n  \n  if (scale === 'massive' && dataStructure === 'document') {\n    return 'NoSQL (MongoDB)';\n  }\n  \n  if (dataStructure === 'graph') {\n    return 'NoSQL (Neo4j)';\n  }\n  \n  if (queryComplexity === 'simple' && scale === 'high') {\n    return 'NoSQL (Redis for caching)';\n  }\n  \n  return 'SQL (PostgreSQL)'; // Default\n}\n\n// Examples\nrecommendDatabase({\n  dataStructure: 'relational',\n  scale: 'medium',\n  queryComplexity: 'high',\n  transactionCritical: true,\n  schemaStability: 'high'\n});\n// Returns: SQL (PostgreSQL)" },
      steps: [
        { text: "Create a function that analyzes database requirements.",
          test: "T.expect(typeof recommendDatabase === 'function', 'Create recommendDatabase function');\nconst rec = recommendDatabase({ scale: 'massive', dataStructure: 'document' });\nT.expect(rec, 'Should return a recommendation');" },
        { text: "Implement logic to recommend SQL for structured data with complex queries.",
          test: "const sqlRec = recommendDatabase({ dataStructure: 'relational', queryComplexity: 'high', transactionCritical: true });\nT.expect(sqlRec.includes('SQL'), 'Should recommend SQL for complex relational data');" },
        { text: "Recommend MongoDB for document data at scale.",
          test: "const mongoRec = recommendDatabase({ dataStructure: 'document', scale: 'massive' });\nT.expect(mongoRec.includes('MongoDB'), 'Should recommend MongoDB for documents at scale');" },
        { text: "Recommend Redis for simple key-value caching.",
          test: "const redisRec = recommendDatabase({ queryComplexity: 'simple', scale: 'high', useCase: 'caching' });\nT.expect(redisRec.includes('Redis'), 'Should recommend Redis for caching');" }
      ],
      files: [
        { name: "script.js", content: "// Database recommendation engine\n\nfunction recommendDatabase(requirements) {\n  const {\n    dataStructure,      // 'relational', 'document', 'key-value', 'graph'\n    scale,              // 'small', 'medium', 'massive'\n    queryComplexity,    // 'simple', 'high'\n    transactionCritical,// boolean\n    schemaStability,   // 'stable', 'evolving'\n    useCase             // 'general', 'caching', 'social', 'analytics'\n  } = requirements;\n  \n  // Decision logic\n  // Return recommendation with reasoning\n}\n\n// Test scenarios\nconst scenarios = [\n  {\n    name: 'E-commerce platform',\n    requirements: {\n      dataStructure: 'relational',\n      scale: 'medium',\n      queryComplexity: 'high',\n      transactionCritical: true,\n      schemaStability: 'stable',\n      useCase: 'general'\n    }\n  },\n  {\n    name: 'Social media feed',\n    requirements: {\n      dataStructure: 'document',\n      scale: 'massive',\n      queryComplexity: 'simple',\n      transactionCritical: false,\n      schemaStability: 'evolving',\n      useCase: 'social'\n    }\n  },\n  {\n    name: 'Session cache',\n    requirements: {\n      dataStructure: 'key-value',\n      scale: 'high',\n      queryComplexity: 'simple',\n      transactionCritical: false,\n      schemaStability: 'stable',\n      useCase: 'caching'\n    }\n  },\n  {\n    name: 'Recommendation engine',\n    requirements: {\n      dataStructure: 'graph',\n      scale: 'medium',\n      queryComplexity: 'high',\n      transactionCritical: false,\n      schemaStability: 'stable',\n      useCase: 'social'\n    }\n  },\n  {\n    name: 'Analytics dashboard',\n    requirements: {\n      dataStructure: 'document',\n      scale: 'massive',\n      queryComplexity: 'high',\n      transactionCritical: false,\n      schemaStability: 'evolving',\n      useCase: 'analytics'\n    }\n  },\n  {\n    name: 'Banking system',\n    requirements: {\n      dataStructure: 'relational',\n      scale: 'medium',\n      queryComplexity: 'high',\n      transactionCritical: true,\n      schemaStability: 'stable',\n      useCase: 'general'\n    }\n  }\n];\n\nconsole.log('=== Database Recommendations ===');\n\nscenarios.forEach(scenario => {\n  const recommendation = recommendDatabase(scenario.requirements);\n  console.log(`\\n${scenario.name}:`);\n  console.log(`  Database: ${recommendation.database}`);\n  console.log(`  Reasoning: ${recommendation.reasoning}`);\n});\n" }
      ],
      hints: [
        "For relational data with complex queries and transaction criticality: recommend SQL (PostgreSQL or MySQL)",
        "For document data with massive scale and evolving schema: recommend NoSQL (MongoDB)",
        "For key-value data with simple queries: recommend NoSQL (Redis)",
        "For graph data with relationship queries: recommend NoSQL (Neo4j)",
        "Include reasoning in the recommendation to explain the decision"
      ],
      solution: {
        "script.js": "// Database recommendation engine\n\nfunction recommendDatabase(requirements) {\n  const {\n    dataStructure,\n    scale,\n    queryComplexity,\n    transactionCritical,\n    schemaStability,\n    useCase\n  } = requirements;\n  \n  // Graph data -> Neo4j\n  if (dataStructure === 'graph') {\n    return {\n      database: 'Neo4j (Graph Database)',\n      reasoning: 'Graph databases are optimized for relationship queries and traversals. Best for social networks, recommendations, and complex hierarchies.'\n    };\n  }\n  \n  // Key-value with simple queries -> Redis\n  if (dataStructure === 'key-value' && queryComplexity === 'simple') {\n    return {\n      database: 'Redis (Key-Value Store)',\n      reasoning: 'Key-value stores provide O(1) lookups, ideal for caching, sessions, and simple data. Extremely fast and scalable.'\n    };\n  }\n  \n  // Massive scale with document data -> MongoDB\n  if (scale === 'massive' && dataStructure === 'document') {\n    return {\n      database: 'MongoDB (Document Database)',\n      reasoning: 'Document databases handle massive scale through horizontal sharding. Flexible schema supports rapid iteration and evolving data structures.'\n    };\n  }\n  \n  // Transaction critical with complex queries -> SQL\n  if (transactionCritical && queryComplexity === 'high') {\n    return {\n      database: 'PostgreSQL (Relational Database)',\n      reasoning: 'SQL databases provide ACID transactions and complex query capabilities (joins, aggregations). Essential for financial systems and data integrity.'\n    };\n  }\n  \n  // Evolving schema with document data -> MongoDB\n  if (schemaStability === 'evolving' && dataStructure === 'document') {\n    return {\n      database: 'MongoDB (Document Database)',\n      reasoning: 'Document databases have flexible schemas — add fields without migrations. Great for rapid development and changing requirements.'\n    };\n  }\n  \n  // Default to SQL for structured data\n  if (dataStructure === 'relational') {\n    return {\n      database: 'PostgreSQL (Relational Database)',\n      reasoning: 'SQL databases are best for structured, consistent data with complex relationships. Mature ecosystem with strong tooling and community support.'\n    };\n  }\n  \n  // Default fallback\n  return {\n    database: 'PostgreSQL (Relational Database)',\n    reasoning: 'SQL is a safe default for most applications. Consider NoSQL when you have specific requirements like massive scale, flexible schema, or relationship-heavy data.'\n  };\n}\n\n// Test scenarios\nconst scenarios = [\n  {\n    name: 'E-commerce platform',\n    requirements: {\n      dataStructure: 'relational',\n      scale: 'medium',\n      queryComplexity: 'high',\n      transactionCritical: true,\n      schemaStability: 'stable',\n      useCase: 'general'\n    }\n  },\n  {\n    name: 'Social media feed',\n    requirements: {\n      dataStructure: 'document',\n      scale: 'massive',\n      queryComplexity: 'simple',\n      transactionCritical: false,\n      schemaStability: 'evolving',\n      useCase: 'social'\n    }\n  },\n  {\n    name: 'Session cache',\n    requirements: {\n      dataStructure: 'key-value',\n      scale: 'high',\n      queryComplexity: 'simple',\n      transactionCritical: false,\n      schemaStability: 'stable',\n      useCase: 'caching'\n    }\n  },\n  {\n    name: 'Recommendation engine',\n    requirements: {\n      dataStructure: 'graph',\n      scale: 'medium',\n      queryComplexity: 'high',\n      transactionCritical: false,\n      schemaStability: 'stable',\n      useCase: 'social'\n    }\n  },\n  {\n    name: 'Analytics dashboard',\n    requirements: {\n      dataStructure: 'document',\n      scale: 'massive',\n      queryComplexity: 'high',\n      transactionCritical: false,\n      schemaStability: 'evolving',\n      useCase: 'analytics'\n    }\n  },\n  {\n    name: 'Banking system',\n    requirements: {\n      dataStructure: 'relational',\n      scale: 'medium',\n      queryComplexity: 'high',\n      transactionCritical: true,\n      schemaStability: 'stable',\n      useCase: 'general'\n    }\n  }\n];\n\nconsole.log('=== Database Recommendations ===');\n\nscenarios.forEach(scenario => {\n  const recommendation = recommendDatabase(scenario.requirements);\n  console.log(`\\n${scenario.name}:`);\n  console.log(`  Database: ${recommendation.database}`);\n  console.log(`  Reasoning: ${recommendation.reasoning}`);\n});\n"
      }
    },

    {
      id: "db-quiz-3",
      title: "Unit 3 quiz: NoSQL Basics",
      kind: "quiz", xp: 10,
      brief: "Document databases, key-value stores, graph databases, and SQL vs NoSQL decision making. 80% to pass.",
      questions: [
        { q: "What's the main advantage of document databases?",
          choices: ["Strict schema enforcement", "Flexible schema with nested documents", "Complex join queries", "ACID transactions"],
          answer: 1, explain: "Document databases store flexible JSON-like documents with nested structures, allowing schema evolution without migrations." },
        { q: "What are key-value stores best for?",
          choices: ["Complex relationship queries", "Caching and simple lookups", "Document storage", "Graph traversals"],
          answer: 1, explain: "Key-value stores excel at O(1) lookups, making them ideal for caching, session storage, and simple data retrieval." },
        { q: "What do graph databases specialize in?",
          choices: ["Storing JSON documents", "Fast key-value lookups", "Relationships and traversals", "Complex SQL queries"],
          answer: 2, explain: "Graph databases optimize for relationships between entities (nodes and edges), making them ideal for social networks and recommendations." },
        { q: "When should you choose SQL over NoSQL?",
          choices: ["When you need flexible schema", "When you have massive scale", "When you need complex queries and ACID transactions", "When data is unstructured"],
          answer: 2, explain: "SQL databases are better when you need complex queries, joins, and ACID transactions for data integrity. NoSQL is better for scale and flexibility." },
        { q: "What does TTL mean in key-value stores?",
          choices: ["Time To Live (expiration)", "Total Transaction Limit", "Table Transaction Lock", "Type To Load"],
          answer: 0, explain: "TTL (Time To Live) sets an expiration time for keys, useful for caching sessions and temporary data." },
        { q: "What's a node in a graph database?",
          choices: ["A relationship between entities", "An entity with properties", "A database table", "A foreign key"],
          answer: 1, explain: "Nodes represent entities (people, companies) with properties. Edges represent relationships between nodes." }
      ]
    }
  ]
});
