/* Testing Fundamentals — Unit 8: Coverage, strategy & the capstone */
window.CODELAB.addUnit("test", {
  id: "test-u8",
  title: "Coverage, strategy & the capstone",
  icon: "🎓",
  blurb: "Coverage measured honestly, the boundary bug that survives 100%, and a capstone: find two shipped bugs in a real API with nothing but your suite.",
  cheat: [
    { h: "Coverage, hand-rolled", lang: "js", code: "let hit = {};\nfunction mark(branch) { hit[branch] = true; }\n\nfunction shippingCost(order) {\n  if (order.total >= 100) { mark(\"free\"); return 0; }\n  if (order.intl)         { mark(\"intl\"); return 25; }\n  if (order.total <= 0)   { mark(\"bad\");  throw new Error(\"empty order\"); }\n  mark(\"flat\"); return 5;\n}\n// after the suite: Object.keys(hit) = branches your tests REACHED", note: "Real tools (c8/Istanbul, under `vitest run --coverage`) inject the marks automatically — their report means exactly this." },
    { h: "Coverage tells you what you TOUCHED", lang: "js", code: "// spec: $100 OR MORE ships free\nif (order.total > 100) { /* bug: exactly $100 pays $5 */ }\n\n// tests at 40 and 150: every branch runs — 100% coverage,\n// and the bug lives BETWEEN your inputs, ON the boundary\nexpect(shippingCost({ total: 100 })).toBe(0); // the test that matters", note: "A boundary gets a test AT it, not just either side of it. Coverage measures execution, never correctness." },
    { h: "The real toolchain: Vitest", lang: "js", code: "// everything you built here has the same names out there\nimport { describe, it, expect, beforeEach } from \"vitest\";\n\n// package.json → \"scripts\": { \"test\": \"vitest run\" }\n// npm test              one run, exit code for CI\n// npx vitest --watch    re-runs affected tests on every save", note: "Not runnable in this sandbox — but your describe/it/expect/beforeEach transfer verbatim." },
    { h: "CI: the suite on every push (GitHub Actions)", lang: "js", code: "# .github/workflows/test.yml\nname: tests\non: [push]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - run: npm test", note: "A fresh machine, every push, no \"works on mine\". Real YAML — it lives in the repo and runs on GitHub's servers." },
    { h: "The strategy pyramid", lang: "js", code: "//        /  E2E   \\      few  — Playwright drives a real browser\n//       / integr.  \\     some — routes, components, fakes (U6–U7)\n//      /   unit     \\    many — fast, deterministic (U1–U5)\n// slow & flaky at the top, fast & precise at the bottom", note: "Snapshots, watch mode, real E2E: named here, practiced in the wild — they need files, processes and browsers this sandbox doesn't have." }
  ],
  lessons: [

    {
      id: "test-u8-1",
      title: "Which branches did your tests reach?",
      kind: "js", chip: "TEST", xp: 15, mins: 13,
      spec: true,
      brief: "Your suite is green. Green over *what*, exactly? A test suite only checks the code it actually **runs** — and **coverage** is the measure of which lines and branches your tests ever reached.\n\nReal tools (`c8`, Istanbul) rewrite your source so every branch reports in as it executes. The shipped `shippingCost()` does the same job with six honest lines: each branch calls `mark(...)` on its way through, and the `hit` object *is* your coverage report.\n\nFour branches: **free** shipping over $100, a $25 **intl** rate, a thrown error for **bad** (empty) orders, and the $5 **flat** rate. Write tests until the suite is green *and* all four branches have actually run. The checks read `hit` and tell you exactly which branches no test ever touched — which is precisely what a coverage report is for: finding the code your tests forgot.",
      steps: [
        { text: "A green suite first: at least **four** tests, all passing against the shipped `shippingCost()`.",
          test: "T.expect(typeof shippingCost === 'function' && typeof mark === 'function', 'Leave the shipped shippingCost() and its mark() calls exactly as they are — the markers ARE the coverage tool.');\nvar r = await run();\nT.expect(r.total >= 4, 'Four branches deserve at least four tests — your suite has ' + r.total + '.');\nT.expect(r.failed === 0, 'Your suite must be green against the shipped shippingCost(). Failing: ' + r.results.filter(function (x) { return !x.pass; }).map(function (x) { return x.name; }).join(', '));" },
        { text: "Reach all three **returning** branches: `free`, `intl` and `flat` must be marked by your tests.",
          test: "Object.keys(hit).forEach(function (k) { delete hit[k]; });\nvar r = await run();\nT.expect(r.failed === 0, 'Keep the suite green while you chase branches.');\nvar missing = ['free', 'intl', 'flat'].filter(function (b) { return !hit[b]; });\nT.expect(missing.length === 0, 'Branches your tests never reached: ' + missing.join(', ') + '. free = a total over 100 → 0 · intl = { total: 40, intl: true } → 25 · flat = a plain small order → 5.');" },
        { text: "Reach the **throwing** branch too — `expect(() => …).toThrow()` — until the report reads 4/4.",
          test: "Object.keys(hit).forEach(function (k) { delete hit[k]; });\nvar r = await run();\nT.expect(r.failed === 0, 'Green, always — a red suite proves nothing about coverage.');\nvar missing = ['free', 'intl', 'bad', 'flat'].filter(function (b) { return !hit[b]; });\nT.expect(missing.length === 0, 'Branches never reached: ' + missing.join(', ') + ' — the bad branch needs expect(() => shippingCost({ total: -5 })).toThrow(): hand the function over UNCALLED; it marks bad on its way into the throw, and the matcher catches the explosion.');" }
      ],
      files: [
        { name: "script.js", content: "// ── shipped code (do not edit) ───────────────────────────────────────\n// shippingCost with hand-placed COVERAGE MARKERS: every branch calls\n// mark(...) as it runs, so `hit` records which branches your tests\n// actually reached. Real tools (c8, Istanbul) inject these marks\n// automatically — the report they print means exactly this.\nlet hit = {};\nfunction mark(branch) { hit[branch] = true; }\n\nfunction shippingCost(order) {\n  if (order.total > 100) { mark(\"free\"); return 0; }   // free over $100\n  if (order.intl)        { mark(\"intl\"); return 25; }  // international\n  if (order.total <= 0)  { mark(\"bad\");  throw new Error(\"empty order\"); }\n  mark(\"flat\");\n  return 5;                                            // flat rate\n}\n\n// ── your suite: one test per branch ──────────────────────────────────\ndescribe(\"shippingCost\", () => {\n  it(\"ships free over $100\", () => {\n    expect(shippingCost({ total: 150 })).toBe(0);\n  });\n  // three branches have never run under a test…\n});\n\nrun();\n" }
      ],
      hints: [
        "The international branch: `expect(shippingCost({ total: 40, intl: true })).toBe(25);` — any small total with `intl: true`.",
        "The throwing branch is a `toThrow` job: `expect(() => shippingCost({ total: -5 })).toThrow();` — wrap it, don't call it.",
        "The flat branch is the one nobody writes on purpose: `expect(shippingCost({ total: 40 })).toBe(5);` — a boring order is still a branch."
      ],
      solution: {
        "script.js": "// ── shipped code (do not edit) ───────────────────────────────────────\nlet hit = {};\nfunction mark(branch) { hit[branch] = true; }\n\nfunction shippingCost(order) {\n  if (order.total > 100) { mark(\"free\"); return 0; }   // free over $100\n  if (order.intl)        { mark(\"intl\"); return 25; }  // international\n  if (order.total <= 0)  { mark(\"bad\");  throw new Error(\"empty order\"); }\n  mark(\"flat\");\n  return 5;                                            // flat rate\n}\n\n// ── your suite: one test per branch ──────────────────────────────────\ndescribe(\"shippingCost\", () => {\n  it(\"ships free over $100\", () => {\n    expect(shippingCost({ total: 150 })).toBe(0);\n  });\n  it(\"charges $25 international\", () => {\n    expect(shippingCost({ total: 40, intl: true })).toBe(25);\n  });\n  it(\"throws on a negative total\", () => {\n    expect(() => shippingCost({ total: -5 })).toThrow();\n  });\n  it(\"charges the $5 flat rate\", () => {\n    expect(shippingCost({ total: 40 })).toBe(5);\n  });\n});\n\nrun();\n"
      }
    },

    {
      id: "test-u8-2",
      title: "100% coverage, still broken",
      kind: "js", chip: "TEST", xp: 15, mins: 13,
      spec: true,
      brief: "The spec changed this sprint: orders of **$100 or more** now ship free. The function was updated (`>=`), your suite from last lesson came along — green bar, 4/4 branches, **100% coverage**. Time for the uncomfortable question: what is that number actually promising you?\n\nImagine a teammate refactors and types `>` where the spec means `>=`. Your test at $150 still passes. Your test at $40 still passes. Every branch still runs — the coverage report doesn't so much as blink. 100%, bug shipped: a customer at exactly $100 pays $5 they were promised not to.\n\nThe checks below make that exact mistake behind your back — and one more like it at the zero boundary — and demand your suite notices. **Coverage tells you what you TOUCHED, never what you CHECKED.** The cure is always the same: put a test exactly *on* every boundary, not just either side of it.",
      steps: [
        { text: "The premise, proven: your suite is green and all four branches run — 100% coverage.",
          test: "Object.keys(hit).forEach(function (k) { delete hit[k]; });\nvar r = await run();\nT.expect(r.total >= 4 && r.failed === 0, 'Start from the shipped suite: at least four tests, all green against shippingCost().');\nvar missing = ['free', 'intl', 'bad', 'flat'].filter(function (b) { return !hit[b]; });\nT.expect(missing.length === 0, 'All four branches must run under your tests — never reached: ' + missing.join(', '));" },
        { text: "A teammate types `>` where the spec says `>=`. Your suite must go red — add the test at **exactly** $100.",
          test: "var r = await T.mutate('shippingCost', function (order) { if (order.total > 100) { return 0; } if (order.intl) { return 25; } if (order.total <= 0) { throw new Error('empty order'); } return 5; }, function () { return run(); });\nT.expect(r.failed > 0, 'Every branch ran — and the bug survived. A version that ships free only ABOVE $100 (> where the spec says $100 OR MORE) passed your 100%-coverage suite: $150 and $40 answer the same under both versions. Coverage tells you what you TOUCHED, never what you CHECKED. Add the boundary test: expect(shippingCost({ total: 100 })).toBe(0).');\nvar r2 = await run();\nT.eq(r2.failed, 0, 'And against the real shippingCost the new test must pass — exactly $100 ships free.');" },
        { text: "Same story at zero: a version that only throws **below** zero must be caught by a test at exactly `0`.",
          test: "var r = await T.mutate('shippingCost', function (order) { if (order.total >= 100) { return 0; } if (order.intl) { return 25; } if (order.total < 0) { throw new Error('empty order'); } return 5; }, function () { return run(); });\nT.expect(r.failed > 0, 'A version that only throws BELOW zero (< 0 instead of <= 0) survived — your empty-order test uses -5, so an order of exactly $0 quietly ships for $5. Boundaries live at the =: add expect(() => shippingCost({ total: 0 })).toThrow().');\nvar r2 = await run();\nT.eq(r2.failed, 0, 'Both boundary tests green against the real code: $100 ships free, $0 throws.');" }
      ],
      files: [
        { name: "script.js", content: "// ── shipped code (do not edit) ───────────────────────────────────────\n// New spec this sprint: orders of $100 OR MORE ship free (>=).\nlet hit = {};\nfunction mark(branch) { hit[branch] = true; }\n\nfunction shippingCost(order) {\n  if (order.total >= 100) { mark(\"free\"); return 0; }  // $100 or more: free\n  if (order.intl)         { mark(\"intl\"); return 25; }\n  if (order.total <= 0)   { mark(\"bad\");  throw new Error(\"empty order\"); }\n  mark(\"flat\");\n  return 5;\n}\n\n// ── the suite that \"covers everything\" ───────────────────────────────\ndescribe(\"shippingCost\", () => {\n  it(\"ships free over $100\", () => {\n    expect(shippingCost({ total: 150 })).toBe(0);\n  });\n  it(\"charges $25 international\", () => {\n    expect(shippingCost({ total: 40, intl: true })).toBe(25);\n  });\n  it(\"throws on a negative total\", () => {\n    expect(() => shippingCost({ total: -5 })).toThrow();\n  });\n  it(\"charges the $5 flat rate\", () => {\n    expect(shippingCost({ total: 40 })).toBe(5);\n  });\n  // 100% coverage. Now run the checks — and read what survives it.\n});\n\nrun();\n" }
      ],
      hints: [
        "The whole lesson is two new tests: one at `{ total: 100 }` expecting `0`, one at `{ total: 0 }` expecting a throw.",
        "`>` and `>=` only disagree at exactly 100 — 99, 101 and 150 answer the same under both. That's why the test has to sit ON the boundary.",
        "For the zero boundary: `expect(() => shippingCost({ total: 0 })).toThrow()` — your -5 test proves negatives throw; only 0 pins the `<=`."
      ],
      solution: {
        "script.js": "// ── shipped code (do not edit) ───────────────────────────────────────\nlet hit = {};\nfunction mark(branch) { hit[branch] = true; }\n\nfunction shippingCost(order) {\n  if (order.total >= 100) { mark(\"free\"); return 0; }  // $100 or more: free\n  if (order.intl)         { mark(\"intl\"); return 25; }\n  if (order.total <= 0)   { mark(\"bad\");  throw new Error(\"empty order\"); }\n  mark(\"flat\");\n  return 5;\n}\n\n// ── the suite, now with its boundaries pinned ────────────────────────\ndescribe(\"shippingCost\", () => {\n  it(\"ships free over $100\", () => {\n    expect(shippingCost({ total: 150 })).toBe(0);\n  });\n  it(\"ships free at exactly $100\", () => {\n    expect(shippingCost({ total: 100 })).toBe(0);\n  });\n  it(\"charges $25 international\", () => {\n    expect(shippingCost({ total: 40, intl: true })).toBe(25);\n  });\n  it(\"throws on a negative total\", () => {\n    expect(() => shippingCost({ total: -5 })).toThrow();\n  });\n  it(\"throws at exactly $0\", () => {\n    expect(() => shippingCost({ total: 0 })).toThrow();\n  });\n  it(\"charges the $5 flat rate\", () => {\n    expect(shippingCost({ total: 40 })).toBe(5);\n  });\n});\n\nrun();\n"
      }
    },

    {
      id: "test-u8-p",
      title: "Capstone: Bookmarks API test suite",
      kind: "js", chip: "TEST", xp: 50, mins: 45, project: true,
      spec: true,
      brief: "The graduation exercise, and the job you'll actually be hired to do: **test code you didn't write**. The shipped handler below is the Bookmarks API v1 straight out of Back-End Foundations — five routes, validation envelopes, `{ items, total }` lists. QA swears **two behaviors break the spec**. Nobody knows where.\n\nYour deliverable is the **suite** — ~40 lines of tests that read the spec comment at the top of the file and hold every route to it. Write it against the shipped code and watch at least two tests go red: that's the bugs, found. Then — and only then — fix the handler (about **4 lines**) and watch the same suite go green.\n\nThe final checkpoint is this course's signature move, one last time: four mutant handlers — subtle, plausible, wrong — get swapped in behind your back, and your suite must kill every one. Assert whole shapes with `toEqual` (loose truthy checks feed mutants), test every 404 the spec promises, and remember the store is stateful — the `beforeEach` reset ships in the starter for a reason.",
      steps: [
        { text: "**Red first:** your suite must fail at least twice, in two *different* tests, against the shipped handler — QA is right, two spec violations shipped.",
          test: "T.expect(typeof handleRequest === 'function', 'Keep handleRequest as a function declaration — the checks swap it for broken versions, and const/let would block the swap.');\nT.expect(typeof bookmarks !== 'undefined' && typeof nextId === 'number', 'Keep the shipped store — let bookmarks = [] and let nextId = 1 — your beforeEach and the checks both reset it.');\nself.__mkHandler = function (mod) {\n  mod = mod || {};\n  return function (req) {\n    if (req.method === 'GET' && req.path === '/api/bookmarks') {\n      var qs = req.query || {};\n      var matches = bookmarks;\n      if (!mod.deafToQ && typeof qs.q === 'string' && qs.q !== '') {\n        matches = bookmarks.filter(function (b) { return b.title.toLowerCase().indexOf(qs.q.toLowerCase()) !== -1; });\n      }\n      var items = matches;\n      if (qs.limit !== undefined) { items = matches.slice(0, Number(qs.limit)); }\n      return { status: 200, body: { items: items, total: matches.length } };\n    }\n    if (req.method === 'POST' && req.path === '/api/bookmarks') {\n      if (!req.body || typeof req.body.title !== 'string' || (!mod.emptyTitleOk && req.body.title.trim() === '')) {\n        return { status: 400, body: { error: { code: 'invalid', message: 'title is required' } } };\n      }\n      if (typeof req.body.url !== 'string' || req.body.url.indexOf('http') !== 0) {\n        return { status: 400, body: { error: { code: 'invalid', message: 'url must start with http' } } };\n      }\n      var bm = { id: mod.stuckId ? nextId : nextId++, title: req.body.title, url: req.body.url };\n      bookmarks.push(bm);\n      return { status: 201, body: bm };\n    }\n    if (req.path.indexOf('/api/bookmarks/') === 0) {\n      var id = Number(req.path.split('/')[3]);\n      var found = bookmarks.find(function (b) { return b.id === id; });\n      if (req.method === 'GET') {\n        if (!found) { return { status: 404, body: { error: { code: 'not_found', message: 'bookmark not found' } } }; }\n        return { status: 200, body: found };\n      }\n      if (req.method === 'PATCH') {\n        if (!found) { return { status: 404, body: { error: { code: 'not_found', message: 'bookmark not found' } } }; }\n        if (mod.patchReplaces) {\n          found.title = req.body ? req.body.title : undefined;\n          found.url = req.body ? req.body.url : undefined;\n        } else {\n          if (req.body && typeof req.body.title === 'string') { found.title = req.body.title; }\n          if (req.body && typeof req.body.url === 'string') { found.url = req.body.url; }\n        }\n        return { status: 200, body: found };\n      }\n      if (req.method === 'DELETE') {\n        if (!mod.deleteBlind && !found) { return { status: 404, body: { error: { code: 'not_found', message: 'bookmark not found' } } }; }\n        bookmarks = bookmarks.filter(function (b) { return b.id !== id; });\n        return { status: 200, body: { ok: true } };\n      }\n    }\n    if (mod.answers405 && req.path === '/api/bookmarks') {\n      return { status: 405, body: { error: { code: 'method_not_allowed', message: 'method not allowed' } } };\n    }\n    return { status: 404, body: { error: { code: 'not_found', message: 'no such route' } } };\n  };\n};\nbookmarks = []; nextId = 1;\nvar r = await T.mutate('handleRequest', self.__mkHandler({ emptyTitleOk: true, deleteBlind: true }), function () { return run(); });\nT.expect(r.total >= 6, 'Five routes plus validation deserve more than ' + r.total + ' test(s) — cover the list (?q, ?limit), the create with both 400 envelopes, and the detail GET/PATCH/DELETE with their 404s.');\nvar reds = [];\nr.results.forEach(function (x) { if (!x.pass && reds.indexOf(x.name) === -1) { reds.push(x.name); } });\nT.expect(r.failed >= 2 && reds.length >= 2, 'QA is right: the shipped handler breaks the spec in TWO separate places — one lets junk input in, one reports success for a thing that does not exist. Your suite caught ' + (reds.length ? 'only: ' + reds.join(', ') : 'neither') + '. Hold every promise in the spec comment to account: both 400 envelopes, every 404.');" },
        { text: "**Green after the fix:** patch the handler (~4 lines) until your suite *and* the checkpoint's own route-by-route audit pass — twice in a row.",
          test: "var r = await run();\nT.expect(r.failed === 0, 'Suite red against the CURRENT code. If you have not fixed the handler yet: the whole fix is about 4 lines — one extra condition in the POST validation, one guard at the top of DELETE. Still failing: ' + r.results.filter(function (x) { return !x.pass; }).map(function (x) { return x.name; }).join(', '));\nvar r2 = await run();\nT.expect(r2.failed === 0 && r2.total === r.total, 'Your suite passes once but not twice — a test is leaking state into the next. Keep the starter beforeEach: bookmarks = []; nextId = 1;');\nbookmarks = []; nextId = 1;\nT.eq(handleRequest({ method: 'GET', path: '/api/bookmarks', body: null }), { status: 200, body: { items: [], total: 0 } }, 'Empty store, no query object at all → { items: [], total: 0 }');\nT.eq(handleRequest({ method: 'POST', path: '/api/bookmarks', body: { title: 'MDN Web Docs', url: 'https://developer.mozilla.org' } }), { status: 201, body: { id: 1, title: 'MDN Web Docs', url: 'https://developer.mozilla.org' } }, 'First create → 201 with id 1');\nhandleRequest({ method: 'POST', path: '/api/bookmarks', body: { title: 'Node.js Docs', url: 'https://nodejs.org' } });\nhandleRequest({ method: 'POST', path: '/api/bookmarks', body: { title: 'CSS Tricks', url: 'https://css-tricks.com' } });\nT.eq(handleRequest({ method: 'POST', path: '/api/bookmarks', body: { title: '', url: 'https://ok.example' } }), { status: 400, body: { error: { code: 'invalid', message: 'title is required' } } }, 'BUG #1: an empty title must answer the 400 envelope — the shipped check forgot to .trim()');\nT.eq(handleRequest({ method: 'POST', path: '/api/bookmarks', body: { title: '   ', url: 'https://ok.example' } }), { status: 400, body: { error: { code: 'invalid', message: 'title is required' } } }, 'A whitespace-only title is an empty title — .trim() catches both');\nT.eq(handleRequest({ method: 'POST', path: '/api/bookmarks', body: null }), { status: 400, body: { error: { code: 'invalid', message: 'title is required' } } }, 'A null body still fails the title check first');\nT.eq(handleRequest({ method: 'POST', path: '/api/bookmarks', body: { title: 'Old FTP', url: 'ftp://files.example' } }), { status: 400, body: { error: { code: 'invalid', message: 'url must start with http' } } }, 'Non-http url → 400 envelope, unchanged');\nT.eq(handleRequest({ method: 'GET', path: '/api/bookmarks', query: {}, body: null }).body.total, 3, 'Rejected creates must never touch the store');\nT.eq(handleRequest({ method: 'GET', path: '/api/bookmarks', query: { q: 'DOCS' }, body: null }), { status: 200, body: { items: [{ id: 1, title: 'MDN Web Docs', url: 'https://developer.mozilla.org' }, { id: 2, title: 'Node.js Docs', url: 'https://nodejs.org' }], total: 2 } }, '?q=DOCS filters titles case-insensitively');\nT.eq(handleRequest({ method: 'GET', path: '/api/bookmarks', query: { q: 'docs', limit: '1' }, body: null }), { status: 200, body: { items: [{ id: 1, title: 'MDN Web Docs', url: 'https://developer.mozilla.org' }], total: 2 } }, 'q + limit combine: 1 shown, 2 matched');\nT.eq(handleRequest({ method: 'PATCH', path: '/api/bookmarks/2', body: { title: 'Node.js Documentation' } }), { status: 200, body: { id: 2, title: 'Node.js Documentation', url: 'https://nodejs.org' } }, 'PATCH still merges — the url survives a title-only patch');\nT.eq(handleRequest({ method: 'DELETE', path: '/api/bookmarks/42', body: null }), { status: 404, body: { error: { code: 'not_found', message: 'bookmark not found' } } }, 'BUG #2: deleting a missing id must answer the 404 envelope — the shipped DELETE never checks whether the record exists');\nT.eq(handleRequest({ method: 'DELETE', path: '/api/bookmarks/1', body: null }), { status: 200, body: { ok: true } }, 'Deleting a real record still answers { ok: true }');\nT.eq(handleRequest({ method: 'DELETE', path: '/api/bookmarks/1', body: null }), { status: 404, body: { error: { code: 'not_found', message: 'bookmark not found' } } }, 'Deleting the same id twice → 404');\nT.eq(handleRequest({ method: 'GET', path: '/api/bookmarks', query: {}, body: null }), { status: 200, body: { items: [{ id: 2, title: 'Node.js Documentation', url: 'https://nodejs.org' }, { id: 3, title: 'CSS Tricks', url: 'https://css-tricks.com' }], total: 2 } }, 'Final state: #2 (patched) and #3, total 2');" },
        { text: "**Prove the suite has teeth:** four mutant handlers — a stuck id counter, a 405 where the spec says 404, a deaf `?q`, a PATCH that replaces — each must be caught.",
          test: "T.expect(typeof self.__mkHandler === 'function', 'Run all checks together — the mutant factory is set up in the first step.');\nbookmarks = []; nextId = 1;\nvar r = await T.mutate('handleRequest', self.__mkHandler({ stuckId: true }), function () { return run(); });\nT.expect(r.failed > 0, 'MUTANT SURVIVED: a handler whose id counter never moves — every create answers { id: 1, ... } — passed your whole suite. Create TWO bookmarks in one test and assert the second one is id 2.');\nbookmarks = []; nextId = 1;\nr = await T.mutate('handleRequest', self.__mkHandler({ answers405: true }), function () { return run(); });\nT.expect(r.failed > 0, 'MUTANT SURVIVED: a handler that answers 405 method-not-allowed for a wrong verb on /api/bookmarks. Defensible design — but NOT this spec: v1 promises every unknown route or verb the 404 no-such-route envelope. Add a test: PUT /api/bookmarks must toEqual the full 404 envelope.');\nbookmarks = []; nextId = 1;\nr = await T.mutate('handleRequest', self.__mkHandler({ deafToQ: true }), function () { return run(); });\nT.expect(r.failed > 0, 'MUTANT SURVIVED: a list route that ignores ?q and returns everything — right SHAPE, wrong CONTENTS, so loose truthy assertions never notice. Seed a few bookmarks, search a term matching only some, and toEqual the exact items and total.');\nbookmarks = []; nextId = 1;\nr = await T.mutate('handleRequest', self.__mkHandler({ patchReplaces: true }), function () { return run(); });\nT.expect(r.failed > 0, 'MUTANT SURVIVED: a PATCH that REPLACES the record instead of merging — a title-only patch silently wipes the url. Patch ONE field and toEqual the whole record, untouched fields included.');\nvar rr = await run();\nT.eq(rr.failed, 0, 'And against the real, fixed handler the same suite stays green — that is the whole game: red for every wrong version, green for the right one.');" }
      ],
      files: [
        { name: "script.js", content: "// ===== Bookmarks API v1 — SHIPPED CODE (you didn't write this) =====\n// The spec it must honor:\n//   request  = { method, path, query?, body? }   query MAY be missing\n//   GET    /api/bookmarks        → 200 { items, total }\n//          ?q=      case-insensitive title filter\n//          ?limit=  slice items (a STRING); total counts matches BEFORE the limit\n//   POST   /api/bookmarks        → validate title then url → 400 envelope; valid → 201 created (counter ids)\n//   GET    /api/bookmarks/<id>   → 200 record | 404 \"bookmark not found\"\n//   PATCH  /api/bookmarks/<id>   → MERGE title/url → 200 | 404 envelope\n//   DELETE /api/bookmarks/<id>   → 200 { ok: true } | 404 envelope\n//   anything else                → 404 { error: { code: \"not_found\", message: \"no such route\" } }\n// envelopes: 400 { error: { code: \"invalid\",   message: … } }\n//            404 { error: { code: \"not_found\", message: … } }\n//\n// QA says TWO of these promises are broken. Your tests find them first.\n\nlet bookmarks = [];\nlet nextId = 1;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/bookmarks\") {\n    const q = req.query || {};\n    let matches = bookmarks;\n    if (typeof q.q === \"string\" && q.q !== \"\") {\n      const needle = q.q.toLowerCase();\n      matches = bookmarks.filter(b => b.title.toLowerCase().includes(needle));\n    }\n    let items = matches;\n    if (q.limit !== undefined) {\n      items = matches.slice(0, Number(q.limit));\n    }\n    return { status: 200, body: { items: items, total: matches.length } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/bookmarks\") {\n    if (!req.body || typeof req.body.title !== \"string\") {\n      return { status: 400, body: { error: { code: \"invalid\", message: \"title is required\" } } };\n    }\n    if (typeof req.body.url !== \"string\" || !req.body.url.startsWith(\"http\")) {\n      return { status: 400, body: { error: { code: \"invalid\", message: \"url must start with http\" } } };\n    }\n    const bm = { id: nextId++, title: req.body.title, url: req.body.url };\n    bookmarks.push(bm);\n    return { status: 201, body: bm };\n  }\n\n  if (req.path.startsWith(\"/api/bookmarks/\")) {\n    const id = Number(req.path.split(\"/\")[3]);\n    const found = bookmarks.find(b => b.id === id);\n\n    if (req.method === \"GET\") {\n      if (!found) return { status: 404, body: { error: { code: \"not_found\", message: \"bookmark not found\" } } };\n      return { status: 200, body: found };\n    }\n    if (req.method === \"PATCH\") {\n      if (!found) return { status: 404, body: { error: { code: \"not_found\", message: \"bookmark not found\" } } };\n      if (req.body && typeof req.body.title === \"string\") found.title = req.body.title;\n      if (req.body && typeof req.body.url === \"string\") found.url = req.body.url;\n      return { status: 200, body: found };\n    }\n    if (req.method === \"DELETE\") {\n      bookmarks = bookmarks.filter(b => b.id !== id);\n      return { status: 200, body: { ok: true } };\n    }\n  }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"no such route\" } } };\n}\n\n// ===== YOUR SUITE (the deliverable) ==================================\n// Test the spec, route by route. At least two tests should go RED —\n// that's the bugs, found. Only then fix the handler (~4 lines).\n\nbeforeEach(() => { bookmarks = []; nextId = 1; });   // a fresh store for every test\n\ndescribe(\"GET /api/bookmarks\", () => {\n  // it(\"answers { items: [], total: 0 } on an empty store\", () => { … });\n});\n\nrun();\n" }
      ],
      hints: [
        "Read the spec comment like a ticket: one `describe` per route group, one `it` per promise — both 400 envelopes, all the detail 404s, the catch-all, `?q`, `?limit`, merge behavior, counter ids. A helper pays for itself: `function post(title, url) { return handleRequest({ method: \"POST\", path: \"/api/bookmarks\", body: { title, url } }); }`",
        "Assert whole shapes: `expect(res).toEqual({ status: 400, body: { error: { code: \"invalid\", message: \"title is required\" } } })` — exact envelopes are what kill mutants. And test the 404 paths the spec promises: `GET /api/bookmarks/99`, `DELETE /api/bookmarks/42`, `PUT /api/bookmarks`.",
        "Stuck after honest red-hunting? The bugs: POST forgot `req.body.title.trim() === \"\"` in its title check, and DELETE never checks `found` before answering `{ ok: true }`. Fix both exactly like the other routes already do it."
      ],
      solution: {
        "script.js": "// ===== Bookmarks API v1 — fixed (the ~4 changed lines are marked) =====\n\nlet bookmarks = [];\nlet nextId = 1;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/bookmarks\") {\n    const q = req.query || {};\n    let matches = bookmarks;\n    if (typeof q.q === \"string\" && q.q !== \"\") {\n      const needle = q.q.toLowerCase();\n      matches = bookmarks.filter(b => b.title.toLowerCase().includes(needle));\n    }\n    let items = matches;\n    if (q.limit !== undefined) {\n      items = matches.slice(0, Number(q.limit));\n    }\n    return { status: 200, body: { items: items, total: matches.length } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/bookmarks\") {\n    if (!req.body || typeof req.body.title !== \"string\" ||\n        req.body.title.trim() === \"\") {                      // FIX #1: empty titles\n      return { status: 400, body: { error: { code: \"invalid\", message: \"title is required\" } } };\n    }\n    if (typeof req.body.url !== \"string\" || !req.body.url.startsWith(\"http\")) {\n      return { status: 400, body: { error: { code: \"invalid\", message: \"url must start with http\" } } };\n    }\n    const bm = { id: nextId++, title: req.body.title, url: req.body.url };\n    bookmarks.push(bm);\n    return { status: 201, body: bm };\n  }\n\n  if (req.path.startsWith(\"/api/bookmarks/\")) {\n    const id = Number(req.path.split(\"/\")[3]);\n    const found = bookmarks.find(b => b.id === id);\n\n    if (req.method === \"GET\") {\n      if (!found) return { status: 404, body: { error: { code: \"not_found\", message: \"bookmark not found\" } } };\n      return { status: 200, body: found };\n    }\n    if (req.method === \"PATCH\") {\n      if (!found) return { status: 404, body: { error: { code: \"not_found\", message: \"bookmark not found\" } } };\n      if (req.body && typeof req.body.title === \"string\") found.title = req.body.title;\n      if (req.body && typeof req.body.url === \"string\") found.url = req.body.url;\n      return { status: 200, body: found };\n    }\n    if (req.method === \"DELETE\") {\n      if (!found) return { status: 404, body: { error: { code: \"not_found\", message: \"bookmark not found\" } } };  // FIX #2\n      bookmarks = bookmarks.filter(b => b.id !== id);\n      return { status: 200, body: { ok: true } };\n    }\n  }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"no such route\" } } };\n}\n\n// ===== THE SUITE =====================================================\n\nbeforeEach(() => { bookmarks = []; nextId = 1; });   // a fresh store for every test\n\nfunction post(title, url) {\n  return handleRequest({ method: \"POST\", path: \"/api/bookmarks\", body: { title: title, url: url } });\n}\nfunction seed() {\n  post(\"MDN Web Docs\", \"https://developer.mozilla.org\");\n  post(\"Node.js Docs\", \"https://nodejs.org\");\n  post(\"CSS Tricks\", \"https://css-tricks.com\");\n}\n\ndescribe(\"GET /api/bookmarks\", () => {\n  it(\"answers { items: [], total: 0 } on an empty store\", () => {\n    expect(handleRequest({ method: \"GET\", path: \"/api/bookmarks\", body: null }))\n      .toEqual({ status: 200, body: { items: [], total: 0 } });\n  });\n  it(\"filters by ?q case-insensitively, total counts the matches\", () => {\n    seed();\n    expect(handleRequest({ method: \"GET\", path: \"/api/bookmarks\", query: { q: \"DOCS\" }, body: null }))\n      .toEqual({ status: 200, body: { items: [\n        { id: 1, title: \"MDN Web Docs\", url: \"https://developer.mozilla.org\" },\n        { id: 2, title: \"Node.js Docs\", url: \"https://nodejs.org\" }\n      ], total: 2 } });\n  });\n  it(\"slices with ?limit but counts total before the slice\", () => {\n    seed();\n    const res = handleRequest({ method: \"GET\", path: \"/api/bookmarks\", query: { limit: \"2\" }, body: null });\n    expect(res.body.items.length).toBe(2);\n    expect(res.body.total).toBe(3);\n  });\n});\n\ndescribe(\"POST /api/bookmarks\", () => {\n  it(\"creates with 201 and counter ids\", () => {\n    expect(post(\"MDN Web Docs\", \"https://developer.mozilla.org\"))\n      .toEqual({ status: 201, body: { id: 1, title: \"MDN Web Docs\", url: \"https://developer.mozilla.org\" } });\n    expect(post(\"Node.js Docs\", \"https://nodejs.org\").body.id).toBe(2);\n  });\n  it(\"rejects an empty title with the 400 envelope\", () => {\n    expect(post(\"\", \"https://ok.example\"))\n      .toEqual({ status: 400, body: { error: { code: \"invalid\", message: \"title is required\" } } });\n    expect(handleRequest({ method: \"GET\", path: \"/api/bookmarks\", body: null }).body.total).toBe(0);\n  });\n  it(\"rejects a non-http url with the 400 envelope\", () => {\n    expect(post(\"Old FTP\", \"ftp://files.example\"))\n      .toEqual({ status: 400, body: { error: { code: \"invalid\", message: \"url must start with http\" } } });\n  });\n});\n\ndescribe(\"detail routes\", () => {\n  it(\"GET /:id returns the record, 404 when missing\", () => {\n    seed();\n    expect(handleRequest({ method: \"GET\", path: \"/api/bookmarks/2\", body: null }))\n      .toEqual({ status: 200, body: { id: 2, title: \"Node.js Docs\", url: \"https://nodejs.org\" } });\n    expect(handleRequest({ method: \"GET\", path: \"/api/bookmarks/99\", body: null }).status).toBe(404);\n  });\n  it(\"PATCH merges — a title-only patch keeps the url\", () => {\n    seed();\n    expect(handleRequest({ method: \"PATCH\", path: \"/api/bookmarks/2\", body: { title: \"Node.js Documentation\" } }))\n      .toEqual({ status: 200, body: { id: 2, title: \"Node.js Documentation\", url: \"https://nodejs.org\" } });\n  });\n  it(\"DELETE of a missing id answers the 404 envelope\", () => {\n    seed();\n    expect(handleRequest({ method: \"DELETE\", path: \"/api/bookmarks/42\", body: null }))\n      .toEqual({ status: 404, body: { error: { code: \"not_found\", message: \"bookmark not found\" } } });\n  });\n  it(\"DELETE removes the record; deleting twice answers 404\", () => {\n    seed();\n    expect(handleRequest({ method: \"DELETE\", path: \"/api/bookmarks/1\", body: null }))\n      .toEqual({ status: 200, body: { ok: true } });\n    expect(handleRequest({ method: \"DELETE\", path: \"/api/bookmarks/1\", body: null }).status).toBe(404);\n    expect(handleRequest({ method: \"GET\", path: \"/api/bookmarks\", body: null }).body.total).toBe(2);\n  });\n});\n\ndescribe(\"routing\", () => {\n  it(\"unknown paths and wrong verbs get the no-such-route envelope\", () => {\n    expect(handleRequest({ method: \"GET\", path: \"/api/nope\", body: null }))\n      .toEqual({ status: 404, body: { error: { code: \"not_found\", message: \"no such route\" } } });\n    expect(handleRequest({ method: \"PUT\", path: \"/api/bookmarks\", body: null }))\n      .toEqual({ status: 404, body: { error: { code: \"not_found\", message: \"no such route\" } } });\n  });\n});\n\nrun();\n"
      }
    },

    {
      id: "test-quiz-8",
      title: "Final quiz: Testing Fundamentals",
      kind: "quiz", xp: 10,
      brief: "The closing exam: what coverage measures, what it never can, and the real toolchain waiting outside — Vitest, CI, Playwright. 80% to pass.",
      questions: [
        { q: "Your suite reaches 100% branch coverage. What is actually guaranteed?",
          choices: ["Every behavior promised by the spec has its own assertion", "Every branch ran at least once while the tests executed", "No subtly broken version of the code could survive the suite", "Every input boundary was tested with an exact value"],
          answer: 1, explain: "Coverage instruments execution: a branch counts as covered the moment it RUNS under any test — even a test that asserts nothing about it. Whether the result was correct, or checked at all, is invisible to the report. That makes 100% a floor for confidence, not a ceiling: it finds the code your tests forgot, and says nothing about the quality of the checks themselves." },
        { q: "This refactor shipped. Your 100%-coverage suite — tests at totals 40, 150 and -5 — stays green. Which single test exposes the bug?",
          code: "// spec: orders of $100 OR MORE ship free\nif (order.total > 100) { mark(\"free\"); return 0; }",
          lang: "js",
          choices: ["`shippingCost({ total: 101 })`", "`shippingCost({ total: 150 })`", "`shippingCost({ total: 100 })`", "`shippingCost({ total: 99 })`"],
          answer: 2, explain: "`>` and `>=` agree everywhere except at exactly 100 — 101 and 150 ship free under both versions, 99 and 40 pay $5 under both. So every non-boundary test stays green across the bug, and coverage stays at 100% because the same branches still run. A boundary bug is caught only by a test that sits exactly ON the boundary." },
        { q: "Which tool runs end-to-end tests — driving a real browser through your deployed app — the layer this course could only name, not run?",
          choices: ["`Playwright`", "`Vitest`", "`ESLint`", "`npm ci`"],
          answer: 0, explain: "Playwright (like Cypress) launches a real browser and drives the whole running app — clicks, navigation, real network — which is exactly why it needs processes and a browser this sandbox does not have; Unit 7's iframe DOM tests were the honest in-page stand-in. Vitest runs your unit and integration suites in Node; ESLint checks style and `npm ci` just installs dependencies." },
        { q: "A snapshot test like `expect(html).toMatchSnapshot()` compares your output against what?",
          choices: ["The expected output documented in the spec", "The output of a deliberately broken mutant version of the code", "The output of the previous test in the same file", "A copy of the output saved to a file on an earlier run"],
          answer: 3, explain: "The first run writes the output to a snapshot file on disk; every later run diffs against that saved copy and fails on any change. That makes it great at catching UNINTENDED changes and terrible at knowing what is correct — it blesses whatever existed when the snapshot was taken. It also needs a file that persists between runs, which this sandbox resets — so here it stayed vocabulary." },
        { q: "When does this CI workflow run the suite?",
          code: "# .github/workflows/test.yml\nname: tests\non: [push]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - run: npm test",
          lang: "js",
          choices: ["Nightly, on a schedule GitHub picks", "Only when someone opens a pull request", "On every push to the repository", "Whenever a developer remembers to trigger it"],
          answer: 2, explain: "`on: [push]` is the trigger: every push spins up a fresh machine that checks out the code, installs dependencies with `npm ci`, and runs `npm test` — and fails the push visibly if the suite fails. That is Continuous Integration: nobody has to remember to run the tests, and \"works on my machine\" stops being an argument. None of it runs in this sandbox; the vocabulary is what transfers." },
        { q: "A team has 300 browser E2E tests and 12 unit tests. The run takes 40 minutes and fails differently every time. The standard strategy fix?",
          choices: ["Add automatic retries so flaky failures rerun until they finally turn green", "Move most of the checking down into fast, deterministic unit tests", "Delete the flaky tests and lean on manual QA before releases", "Split the suite across more machines so it finishes sooner"],
          answer: 1, explain: "This is the test pyramid: many fast, deterministic unit tests at the bottom, some integration tests, a few E2E smoke tests on top. Browser tests are slow and flaky because they cross real networks, timers and rendering — retries just teach the team to ignore red, and more machines make the flakiness finish faster. Most of what those 300 E2E tests check can be pinned more precisely at the unit level." }
      ]
    }
  ]
});
