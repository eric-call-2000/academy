/* Testing Fundamentals — Unit 6: Testing async code */
window.CODELAB.addUnit("test", {
  id: "test-u6",
  title: "Testing async code",
  icon: "⏳",
  blurb: "Upgrade your runner to survive promises, prove rejections, hand-roll a fake fetch, and test timeouts in zero milliseconds.",
  cheat: [
    { h: "await inside the try/catch", lang: "js", code: "tests.push(async function () {\n  try { await fn(); return { name: name, pass: true }; }\n  catch (e) { return { name: name, pass: false, error: e.message }; }\n});", note: "Without the await, an async test 'passes' now and rejects later, where nobody is listening." },
    { h: "expectRejects", lang: "js", code: "async function expectRejects(promise, pattern) {\n  try { var v = await promise; }\n  catch (err) {\n    if (pattern && !pattern.test(err.message)) throw new Error(\"wrong message: \" + err.message);\n    return err;\n  }\n  throw new Error(\"expected a rejection, got \" + JSON.stringify(v));\n}", note: "toThrow only catches synchronous throws — rejections need an await." },
    { h: "A hand-rolled fake fetch", lang: "js", code: "function fakeFetch(routes) {\n  function f(url) {\n    f.calls.push([url]);\n    var hit = routes[url];\n    return Promise.resolve(hit !== undefined\n      ? { ok: true, status: 200, json: () => Promise.resolve(hit) }\n      : { ok: false, status: 404, json: () => Promise.resolve({ error: \"not found\" }) });\n  }\n  f.calls = [];\n  return f;\n}", note: "Stub + spy in one closure: scripted answers, recorded questions." },
    { h: "Unhappy-path stubs", lang: "js", code: "const s500 = () => Promise.resolve({ ok: false, status: 500, json: () => Promise.resolve({}) });\nconst junk = () => Promise.resolve({ ok: true, status: 200, json: () => Promise.reject(new Error(\"unexpected token <\")) });\nconst never = () => new Promise(() => {});", note: "One stub per disaster: server error, junk body, server that hangs." },
    { h: "Timeout without waiting", lang: "js", code: "const clock = makeClock();\nconst p = loadProfile(1, never, clock);\nclock.tick(1000);            // 0 real milliseconds\nawait expectRejects(p, /timed out/i);", note: "Start the call, advance the fake clock, assert the rejection." }
  ],
  lessons: [

    {
      id: "test-u6-1",
      title: "Awaiting an assertion: async it()",
      kind: "js", chip: "TEST", xp: 15, mins: 13,
      brief: "Your Unit 2 runner has a quiet catastrophe waiting in it. Hand it an **async test** and the body returns a *promise* — `fn()` comes back instantly, the `try/catch` finishes, and the runner records a **pass**. When the assertion finally throws, the rejection lands in a world where nobody is listening.\n\nThe fix is two `await`s. Inside `it()`, **`await fn()`** — a rejection now re-throws inside the `try`, exactly like a synchronous throw. That makes the recorded test async, so `run()` must **await each one** and hand back a promise itself.\n\nThe checks below register async tests through *your* `it()` and await *your* `run()` — the same trick this app has been playing on you for eight courses.",
      example: { lang: "js", code: "// a synchronous runner meets an async test:\nit(\"rejects bad ids\", async function () {\n  expect(await loadUser(99)).toBe(null); // loadUser rejects…\n});\n// fn() returned a promise. The try/catch is already gone\n// when the rejection arrives → recorded as a PASS." },
      steps: [
        { text: "Make `run()` **async**: `await` each recorded test and return the summary — calling `run()` now hands back a promise.",
          test: "T.expect(typeof it === 'function' && typeof run === 'function' && typeof expect === 'function', 'Keep your runner: it(), run() and expect() from Unit 2.');\nvar p = run();\nT.expect(p && typeof p.then === 'function', 'run() should now RETURN A PROMISE — declare it async function run() and await each recorded test.');\nawait p;" },
        { text: "Inside `it()`, make the recorded test **async** and `await fn()` — a rejected async test gets *recorded* as a failure.",
          test: "it('async pass', async function () { expect(await Promise.resolve(2)).toBe(2); });\nit('async fail', async function () { expect(await Promise.resolve(2)).toBe(3); });\nvar r = await run();\nT.eq(r.failed, 1, 'A rejected async test must be RECORDED as a failure. If failed is 0 your it() is not awaiting fn() — the assertion throws after the try/catch has already exited.');\nT.eq(r.passed, 1, 'and the passing async test still passes');" },
        { text: "Old tests still work: a plain synchronous test passes through the upgraded runner unchanged, and failures carry their `error` message.",
          test: "it('sync still works', function () { expect(1).toBe(1); });\nvar r = await run();\nT.eq(r.total, 3, 'Three tests registered so far — the two async ones plus this synchronous one.');\nT.eq(r.passed, 2, 'A plain synchronous test must still pass through the upgraded runner — await on a non-promise value is harmless.');\nvar bad = (r.results || []).filter(function (x) { return !x.pass; })[0];\nT.expect(!!bad && /3/.test(String(bad.error || '')), 'Record the rejection message on the failing entry (error) so the report can print WHY it failed.');" }
      ],
      files: [
        { name: "script.js", content: "// ── your test runner from Unit 2 — synchronous ─────────────\n// it() records each test; run() executes them and reports.\nlet tests = [];\n\nfunction it(name, fn) {\n  tests.push(function () {\n    try {\n      fn();                     // ← runs the test body\n      return { name: name, pass: true };\n    } catch (e) {\n      return { name: name, pass: false, error: e.message };\n    }\n  });\n}\n\nfunction run() {\n  const results = tests.map(function (t) { return t(); });\n  const failed = results.filter(function (r) { return !r.pass; }).length;\n  return { total: results.length, passed: results.length - failed, failed: failed, results: results };\n}\n\nfunction expect(actual) {\n  return {\n    toBe: function (want) {\n      if (actual !== want) {\n        throw new Error(\"expected \" + JSON.stringify(want) + \" but got \" + JSON.stringify(actual));\n      }\n    }\n  };\n}\n\n// The checks register async tests through YOUR it() and await YOUR run().\n// 1) inside it(): make the recorded test async, and `await fn()`\n// 2) make run() async and `await` each recorded test\n" }
      ],
      hints: [
        "Two edits inside it(): mark the pushed function `async` and change `fn()` to `await fn()` — a rejection now lands in the catch like any other throw.",
        "run() must wait too: `async function run()`, then collect with a loop — `const results = []; for (const t of tests) results.push(await t());` (.map would hand you an array of pending promises).",
        "`await` on a non-promise value is harmless — your old synchronous tests keep working unchanged."
      ],
      solution: {
        "script.js": "// ── your test runner from Unit 2 — now promise-proof ───────\nlet tests = [];\n\nfunction it(name, fn) {\n  tests.push(async function () {\n    try {\n      await fn();               // ← the rejection re-throws HERE\n      return { name: name, pass: true };\n    } catch (e) {\n      return { name: name, pass: false, error: e.message };\n    }\n  });\n}\n\nasync function run() {\n  const results = [];\n  for (const t of tests) {\n    results.push(await t());\n  }\n  const failed = results.filter(function (r) { return !r.pass; }).length;\n  return { total: results.length, passed: results.length - failed, failed: failed, results: results };\n}\n\nfunction expect(actual) {\n  return {\n    toBe: function (want) {\n      if (actual !== want) {\n        throw new Error(\"expected \" + JSON.stringify(want) + \" but got \" + JSON.stringify(actual));\n      }\n    }\n  };\n}\n\n// The checks register async tests through it() and await run().\n"
      }
    },

    {
      id: "test-u6-2",
      title: "Proving a rejection: expectRejects",
      kind: "js", chip: "TEST", xp: 15, mins: 13,
      spec: true,
      brief: "`withdraw(50, 80)` is *supposed* to reject — that rejection is a feature, and features get tests. But you can't write `expect(await withdraw(50, 80))`: the await throws before `expect` ever runs, and your test fails for doing its job.\n\nWrapping every rejection test in `try/catch` works — and gets old by the second test. So build the helper once: **`expectRejects(promise, pattern)`**. It *resolves* when the promise rejects (with a message matching `pattern`, if you pass one), and *throws* when the promise has the nerve to resolve.\n\nThe subject code ships written. You write the helper, then a three-test suite: the exact happy-path balance, plus both rejections.",
      steps: [
        { text: "Write `async function expectRejects(promise)` — resolves if the promise rejects; throws (naming the resolved value) if it resolves.",
          test: "T.expect(typeof expectRejects === 'function', 'Define async function expectRejects(promise, pattern).');\nvar resolvedOk = false;\ntry { await expectRejects(Promise.reject(new Error('boom'))); resolvedOk = true; } catch (e) {}\nT.expect(resolvedOk, 'When the promise REJECTS, expectRejects should resolve quietly — the rejection is the outcome you wanted.');\nvar threw = false, msg = '';\ntry { await expectRejects(Promise.resolve(42)); } catch (e) { threw = true; msg = String((e && e.message) || e); }\nT.expect(threw, 'When the promise RESOLVES, expectRejects must THROW — a rejection test that cannot fail is not a test.');\nT.expect(/resolved/i.test(msg) && /42/.test(msg), 'Name what happened, value included: \"expected the promise to reject, but it resolved with 42\". Yours said: ' + msg);" },
        { text: "Add the optional `pattern`: a rejection whose message doesn't match must fail the test too.",
          test: "var okMatch = false;\ntry { await expectRejects(Promise.reject(new Error('insufficient funds')), /insufficient/i); okMatch = true; } catch (e) {}\nT.expect(okMatch, 'A rejection whose message matches the pattern is a pass — /insufficient/i matches \"insufficient funds\".');\nvar wrongReason = false;\ntry { await expectRejects(Promise.reject(new Error('insufficient funds')), /timeout/i); } catch (e) { wrongReason = true; }\nT.expect(wrongReason, 'A rejection with the WRONG message must still fail — rejecting for the wrong reason is a bug your test would be hiding.');" },
        { text: "Write the three-test suite for `withdraw()` — exact happy-path balance, plus both rejections via `expectRejects`.",
          test: "var r = await run();\nT.expect(r.total >= 3, 'Three behaviours, three tests: the new balance, the overdraft rejection, the zero-amount rejection.');\nT.eq(r.failed, 0, 'All tests must pass against the REAL withdraw() before the mutants come out.');\nvar m1 = await T.mutate('withdraw', async function (balance, amount) { return balance - amount; }, function () { return run(); });\nT.expect(m1.failed > 0, 'A withdraw() that happily overdraws the account (and accepts an amount of 0) passed ALL your tests — use expectRejects to assert withdraw(50, 80) rejects with /insufficient/i.');\nvar m2 = await T.mutate('withdraw', async function (balance, amount) { if (amount <= 0) throw new Error('invalid amount'); if (amount > balance) throw new Error('insufficient funds'); return balance - amount - 1; }, function () { return run(); });\nT.expect(m2.failed > 0, 'A withdraw() that returns one dollar less than it should passed all your tests — assert the exact new balance, e.g. expect(await withdraw(100, 30)).toBe(70).');" }
      ],
      files: [
        { name: "script.js", content: "// ── shipped code under test — complete, do not edit ────────\nasync function withdraw(balance, amount) {\n  if (amount <= 0) throw new Error(\"invalid amount\");\n  if (amount > balance) throw new Error(\"insufficient funds\");\n  return balance - amount;\n}\n\n// 1+2) async function expectRejects(promise, pattern):\n//   - await the promise inside try/catch\n//   - it RESOLVES → throw new Error(\"expected the promise to\n//     reject, but it resolved with \" + JSON.stringify(value))\n//   - it REJECTS  → if pattern is given and it does NOT match\n//     err.message, throw; otherwise return err\n\n// 3) the suite — happy path plus BOTH rejections:\ndescribe(\"withdraw\", function () {\n  it(\"returns the new balance\", async function () {\n    expect(await withdraw(100, 30)).toBe(70);\n  });\n  // it(\"rejects an overdraft\", …)   → expectRejects(withdraw(50, 80), /insufficient/i)\n  // it(\"rejects a zero amount\", …)  → expectRejects(withdraw(100, 0), /invalid/i)\n});\n\nrun();\n" }
      ],
      hints: [
        "The skeleton: `async function expectRejects(promise, pattern) { try { var v = await promise; } catch (err) { /* pattern check */ return err; } throw new Error(\"expected the promise to reject, but it resolved with \" + JSON.stringify(v)); }` — the final throw only runs when the await did NOT throw.",
        "Pattern check inside the catch: `if (pattern && !pattern.test(err.message)) throw new Error(\"rejected, but with the wrong message: \" + err.message);`",
        "In a test, RETURN the promise: `return expectRejects(withdraw(50, 80), /insufficient/i);` — the runner awaits whatever your test returns."
      ],
      solution: {
        "script.js": "// ── shipped code under test — complete, do not edit ────────\nasync function withdraw(balance, amount) {\n  if (amount <= 0) throw new Error(\"invalid amount\");\n  if (amount > balance) throw new Error(\"insufficient funds\");\n  return balance - amount;\n}\n\nasync function expectRejects(promise, pattern) {\n  let value;\n  try {\n    value = await promise;\n  } catch (err) {\n    const msg = String((err && err.message) || err);\n    if (pattern && !pattern.test(msg)) {\n      throw new Error(\"rejected, but with the wrong message: \" + JSON.stringify(msg) + \" does not match \" + pattern);\n    }\n    return err;\n  }\n  throw new Error(\"expected the promise to reject, but it resolved with \" + JSON.stringify(value));\n}\n\ndescribe(\"withdraw\", function () {\n  it(\"returns the new balance\", async function () {\n    expect(await withdraw(100, 30)).toBe(70);\n  });\n  it(\"rejects an overdraft\", function () {\n    return expectRejects(withdraw(50, 80), /insufficient/i);\n  });\n  it(\"rejects a zero amount\", function () {\n    return expectRejects(withdraw(100, 0), /invalid/i);\n  });\n});\n\nrun();\n"
      }
    },

    {
      id: "test-u6-3",
      title: "A fake fetch you control",
      kind: "js", chip: "TEST", xp: 15, mins: 14,
      spec: true,
      brief: "Tests that hit a real network are slow, flaky, and can't order a `500` on demand. So you hand the code a **fake fetch you wrote yourself** — you built spies and stubs in Unit 5; this is both in one closure.\n\n`fakeFetch(routes)` returns a function that *looks* exactly like `fetch` to the code under test: it resolves `{ ok, status, json() }` — never rejects on a miss, because the real fetch doesn't either. It's a **stub** (you script the answers) and a **spy** (`f.calls` records every URL). The spy half matters most: **asserting the URL your code *built*** is the bug fakes catch — a typo'd route otherwise returns the right shape from the wrong place.\n\n`loadUser` ships written. You write the fake and the suite.",
      steps: [
        { text: "Write `fakeFetch(routes)` — known URLs resolve `{ ok: true, status: 200, json() }`; unknown URLs resolve `{ ok: false, status: 404 }`.",
          test: "T.expect(typeof fakeFetch === 'function', 'Define fakeFetch(routes).');\nvar f = fakeFetch({ '/api/ping': { pong: true } });\nT.expect(typeof f === 'function', 'fakeFetch(routes) must RETURN a function — the fake stands in for fetch itself.');\nvar res = await f('/api/ping');\nT.eq(res.ok, true, 'A URL that is in routes answers ok: true.');\nT.eq(res.status, 200, 'and status 200');\nT.eq(await res.json(), { pong: true }, 'json() must return a PROMISE of the routed body — the code under test does await res.json().');\nvar missing = await f('/api/nope');\nT.eq(missing.ok, false, 'A URL that is NOT in routes must still RESOLVE, with ok: false — fetch never rejects on a 404, so neither does your fake.');\nT.eq(missing.status, 404, 'status 404 for unknown URLs');" },
        { text: "Make it a spy: `f.calls` records each call's arguments, and every fake gets its own array.",
          test: "var f = fakeFetch({ '/api/a': 1, '/api/b': 2 });\nawait f('/api/a');\nawait f('/api/b');\nT.expect(Array.isArray(f.calls), 'Give the fake a calls array (f.calls = []) — a fake that cannot testify about how it was called is only half a fake.');\nT.eq(f.calls.length, 2, 'One entry per call.');\nT.eq(f.calls[0][0], '/api/a', 'Each entry records the arguments as an array: calls[0][0] is the first requested URL.');\nT.eq(f.calls[1][0], '/api/b', 'and calls[1][0] is the second');\nvar f2 = fakeFetch({});\nT.eq(f2.calls, [], 'Each fake needs its OWN calls array — if this fresh one already has entries, calls lives outside the closure and every fake shares it.');" },
        { text: "Test `loadUser()` with two different users — assert the data *and* the URL it built.",
          test: "var r = await run();\nT.expect(r.total >= 2, 'Two tests, two DIFFERENT users — a fake that only ever serves user 7 proves nothing about the URL builder.');\nT.eq(r.failed, 0, 'All tests must pass against the REAL loadUser().');\nvar f = fakeFetch({ '/api/users/7': { name: 'Ada' } });\nT.eq(await loadUser(7, f), { name: 'Ada' }, 'loadUser(7, yourFake) should resolve with the routed body.');\nT.eq(f.calls[0][0], '/api/users/7', 'Assert the URL your code BUILT — that is the bug fakes catch.');\nvar m1 = await T.mutate('loadUser', async function (id, fetchFn) { var res = await fetchFn('/api/users/7'); if (!res.ok) throw new Error('could not load user ' + id); return res.json(); }, function () { return run(); });\nT.expect(m1.failed > 0, 'A loadUser() that requests user 7 no matter which id you pass survived your suite — test a SECOND id (e.g. 42) so the URL it builds actually matters.');\nvar m2 = await T.mutate('loadUser', async function (id, fetchFn) { return { name: 'Ada' }; }, function () { return run(); });\nT.expect(m2.failed > 0, 'A loadUser() that never calls fetch at all and always answers {name:\"Ada\"} survived — give each user DIFFERENT data in your routes, or assert f.calls recorded the request.');" }
      ],
      files: [
        { name: "script.js", content: "// ── shipped code under test — complete, do not edit ────────\nasync function loadUser(id, fetchFn) {\n  const res = await fetchFn(\"/api/users/\" + id);\n  if (!res.ok) throw new Error(\"could not load user \" + id);\n  return res.json();\n}\n\n// 1+2) function fakeFetch(routes) → a fake fetch that:\n//   - records every call: f.calls[0][0] is the first requested URL\n//   - URL in routes  → resolves { ok: true, status: 200,\n//                       json: () => Promise.resolve(routes[url]) }\n//   - anything else  → resolves { ok: false, status: 404,\n//                       json: () => Promise.resolve({ error: \"not found\" }) }\n\n// 3) the suite — two different users, data AND the URL built:\ndescribe(\"loadUser\", function () {\n  // it(\"loads user 7\", …)  → routes { \"/api/users/7\": { name: \"Ada\" } }\n  // it(\"loads user 42\", …) → routes { \"/api/users/42\": { name: \"Grace\" } }\n});\n\nrun();\n" }
      ],
      hints: [
        "Spy + stub in one closure: `function fakeFetch(routes) { function f(url) { f.calls.push([url]); /* …answer… */ } f.calls = []; return f; }`",
        "Route hit: `return Promise.resolve({ ok: true, status: 200, json: function () { return Promise.resolve(routes[url]); } });` — a miss is the same shape with ok: false, status: 404.",
        "Two DIFFERENT users with DIFFERENT data is what gives the assertions teeth: `expect(await loadUser(42, f)).toEqual({ name: \"Grace\" }); expect(f.calls[0][0]).toBe(\"/api/users/42\");`"
      ],
      solution: {
        "script.js": "// ── shipped code under test — complete, do not edit ────────\nasync function loadUser(id, fetchFn) {\n  const res = await fetchFn(\"/api/users/\" + id);\n  if (!res.ok) throw new Error(\"could not load user \" + id);\n  return res.json();\n}\n\nfunction fakeFetch(routes) {\n  function f(url) {\n    f.calls.push([url]);\n    if (Object.prototype.hasOwnProperty.call(routes, url)) {\n      return Promise.resolve({\n        ok: true,\n        status: 200,\n        json: function () { return Promise.resolve(routes[url]); }\n      });\n    }\n    return Promise.resolve({\n      ok: false,\n      status: 404,\n      json: function () { return Promise.resolve({ error: \"not found\" }); }\n    });\n  }\n  f.calls = [];\n  return f;\n}\n\ndescribe(\"loadUser\", function () {\n  it(\"loads user 7 from the URL it built\", async function () {\n    const f = fakeFetch({ \"/api/users/7\": { name: \"Ada\" } });\n    expect(await loadUser(7, f)).toEqual({ name: \"Ada\" });\n    expect(f.calls[0][0]).toBe(\"/api/users/7\");\n  });\n  it(\"loads user 42 from its own URL\", async function () {\n    const f = fakeFetch({ \"/api/users/42\": { name: \"Grace\" } });\n    expect(await loadUser(42, f)).toEqual({ name: \"Grace\" });\n    expect(f.calls[0][0]).toBe(\"/api/users/42\");\n  });\n});\n\nrun();\n"
      }
    },

    {
      id: "test-u6-4",
      title: "Testing the unhappy path",
      kind: "js", chip: "TEST", xp: 15, mins: 15,
      spec: true,
      brief: "The happy path is the easy fifth of the job. Real code meets **500s**, **junk JSON**, and servers that simply never answer — and `loadProfile` (shipped, complete) already handles all three. Your job is to *prove* it, one stub per disaster.\n\nA stub that answers `{ ok: false, status: 500 }`. A stub whose `json()` **rejects**. And the nastiest one: a fetch that **never settles** — `new Promise(() => {})` — which only the code's own timeout can escape. You test that with your Unit 5 fake clock: start the call, `tick(1000)`, assert the rejection. Zero real milliseconds.\n\nNotice what you are *not* writing: any of the error handling itself. That was the Async course. Here you write the evidence that it works.",
      steps: [
        { text: "The 500: a stub answering `{ ok: false, status: 500 }` must make `loadProfile` reject with `/server/i`.",
          test: "var r = await run();\nT.expect(r.total >= 1 && r.failed === 0, 'Write the 500 test first — a stub that resolves { ok: false, status: 500, json: … } plus expectRejects(loadProfile(1, stub, makeClock()), /server/i) — and keep the suite green.');\nvar m1 = await T.mutate('loadProfile', async function (id, fetchFn, clock) { var res = await withTimeout(fetchFn('/api/profiles/' + id), 1000, clock); if (!res.ok && res.status < 500) throw new Error('profile not found'); try { return await res.json(); } catch (e) { throw new Error('bad JSON from the server: ' + e.message); } }, function () { return run(); });\nT.expect(m1.failed > 0, 'A loadProfile() that treats a 500 as a success and hands back whatever json() says passed ALL your tests — assert that a { status: 500 } stub makes it reject with /server/i.');" },
        { text: "Junk JSON: a stub whose `json()` rejects must surface `/bad json/i` — a parse error, not a crash.",
          test: "var r = await run();\nT.expect(r.total >= 2 && r.failed === 0, 'Add the junk-JSON test: a stub whose json() returns Promise.reject(new Error(\"unexpected token <\")) — the load should reject with /bad json/i.');\nvar m2 = await T.mutate('loadProfile', async function (id, fetchFn, clock) { var res = await withTimeout(fetchFn('/api/profiles/' + id), 1000, clock); if (res.status >= 500) throw new Error('server error (' + res.status + ')'); if (!res.ok) throw new Error('profile not found'); try { return await res.json(); } catch (e) { return {}; } }, function () { return run(); });\nT.expect(m2.failed > 0, 'A loadProfile() that swallows the parse error and quietly hands back {} passed all your tests — stub a json() that REJECTS and assert the /bad json/i rejection.');" },
        { text: "The hang: a fetch that never settles, cut off by the code's own timeout — fake clock, `tick(1000)`, zero real waiting.",
          test: "var firedBefore = withTimeout.fired || 0;\nvar r = await run();\nT.expect(r.total >= 3, 'Three unhappy paths, three tests.');\nT.eq(r.failed, 0, 'The whole suite must be green against the real loadProfile().');\nT.expect((withTimeout.fired || 0) > firedBefore, 'No test of yours ever made the timeout fire. Stub a fetch that NEVER settles — function () { return new Promise(function () {}); } — start loadProfile with a fresh clock, then clock.tick(1000) and assert the /timed out/i rejection. Cost: zero milliseconds.');" }
      ],
      files: [
        { name: "script.js", content: "// ── shipped code under test — complete, do not edit ────────\nasync function loadProfile(id, fetchFn, clock) {\n  const res = await withTimeout(fetchFn(\"/api/profiles/\" + id), 1000, clock);\n  if (res.status >= 500) throw new Error(\"server error (\" + res.status + \")\");\n  if (!res.ok) throw new Error(\"profile not found\");\n  try {\n    return await res.json();\n  } catch (e) {\n    throw new Error(\"bad JSON from the server: \" + e.message);\n  }\n}\n\nfunction withTimeout(promise, ms, clock) {\n  return new Promise(function (resolve, reject) {\n    const timer = clock.setTimeout(function () {\n      withTimeout.fired = (withTimeout.fired || 0) + 1; // (counter read by the checks)\n      reject(new Error(\"timed out after \" + ms + \"ms\"));\n    }, ms);\n    promise.then(\n      function (v) { clock.clearTimeout(timer); resolve(v); },\n      function (e) { clock.clearTimeout(timer); reject(e); }\n    );\n  });\n}\n\n// ── your fake clock from Unit 5, shipped so you can reuse it ─\nfunction makeClock() {\n  let t = 0, nextId = 1;\n  const timers = [];\n  return {\n    now: function () { return t; },\n    setTimeout: function (fn, ms) { timers.push({ id: nextId, at: t + ms, fn: fn }); return nextId++; },\n    clearTimeout: function (id) {\n      for (let i = timers.length - 1; i >= 0; i--) if (timers[i].id === id) timers.splice(i, 1);\n    },\n    tick: function (ms) {\n      t += ms;\n      timers.sort(function (a, b) { return a.at - b.at; });\n      while (timers.length && timers[0].at <= t) timers.shift().fn();\n    }\n  };\n}\n\n// ── your expectRejects from the last lesson ─────────────────\nasync function expectRejects(promise, pattern) {\n  let value;\n  try { value = await promise; }\n  catch (err) {\n    const msg = String((err && err.message) || err);\n    if (pattern && !pattern.test(msg)) throw new Error(\"rejected, but with the wrong message: \" + JSON.stringify(msg) + \" does not match \" + pattern);\n    return err;\n  }\n  throw new Error(\"expected the promise to reject, but it resolved with \" + JSON.stringify(value));\n}\n\n// Three unhappy paths, three stubs, three tests. No real waiting.\ndescribe(\"loadProfile — unhappy paths\", function () {\n  // 1) a stub answering { ok: false, status: 500, json: … }\n  //    → expectRejects(loadProfile(1, stub, makeClock()), /server/i)\n  // 2) a stub whose json() REJECTS (the body was junk)\n  //    → the load rejects with /bad json/i, not a raw crash\n  // 3) a stub that NEVER settles: function () { return new Promise(function () {}); }\n  //    → const p = loadProfile(1, never, clock); clock.tick(1000);\n  //      expectRejects(p, /timed out/i)\n});\n\nrun();\n" }
      ],
      hints: [
        "A stub is one line: `const s500 = function () { return Promise.resolve({ ok: false, status: 500, json: function () { return Promise.resolve({}); } }); };` — then `return expectRejects(loadProfile(1, s500, makeClock()), /server/i);`",
        "Junk JSON is a json() that rejects: `json: function () { return Promise.reject(new Error(\"unexpected token <\")); }` — the code wraps it, so assert /bad json/i.",
        "Timeout: start the call FIRST, then advance the clock: `const clock = makeClock(); const p = loadProfile(1, never, clock); clock.tick(1000); return expectRejects(p, /timed out/i);`"
      ],
      solution: {
        "script.js": "// ── shipped code under test — complete, do not edit ────────\nasync function loadProfile(id, fetchFn, clock) {\n  const res = await withTimeout(fetchFn(\"/api/profiles/\" + id), 1000, clock);\n  if (res.status >= 500) throw new Error(\"server error (\" + res.status + \")\");\n  if (!res.ok) throw new Error(\"profile not found\");\n  try {\n    return await res.json();\n  } catch (e) {\n    throw new Error(\"bad JSON from the server: \" + e.message);\n  }\n}\n\nfunction withTimeout(promise, ms, clock) {\n  return new Promise(function (resolve, reject) {\n    const timer = clock.setTimeout(function () {\n      withTimeout.fired = (withTimeout.fired || 0) + 1; // (counter read by the checks)\n      reject(new Error(\"timed out after \" + ms + \"ms\"));\n    }, ms);\n    promise.then(\n      function (v) { clock.clearTimeout(timer); resolve(v); },\n      function (e) { clock.clearTimeout(timer); reject(e); }\n    );\n  });\n}\n\n// ── your fake clock from Unit 5, shipped so you can reuse it ─\nfunction makeClock() {\n  let t = 0, nextId = 1;\n  const timers = [];\n  return {\n    now: function () { return t; },\n    setTimeout: function (fn, ms) { timers.push({ id: nextId, at: t + ms, fn: fn }); return nextId++; },\n    clearTimeout: function (id) {\n      for (let i = timers.length - 1; i >= 0; i--) if (timers[i].id === id) timers.splice(i, 1);\n    },\n    tick: function (ms) {\n      t += ms;\n      timers.sort(function (a, b) { return a.at - b.at; });\n      while (timers.length && timers[0].at <= t) timers.shift().fn();\n    }\n  };\n}\n\n// ── your expectRejects from the last lesson ─────────────────\nasync function expectRejects(promise, pattern) {\n  let value;\n  try { value = await promise; }\n  catch (err) {\n    const msg = String((err && err.message) || err);\n    if (pattern && !pattern.test(msg)) throw new Error(\"rejected, but with the wrong message: \" + JSON.stringify(msg) + \" does not match \" + pattern);\n    return err;\n  }\n  throw new Error(\"expected the promise to reject, but it resolved with \" + JSON.stringify(value));\n}\n\ndescribe(\"loadProfile — unhappy paths\", function () {\n  it(\"surfaces a 500 as a server error\", function () {\n    const s500 = function () {\n      return Promise.resolve({ ok: false, status: 500, json: function () { return Promise.resolve({}); } });\n    };\n    return expectRejects(loadProfile(1, s500, makeClock()), /server/i);\n  });\n\n  it(\"surfaces junk JSON as a parse error, not a crash\", function () {\n    const junk = function () {\n      return Promise.resolve({ ok: true, status: 200, json: function () { return Promise.reject(new Error(\"unexpected token < at line 1\")); } });\n    };\n    return expectRejects(loadProfile(1, junk, makeClock()), /bad json/i);\n  });\n\n  it(\"gives up when the server never answers\", function () {\n    const clock = makeClock();\n    const never = function () { return new Promise(function () {}); };\n    const p = loadProfile(1, never, clock);\n    clock.tick(1000);\n    return expectRejects(p, /timed out/i);\n  });\n});\n\nrun();\n"
      }
    },

    {
      id: "test-quiz-6",
      title: "Unit 6 quiz: Async tests",
      kind: "quiz", xp: 10,
      brief: "How runners, fakes and clocks survive promises. 80% to pass.",
      questions: [
        { q: "The runner below is synchronous. What does the report say about this test?",
          code: "function it(name, fn) {\n  try { fn(); results.push({ name: name, pass: true }); }\n  catch (e) { results.push({ name: name, pass: false }); }\n}\n\nit(\"rejects bad ids\", async function () {\n  expect(await loadUser(99)).toBe(null); // loadUser(99) REJECTS\n});",
          lang: "js",
          choices: ["Fails — the catch block records the rejection like any throw", "Passes — fn() returned a promise, and the rejection lands after the try/catch has already exited", "Crashes the entire run with an uncaught exception", "The test is skipped because its body is an async function"],
          answer: 1, explain: "An async function returns a promise the instant it is called, so fn() 'succeeds' and the runner records a pass before the body has really run. The rejection arrives later, when the try/catch is long gone — an unhandled rejection nobody records. The fix is `await fn()` inside the try, which forces it() and run() to go async too." },
        { q: "What is wrong with this rejection test?",
          code: "it(\"rejects a bad pin\", function () {\n  parsePin(\"12\"); // parsePin rejects for bad pins\n});",
          lang: "js",
          choices: ["Nothing is wrong — if parsePin rejects, the runner records the failure automatically", "It should call parsePin twice to make sure the rejection is consistent", "It passes no matter what — the rejection is never awaited or asserted", "parsePin needs to be wrapped in expect(parsePin).toThrow() instead"],
          answer: 2, explain: "The body starts the promise and returns immediately, throwing nothing — so the runner records a pass whether parsePin rejects, resolves, or does nothing at all. The rejection becomes an unhandled rejection nobody sees. A rejection test must await the promise and fail if it resolves, which is exactly what expectRejects packages up." },
        { q: "Your suite must prove that `withdraw(50, 80)` rejects. Which helper from this unit does it in one line?",
          choices: ["`expectRejects`", "`toThrow`", "`toBeFalsy`", "`beforeEach`"],
          answer: 0, explain: "`return expectRejects(withdraw(50, 80), /insufficient/i)` awaits the promise, resolves when the rejection arrives, and throws if the promise resolves — or rejects with the wrong message. `toThrow` only catches synchronous throws: it calls the function and catches on the spot, so an async rejection sails right past it." },
        { q: "The code under test calls `await res.json()`. What must your hand-written fake fetch resolve with?",
          choices: ["The parsed body object directly — the real fetch already did the JSON work", "A JSON string that the code under test can parse for itself", "An object with `ok` and `status` only — the json method is optional", "An object whose `json()` method returns a promise of the body"],
          answer: 3, explain: "A fake has to honor the same contract as the real thing: fetch resolves a response object whose .json() returns a promise. Skip json() (or make it return the raw body instead of a promise) and the code under test crashes for a reason that has nothing to do with the behavior you meant to test. { ok, status, json() } is the minimal trio." },
        { q: "Why does the timeout test inject a fake clock and call `tick(1000)` instead of really waiting a second?",
          choices: ["Real timers are not allowed to run inside JavaScript workers", "tick(1000) is synchronous bookkeeping — the timeout is proven in zero milliseconds", "Real setTimeout can drift by a few milliseconds, so a fake clock fires the callback far more accurately", "Real waiting works exactly as well, but tick() is less typing"],
          answer: 1, explain: "A suite that genuinely sleeps gets slow, and slow suites stop being run — the habit dies. tick() just advances a number and synchronously fires the timers that came due, so the test proves the 1000ms cutoff instantly and deterministically. Precision isn't the point; never waiting is." },
        { q: "What behavior is a stub like this written to test?",
          code: "const never = () => new Promise(() => {});",
          lang: "js",
          choices: ["That fetch retries the request until the server finally answers", "That the fake clock's tick() fires callbacks in the right order", "That the code under test gives up via its own timeout", "That promises always resolve in the order they were created"],
          answer: 2, explain: "An executor that never calls resolve or reject produces a promise that never settles — a server that hangs forever. Nothing can make that promise finish, so the only way the code under test escapes is its own timeout logic. The test starts the call, ticks the fake clock past the limit, and asserts the timed-out rejection." }
      ]
    }
  ]
});
