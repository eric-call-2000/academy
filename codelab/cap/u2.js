/* Full-Stack Capstone — Unit 2: Front meets back */
window.CODELAB.addUnit("cap", {
  id: "cap-u2",
  title: "Front meets back",
  icon: "🔌",
  blurb: "Client and server in one file — routes, status codes, envelopes, and a page that only talks through the API.",
  cheat: [
    { h: "The one-file full stack", lang: "js", code: "/* SERVER (imagine another machine) */\nfunction handleRequest(req) { /* routes → answers */ }\n\n/* THE WIRE (fake network: 20 ms) */\nfunction api(req) {\n  return new Promise((resolve) =>\n    setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* CLIENT (the page) */\nconst res = await api({ method: \"GET\", path: \"/api/notes\" });", note: "Swap api() for fetch() and the client half runs on a real network, unchanged." },
    { h: "Requests & responses are plain data", lang: "js", code: "// a request:\n{ method: \"POST\", path: \"/api/notes\", body: { text: \"hi\" } }\n\n// a response:\n{ status: 201, body: { note: { id: 4, text: \"hi\" } } }", note: "200 read OK · 201 created · 400 bad request · 404 not found." },
    { h: "The client loop: mutate → refetch → render", lang: "js", code: "async function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\n// after ANY change: ask again, redraw everything\nawait api({ method: \"DELETE\", path: \"/api/notes/\" + id });\nloadNotes();", note: "One source of truth: the server's store. The page never guesses." },
    { h: "The error envelope", lang: "js", code: "// server says no — with structure:\nreturn { status: 400, body: {\n  error: { code: \"empty\", message: \"A note needs some text.\" }\n} };\n\n// client relays the server's words:\nif (res.status === 400) {\n  banner.textContent = res.body.error.message;\n}", note: "code is for machines (if-checks); message is for humans (the banner)." },
    { h: "Routes with ids in the path", lang: "js", code: "// PATCH /api/notes/7 → which note?\nif (req.method === \"PATCH\" && req.path.indexOf(\"/api/notes/\") === 0) {\n  const id = Number(req.path.slice(\"/api/notes/\".length));\n  const found = notes.find(n => n.id === id);\n  if (!found) return { status: 404, body: {\n    error: { code: \"not_found\", message: \"No note \" + id } } };\n  // ...update found, answer 200\n}", note: "Ids come from the server's counter (nextId++) — the client never invents them." }
  ],
  lessons: [

    {
      id: "cap-u2-1",
      title: "An API in the page",
      kind: "web", chip: "WEB", xp: 15, mins: 14,
      brief: "This is the unit where the path closes its loop. 🎉 One `script.js`, two worlds:\n\n- **SERVER** — a data store plus `handleRequest(req)` that routes requests to answers. This is *exactly* what you built in Back-End Foundations — it lives in the same file here so you can see both halves at once.\n- **THE WIRE** — `api(req)` wraps `handleRequest` in a promise with a 20 ms delay: a stand-in for the network.\n- **CLIENT** — the page. It may ONLY talk to the server through `api()`.\n\nThe server half ships finished. You write the client: `loadNotes()` awaits `GET /api/notes`, and `render()` draws what came back.",
      example: { lang: "js", code: "const res = await api({ method: \"GET\", path: \"/api/notes\" });\n// res → { status: 200, body: { notes: [...] } }" },
      steps: [
        { text: "First, prove the back half is real: the checks call `handleRequest` directly — it's a plain function — and through `api()`, which just adds the wire.",
          test: "var direct = handleRequest({ method: 'GET', path: '/api/notes' });\nT.eq(direct.status, 200, 'handleRequest({ method: \"GET\", path: \"/api/notes\" }) should answer status 200 — it ships working; keep the SERVER section intact.');\nT.expect(direct.body && Array.isArray(direct.body.notes) && direct.body.notes.length === 3, 'The 200 body carries { notes: [...] } with the 3 seed notes.');\nvar res = await api({ method: 'GET', path: '/api/notes' });\nT.eq(res.status, 200, 'api() wraps that same answer in a promise — await it and you get the identical { status, body }.');" },
        { text: "Write the client: `loadNotes()` awaits `GET /api/notes` and hands `res.body.notes` to `render()`, which draws one `li.note` per note.",
          test: "await T.sleep(300);\nT.eq(T.count('#noteList .note'), 3, 'After load (the wire takes ~20 ms) 3 li.note items should render — loadNotes() → await api GET → render(res.body.notes).');\nT.expect((T.text('#noteList') || '').toLowerCase().indexOf('ride the wire') !== -1, 'Each card shows its note text — \"Ride the wire\" should be on the page.');\nvar items = T.$$('#noteList .note');\nT.expect((items[2].textContent || '').toLowerCase().indexOf('render what it says') !== -1, 'Keep server order: the third card is \"Render what it says\".');" },
        { text: "`render()` also updates `#countLine` — \"3 notes\" — so the count can never drift from the list.",
          test: "T.expect((T.text('#countLine') || '').indexOf('3') !== -1, 'Set #countLine inside render() from the array it was handed — 3 notes arrived.');\nT.expect((T.text('#countLine') || '').toLowerCase().indexOf('note') !== -1, 'Say the word too: \"3 notes\".');" }
      ],
      files: [
        { name: "index.html", content: "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n</head>\n<body>\n  <header>\n    <h1>NoteStream 🔌</h1>\n  </header>\n\n  <ul id=\"noteList\"></ul>\n  <p id=\"countLine\">loading…</p>\n\n  <script src=\"script.js\"></script>\n</body>\n</html>\n" },
        { name: "styles.css", content: "body {\n  font-family: Arial, sans-serif;\n  background: #f1f5f9;\n  max-width: 480px;\n  margin: 0 auto;\n  padding: 20px;\n}\nheader h1 { margin: 8px 0 16px; }\n#noteList { list-style: none; padding: 0; }\n#noteList .note {\n  background: white;\n  margin-top: 8px;\n  padding: 12px 14px;\n  border-radius: 10px;\n}\n#countLine { color: #64748b; font-size: 14px; margin-top: 14px; }\n" },
        { name: "script.js", content: "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Ride the wire\" },\n  { id: 2, text: \"Ask the server\" },\n  { id: 3, text: \"Render what it says\" }\n];\nlet nextId = 4;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\n\n// 1) function render(list):\n//    - clear noteList, then one <li class=\"note\"> per note (show its text)\n//    - countLine: \"<how many> notes\"\n\n// 2) async function loadNotes():\n//    - const res = await api({ method: \"GET\", path: \"/api/notes\" });\n//    - render(res.body.notes)\n\nloadNotes();\n" }
      ],
      hints: [
        "render() is DOM work you already know: `noteList.innerHTML = \"\";` then loop the array — `const li = document.createElement(\"li\"); li.className = \"note\"; li.textContent = note.text; noteList.appendChild(li);`",
        "loadNotes must be async so it can await the wire: `async function loadNotes() { const res = await api({ method: \"GET\", path: \"/api/notes\" }); render(res.body.notes); }`",
        "The count goes at the end of render(): `countLine.textContent = list.length + \" notes\";` — render from the array it was handed, never from the DOM."
      ],
      solution: {
        "script.js": "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Ride the wire\" },\n  { id: 2, text: \"Ask the server\" },\n  { id: 3, text: \"Render what it says\" }\n];\nlet nextId = 4;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = \"note\";\n    li.textContent = note.text;\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nloadNotes();\n"
      }
    },

    {
      id: "cap-u2-2",
      title: "Create through the API",
      kind: "web", chip: "WEB", xp: 15, mins: 14,
      brief: "Reading is half a conversation — now **create**. The contract:\n\n- client: `api({ method: \"POST\", path: \"/api/notes\", body: { text } })`\n- server: build `{ id: nextId++, text }`, push it into the store, answer **201 Created** with `{ note: created }`\n\nYou write BOTH halves this time. Afterwards the client doesn't guess what the list looks like — it asks again: a fresh `GET`, then `render()`. *Mutate → refetch → redraw* is the calmest data flow in web dev; every fancy framework cache is an optimization of it. And notice who invents the id: the **server**. Always the server.",
      steps: [
        { text: "Server half: a `POST /api/notes` route that creates `{ id: nextId++, text: req.body.text }`, stores it, and answers **201** with `{ note: created }`.",
          test: "await T.sleep(300);\nvar res = handleRequest({ method: 'POST', path: '/api/notes', body: { text: 'straight into the store' } });\nT.eq(res.status, 201, 'POST /api/notes should answer 201 Created — the status code for \"made a new thing\". Add the route ABOVE the 404 fallback.');\nT.expect(res.body && res.body.note && res.body.note.text === 'straight into the store', 'Answer with the envelope { note: created } so the client can see exactly what it made.');\nT.eq(res.body.note.id, 4, 'Ids come from the counter: nextId++ hands out 4 first. The client never invents ids.');" },
        { text: "Client half: submitting `#noteForm` POSTs the trimmed text, clears the input, then calls `loadNotes()` — a fresh GET re-renders everything.",
          test: "T.type('#noteInput', 'Posted from the page');\nT.submit('#noteForm');\nawait T.sleep(250);\nT.eq(T.count('#noteList .note'), 5, 'Submit → POST → loadNotes(). The previous check posted a 4th note straight into the server, and your submit makes 5 — the fresh GET should render all 5.');\nvar items = T.$$('#noteList .note');\nT.expect((items[4].textContent || '').toLowerCase().indexOf('posted from the page') !== -1, 'New notes go on the END of the list — push() keeps server order, newest last.');\nT.eq(T.val('#noteInput'), '', 'Clear the input after posting.');" },
        { text: "The count line follows for free — `render()` ran from the fresh GET, so it says 5.",
          test: "T.expect((T.text('#countLine') || '').indexOf('5') !== -1, 'No extra code needed if render() owns the count line — the fresh GET carried 5 notes. If it still says 3, your submit is not calling loadNotes().');" }
      ],
      files: [
        { name: "index.html", content: "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n</head>\n<body>\n  <header>\n    <h1>NoteStream 🔌</h1>\n  </header>\n\n  <form id=\"noteForm\">\n    <input id=\"noteInput\" placeholder=\"Write a note…\">\n    <button>Post</button>\n  </form>\n\n  <ul id=\"noteList\"></ul>\n  <p id=\"countLine\">loading…</p>\n\n  <script src=\"script.js\"></script>\n</body>\n</html>\n" },
        { name: "styles.css", content: "body {\n  font-family: Arial, sans-serif;\n  background: #f1f5f9;\n  max-width: 480px;\n  margin: 0 auto;\n  padding: 20px;\n}\nheader h1 { margin: 8px 0 16px; }\n#noteForm { display: flex; gap: 8px; }\n#noteInput {\n  flex: 1;\n  padding: 10px 12px;\n  border: 2px solid #cbd5e1;\n  border-radius: 10px;\n  font-size: 15px;\n}\n#noteForm button {\n  padding: 10px 16px;\n  border: none;\n  border-radius: 10px;\n  background: #0ea5e9;\n  color: white;\n  font-weight: bold;\n  cursor: pointer;\n}\n#noteList { list-style: none; padding: 0; }\n#noteList .note {\n  background: white;\n  margin-top: 8px;\n  padding: 12px 14px;\n  border-radius: 10px;\n}\n#countLine { color: #64748b; font-size: 14px; margin-top: 14px; }\n" },
        { name: "script.js", content: "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Ride the wire\" },\n  { id: 2, text: \"Ask the server\" },\n  { id: 3, text: \"Render what it says\" }\n];\nlet nextId = 4;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n\n  // 1) POST /api/notes — create a note:\n  //    const created = { id: nextId++, text: req.body.text };\n  //    push it into notes, then\n  //    return { status: 201, body: { note: created } };\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = \"note\";\n    li.textContent = note.text;\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nnoteForm.addEventListener(\"submit\", async (e) => {\n  e.preventDefault();\n  // 2) const text = noteInput.value.trim();\n  // 3) await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });\n  // 4) clear the input, then loadNotes() — re-render from a fresh GET\n});\n\nloadNotes();\n" }
      ],
      hints: [
        "The route goes above the 404 fallback: `if (req.method === \"POST\" && req.path === \"/api/notes\") { const created = { id: nextId++, text: req.body.text }; notes.push(created); return { status: 201, body: { note: created } }; }`",
        "The submit handler is async (it awaits the wire): `const text = noteInput.value.trim(); await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });`",
        "After the POST resolves: `noteInput.value = \"\"; loadNotes();` — don't build the <li> by hand; let the fresh GET redraw everything."
      ],
      solution: {
        "script.js": "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Ride the wire\" },\n  { id: 2, text: \"Ask the server\" },\n  { id: 3, text: \"Render what it says\" }\n];\nlet nextId = 4;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/notes\") {\n    const created = { id: nextId++, text: req.body.text };\n    notes.push(created);\n    return { status: 201, body: { note: created } };\n  }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = \"note\";\n    li.textContent = note.text;\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nnoteForm.addEventListener(\"submit\", async (e) => {\n  e.preventDefault();\n  const text = noteInput.value.trim();\n  await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });\n  noteInput.value = \"\";\n  loadNotes();\n});\n\nloadNotes();\n"
      }
    },

    {
      id: "cap-u2-3",
      title: "Update & delete through the API",
      kind: "web", chip: "WEB", xp: 15, mins: 14,
      brief: "Full CRUD time. Two new routes carry an **id in the path** — `PATCH /api/notes/2` flips note 2's pin, `DELETE /api/notes/2` removes it — and unknown ids get the honest answer: a **404** with an error envelope, not a silent shrug.\n\nOn the client, ONE listener on the list handles every button ever rendered (event delegation — you built this pattern in the DOM course): read `data-id` off the clicked button, fire the matching request, then `loadNotes()` to redraw the truth. The checks hit your routes directly AND through real clicks — keep both halves honest.",
      example: { lang: "js", code: "// pull the id off the end of the path:\nconst id = Number(req.path.slice(\"/api/notes/\".length));\nconst found = notes.find(n => n.id === id);" },
      steps: [
        { text: "Server: `PATCH /api/notes/<id>` flips that note's `pinned` flag and answers 200 with `{ note: found }`; an unknown id answers **404** with the error envelope.",
          test: "await T.sleep(300);\nvar res = handleRequest({ method: 'PATCH', path: '/api/notes/1' });\nT.eq(res.status, 200, 'PATCH /api/notes/1 should answer 200 — match paths that START with \"/api/notes/\" and pull the id off the end with slice + Number.');\nT.expect(res.body && res.body.note && res.body.note.pinned === true, 'The route flips pinned: note 1 starts false, so one PATCH turns it true. Answer { note: found }.');\nvar missing = handleRequest({ method: 'PATCH', path: '/api/notes/99' });\nT.eq(missing.status, 404, 'No note has id 99 — answer 404, not a crash and not a quiet 200.');\nT.expect(missing.body && missing.body.error && typeof missing.body.error.message === 'string', 'The 404 body is the envelope: { error: { code: \"not_found\", message: ... } }.');" },
        { text: "Server: `DELETE /api/notes/<id>` removes the note from the store (same 404 envelope for unknown ids).",
          test: "var res = handleRequest({ method: 'DELETE', path: '/api/notes/3' });\nT.eq(res.status, 200, 'DELETE /api/notes/3 should answer 200 — filter the store down to every note EXCEPT that id.');\nvar check = handleRequest({ method: 'GET', path: '/api/notes' });\nT.eq(check.body.notes.length, 2, 'After the DELETE, a GET should list 2 notes — the store really shrank.');\nvar missing = handleRequest({ method: 'DELETE', path: '/api/notes/99' });\nT.eq(missing.status, 404, 'Deleting a note that does not exist → the same 404 envelope as PATCH.');" },
        { text: "Client: one click listener on `#noteList` (delegation). A `.pin` button PATCHes its `data-id`, then `loadNotes()` redraws.",
          test: "T.click('#noteList .note .pin');\nawait T.sleep(250);\nT.eq(T.count('#noteList .note'), 2, 'Pin click → PATCH → loadNotes(). The earlier checks deleted note 3 on the server, so the fresh GET renders 2 cards. If you still see 3, the click is not triggering a refetch.');\nvar cards = T.$$('#noteList .note');\nT.expect(!cards[0].classList.contains('pinned'), 'An earlier check PATCHed note 1 to pinned — your click flips it back OFF, so card 1 has no pinned class.');\nT.expect(cards[1].classList.contains('pinned'), 'Note 2 keeps its pin — only the id you clicked may change.');" },
        { text: "Client: a `.del` button DELETEs its `data-id`, then `loadNotes()` — the card vanishes and the count follows.",
          test: "T.click('#noteList .note .del');\nawait T.sleep(250);\nT.eq(T.count('#noteList .note'), 1, '✕ click → DELETE → loadNotes(): one card left.');\nT.expect((T.text('#noteList') || '').toLowerCase().indexOf('trust the server') !== -1, 'The survivor is note 2 — read the id off the CLICKED button (btn.dataset.id), never delete blindly.');\nT.expect((T.text('#countLine') || '').indexOf('1') !== -1, 'The count line follows the fresh GET: 1.');" }
      ],
      files: [
        { name: "index.html", content: "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n</head>\n<body>\n  <header>\n    <h1>NoteStream 🔌</h1>\n  </header>\n\n  <form id=\"noteForm\">\n    <input id=\"noteInput\" placeholder=\"Write a note…\">\n    <button>Post</button>\n  </form>\n\n  <ul id=\"noteList\"></ul>\n  <p id=\"countLine\">loading…</p>\n\n  <script src=\"script.js\"></script>\n</body>\n</html>\n" },
        { name: "styles.css", content: "body {\n  font-family: Arial, sans-serif;\n  background: #f1f5f9;\n  max-width: 480px;\n  margin: 0 auto;\n  padding: 20px;\n}\nheader h1 { margin: 8px 0 16px; }\n#noteForm { display: flex; gap: 8px; }\n#noteInput {\n  flex: 1;\n  padding: 10px 12px;\n  border: 2px solid #cbd5e1;\n  border-radius: 10px;\n  font-size: 15px;\n}\n#noteForm button {\n  padding: 10px 16px;\n  border: none;\n  border-radius: 10px;\n  background: #0ea5e9;\n  color: white;\n  font-weight: bold;\n  cursor: pointer;\n}\n#noteList { list-style: none; padding: 0; }\n#noteList .note {\n  background: white;\n  margin-top: 8px;\n  padding: 12px 14px;\n  border-radius: 10px;\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  gap: 8px;\n}\n#noteList .note span { flex: 1; }\n#noteList .note.pinned {\n  background: #fef9c3;\n  border: 2px solid #fde047;\n}\n.pin {\n  border: none;\n  background: #e2e8f0;\n  border-radius: 8px;\n  padding: 4px 10px;\n  cursor: pointer;\n  font-size: 12px;\n}\n.del {\n  border: none;\n  background: none;\n  color: #ef4444;\n  font-weight: bold;\n  cursor: pointer;\n}\n#countLine { color: #64748b; font-size: 14px; margin-top: 14px; }\n" },
        { name: "script.js", content: "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Ride the wire\", pinned: false },\n  { id: 2, text: \"Trust the server\", pinned: true },\n  { id: 3, text: \"Render from data\", pinned: false }\n];\nlet nextId = 4;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/notes\") {\n    const created = { id: nextId++, text: req.body.text, pinned: false };\n    notes.push(created);\n    return { status: 201, body: { note: created } };\n  }\n\n  // 1) PATCH /api/notes/<id> — flip that note's pinned flag:\n  //    - match: req.method === \"PATCH\" && req.path.indexOf(\"/api/notes/\") === 0\n  //    - const id = Number(req.path.slice(\"/api/notes/\".length));\n  //    - not found → { status: 404, body: { error: { code: \"not_found\", message: \"No note \" + id } } }\n  //    - found → found.pinned = !found.pinned; return { status: 200, body: { note: found } }\n\n  // 2) DELETE /api/notes/<id> — remove it from the store:\n  //    - same id parsing, same 404 envelope\n  //    - found → notes = notes.filter(n => n.id !== id);\n  //      return { status: 200, body: { deleted: id } }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = note.pinned ? \"note pinned\" : \"note\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = (note.pinned ? \"📌 \" : \"\") + note.text;\n    li.appendChild(label);\n\n    const pinBtn = document.createElement(\"button\");\n    pinBtn.className = \"pin\";\n    pinBtn.textContent = note.pinned ? \"Unpin\" : \"Pin\";\n    pinBtn.dataset.id = note.id;\n    li.appendChild(pinBtn);\n\n    const delBtn = document.createElement(\"button\");\n    delBtn.className = \"del\";\n    delBtn.textContent = \"✕\";\n    delBtn.dataset.id = note.id;\n    li.appendChild(delBtn);\n\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nnoteForm.addEventListener(\"submit\", async (e) => {\n  e.preventDefault();\n  const text = noteInput.value.trim();\n  await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });\n  noteInput.value = \"\";\n  loadNotes();\n});\n\n// 3) ONE async click listener on noteList (event delegation):\n//    - const btn = e.target.closest(\"button\"); if (!btn) return;\n//    - const id = Number(btn.dataset.id);\n//    - .pin  → await api({ method: \"PATCH\",  path: \"/api/notes/\" + id })\n//    - .del  → await api({ method: \"DELETE\", path: \"/api/notes/\" + id })\n//    - either way: loadNotes()\n\nloadNotes();\n" }
      ],
      hints: [
        "PATCH route skeleton (above the 404 fallback): `if (req.method === \"PATCH\" && req.path.indexOf(\"/api/notes/\") === 0) { const id = Number(req.path.slice(\"/api/notes/\".length)); const found = notes.find(n => n.id === id); if (!found) return { status: 404, body: { error: { code: \"not_found\", message: \"No note \" + id } } }; found.pinned = !found.pinned; return { status: 200, body: { note: found } }; }`",
        "DELETE is the same shape, but reassigns the store: `notes = notes.filter(n => n.id !== id); return { status: 200, body: { deleted: id } };` — find first so unknown ids still 404.",
        "Delegation: `noteList.addEventListener(\"click\", async (e) => { const btn = e.target.closest(\"button\"); if (!btn) return; const id = Number(btn.dataset.id); ... });`",
        "Branch on the button class: `if (btn.classList.contains(\"pin\")) { await api({ method: \"PATCH\", path: \"/api/notes/\" + id }); } else if (btn.classList.contains(\"del\")) { await api({ method: \"DELETE\", path: \"/api/notes/\" + id }); } loadNotes();`"
      ],
      solution: {
        "script.js": "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Ride the wire\", pinned: false },\n  { id: 2, text: \"Trust the server\", pinned: true },\n  { id: 3, text: \"Render from data\", pinned: false }\n];\nlet nextId = 4;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/notes\") {\n    const created = { id: nextId++, text: req.body.text, pinned: false };\n    notes.push(created);\n    return { status: 201, body: { note: created } };\n  }\n\n  if (req.method === \"PATCH\" && req.path.indexOf(\"/api/notes/\") === 0) {\n    const id = Number(req.path.slice(\"/api/notes/\".length));\n    const found = notes.find(n => n.id === id);\n    if (!found) {\n      return { status: 404, body: { error: { code: \"not_found\", message: \"No note \" + id } } };\n    }\n    found.pinned = !found.pinned;\n    return { status: 200, body: { note: found } };\n  }\n\n  if (req.method === \"DELETE\" && req.path.indexOf(\"/api/notes/\") === 0) {\n    const id = Number(req.path.slice(\"/api/notes/\".length));\n    const found = notes.find(n => n.id === id);\n    if (!found) {\n      return { status: 404, body: { error: { code: \"not_found\", message: \"No note \" + id } } };\n    }\n    notes = notes.filter(n => n.id !== id);\n    return { status: 200, body: { deleted: id } };\n  }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = note.pinned ? \"note pinned\" : \"note\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = (note.pinned ? \"📌 \" : \"\") + note.text;\n    li.appendChild(label);\n\n    const pinBtn = document.createElement(\"button\");\n    pinBtn.className = \"pin\";\n    pinBtn.textContent = note.pinned ? \"Unpin\" : \"Pin\";\n    pinBtn.dataset.id = note.id;\n    li.appendChild(pinBtn);\n\n    const delBtn = document.createElement(\"button\");\n    delBtn.className = \"del\";\n    delBtn.textContent = \"✕\";\n    delBtn.dataset.id = note.id;\n    li.appendChild(delBtn);\n\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nnoteForm.addEventListener(\"submit\", async (e) => {\n  e.preventDefault();\n  const text = noteInput.value.trim();\n  await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });\n  noteInput.value = \"\";\n  loadNotes();\n});\n\nnoteList.addEventListener(\"click\", async (e) => {\n  const btn = e.target.closest(\"button\");\n  if (!btn) return;\n  const id = Number(btn.dataset.id);\n  if (btn.classList.contains(\"pin\")) {\n    await api({ method: \"PATCH\", path: \"/api/notes/\" + id });\n  } else if (btn.classList.contains(\"del\")) {\n    await api({ method: \"DELETE\", path: \"/api/notes/\" + id });\n  }\n  loadNotes();\n});\n\nloadNotes();\n"
      }
    },

    {
      id: "cap-u2-4",
      title: "When the server says no",
      kind: "web", chip: "WEB", xp: 15, mins: 12,
      brief: "Your form posts whatever's in the box — including nothing. And the client can't be trusted: anyone can hit an API without ever seeing your form. So the **server** defends itself: empty text → **400 Bad Request** with `{ error: { code: \"empty\", message: \"A note needs some text.\" } }`, and nothing enters the store.\n\nThe client's job flips from optimist to diplomat: check `res.status`. On 400, show **the server's message** — its wording, not yours — in `#errorBanner`. On 201, clear the banner, clear the input, refetch. One rule to keep forever: *the server decides, the client relays.*",
      steps: [
        { text: "Server: the POST route trims `req.body.text` first. Empty → **400** with `{ error: { code: \"empty\", message: ... } }` and the store untouched.",
          test: "await T.sleep(300);\nvar res = handleRequest({ method: 'POST', path: '/api/notes', body: { text: '   ' } });\nT.eq(res.status, 400, 'Whitespace-only text must be rejected with 400 — trim() BEFORE judging, at the top of the POST route.');\nT.expect(res.body && res.body.error && res.body.error.code === 'empty', 'Answer with the envelope { error: { code: \"empty\", message: ... } } — code is the machine-readable part.');\nT.expect(res.body && res.body.error && typeof res.body.error.message === 'string' && res.body.error.message.length > 0, 'Give the error a human message — the client will put it on screen.');\nvar check = handleRequest({ method: 'GET', path: '/api/notes' });\nT.eq(check.body.notes.length, 2, 'A rejected POST creates NOTHING — the store still holds the 2 seed notes.');" },
        { text: "Client: on a 400, show the **server's** `res.body.error.message` in `#errorBanner` (add the class `show`) and leave the list alone.",
          test: "T.type('#noteInput', '   ');\nT.submit('#noteForm');\nawait T.sleep(250);\nT.expect(T.css('#errorBanner', 'display') !== 'none', 'When res.status is 400, add the class \"show\" to #errorBanner — the CSS reveals it.');\nvar serverMsg = handleRequest({ method: 'POST', path: '/api/notes', body: { text: '' } }).body.error.message;\nT.expect((T.text('#errorBanner') || '').toLowerCase().indexOf(serverMsg.toLowerCase()) !== -1, 'Show the SERVER\\'s words: errorBanner.textContent = res.body.error.message — not wording of your own.');\nT.eq(T.count('#noteList .note'), 2, 'Nothing was created, so nothing re-renders — still 2 cards.');" },
        { text: "Client: a **valid** note posts normally — and success clears the banner and the input.",
          test: "T.type('#noteInput', 'A real note this time');\nT.submit('#noteForm');\nawait T.sleep(250);\nT.eq(T.count('#noteList .note'), 3, 'A valid POST answers 201; then loadNotes() re-renders — 3 cards now.');\nT.expect(T.css('#errorBanner', 'display') === 'none', 'Success must clear the banner: remove the class \"show\" (and empty its text) on the 201 path.');\nT.eq(T.val('#noteInput'), '', 'Clear the input on success — and only on success.');" }
      ],
      files: [
        { name: "index.html", content: "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n</head>\n<body>\n  <header>\n    <h1>NoteStream 🔌</h1>\n  </header>\n\n  <div id=\"errorBanner\" role=\"alert\"></div>\n\n  <form id=\"noteForm\">\n    <input id=\"noteInput\" placeholder=\"Write a note…\">\n    <button>Post</button>\n  </form>\n\n  <ul id=\"noteList\"></ul>\n  <p id=\"countLine\">loading…</p>\n\n  <script src=\"script.js\"></script>\n</body>\n</html>\n" },
        { name: "styles.css", content: "body {\n  font-family: Arial, sans-serif;\n  background: #f1f5f9;\n  max-width: 480px;\n  margin: 0 auto;\n  padding: 20px;\n}\nheader h1 { margin: 8px 0 16px; }\n#errorBanner {\n  display: none;\n  background: #fee2e2;\n  color: #b91c1c;\n  border: 2px solid #fca5a5;\n  border-radius: 10px;\n  padding: 10px 14px;\n  margin-bottom: 12px;\n}\n#errorBanner.show { display: block; }\n#noteForm { display: flex; gap: 8px; }\n#noteInput {\n  flex: 1;\n  padding: 10px 12px;\n  border: 2px solid #cbd5e1;\n  border-radius: 10px;\n  font-size: 15px;\n}\n#noteForm button {\n  padding: 10px 16px;\n  border: none;\n  border-radius: 10px;\n  background: #0ea5e9;\n  color: white;\n  font-weight: bold;\n  cursor: pointer;\n}\n#noteList { list-style: none; padding: 0; }\n#noteList .note {\n  background: white;\n  margin-top: 8px;\n  padding: 12px 14px;\n  border-radius: 10px;\n}\n#countLine { color: #64748b; font-size: 14px; margin-top: 14px; }\n" },
        { name: "script.js", content: "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Believe nothing the client says\" },\n  { id: 2, text: \"Answer with envelopes\" }\n];\nlet nextId = 3;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/notes\") {\n    // 1) validation FIRST: const text = (req.body.text || \"\").trim();\n    //    empty → return { status: 400, body: { error: { code: \"empty\", message: \"A note needs some text.\" } } };\n    const created = { id: nextId++, text: req.body.text };\n    notes.push(created);\n    return { status: 201, body: { note: created } };\n  }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\nconst errorBanner = document.querySelector(\"#errorBanner\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = \"note\";\n    li.textContent = note.text;\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nnoteForm.addEventListener(\"submit\", async (e) => {\n  e.preventDefault();\n  const res = await api({ method: \"POST\", path: \"/api/notes\", body: { text: noteInput.value } });\n  // 2) if res.status === 400: show the SERVER's words —\n  //    errorBanner.textContent = res.body.error.message;\n  //    errorBanner.classList.add(\"show\"); then STOP (return)\n  // 3) otherwise (201): clear the banner (remove \"show\", empty its text),\n  //    clear the input, loadNotes()\n  noteInput.value = \"\";\n  loadNotes();\n});\n\nloadNotes();\n" }
      ],
      hints: [
        "Server, top of the POST route: `const text = (req.body.text || \"\").trim(); if (text === \"\") { return { status: 400, body: { error: { code: \"empty\", message: \"A note needs some text.\" } } }; }` — then create from the trimmed `text`.",
        "Client, right after the await: `if (res.status === 400) { errorBanner.textContent = res.body.error.message; errorBanner.classList.add(\"show\"); return; }` — the return stops the success path from running.",
        "The success path clears everything: `errorBanner.textContent = \"\"; errorBanner.classList.remove(\"show\"); noteInput.value = \"\"; loadNotes();`",
        "Why not just block empty text in the form? Do that too in real apps — but only the server-side check protects the store from clients you didn't write."
      ],
      solution: {
        "script.js": "/* ============ SERVER (imagine this on another machine) ============ */\n\nlet notes = [\n  { id: 1, text: \"Believe nothing the client says\" },\n  { id: 2, text: \"Answer with envelopes\" }\n];\nlet nextId = 3;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: { notes: notes } };\n  }\n\n  if (req.method === \"POST\" && req.path === \"/api/notes\") {\n    const text = (req.body.text || \"\").trim();\n    if (text === \"\") {\n      return { status: 400, body: { error: { code: \"empty\", message: \"A note needs some text.\" } } };\n    }\n    const created = { id: nextId++, text: text };\n    notes.push(created);\n    return { status: 201, body: { note: created } };\n  }\n\n  return { status: 404, body: { error: { code: \"not_found\", message: \"No route for \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\n\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\n\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\nconst errorBanner = document.querySelector(\"#errorBanner\");\n\nfunction render(list) {\n  noteList.innerHTML = \"\";\n  list.forEach((note) => {\n    const li = document.createElement(\"li\");\n    li.className = \"note\";\n    li.textContent = note.text;\n    noteList.appendChild(li);\n  });\n  countLine.textContent = list.length + \" notes\";\n}\n\nasync function loadNotes() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\" });\n  render(res.body.notes);\n}\n\nnoteForm.addEventListener(\"submit\", async (e) => {\n  e.preventDefault();\n  const res = await api({ method: \"POST\", path: \"/api/notes\", body: { text: noteInput.value } });\n  if (res.status === 400) {\n    errorBanner.textContent = res.body.error.message;\n    errorBanner.classList.add(\"show\");\n    return;\n  }\n  errorBanner.textContent = \"\";\n  errorBanner.classList.remove(\"show\");\n  noteInput.value = \"\";\n  loadNotes();\n});\n\nloadNotes();\n"
      }
    },

    {
      id: "cap-quiz-2",
      title: "Unit 2 quiz: Client ↔ server",
      kind: "quiz", xp: 10,
      questions: [
        { q: "In this course's one-file pattern, what is `api()` standing in for?",
          choices: ["A database the browser queries directly over SQL", "fetch() calling a real server over a network", "A shortcut that re-renders without state", "localStorage under a different name"],
          answer: 1, explain: "The returned promise and the artificial delay exist to simulate the network round trip — that latency is the whole point. Swap the setTimeout wrapper for `fetch(\"https://…\")` and your client code barely changes: await the call, read status, read the body. Browsers never talk SQL to a database directly; a server sits in between." },
        { q: "The server successfully creates the note. What should `res.status` be?",
          code: "const res = await api({\n  method: \"POST\", path: \"/api/notes\",\n  body: { text: \"hi\" }\n});",
          lang: "js",
          choices: ["400 — the request was malformed", "404 — that note does not exist", "200 — the only success code", "201 — a brand-new thing exists"],
          answer: 3, explain: "2xx means success, and **201 Created** specifically says \"made something new\". 200 is the answer for reads; 4xx codes report client mistakes, and nothing here was malformed or missing. Answering precisely lets clients react precisely." },
        { q: "Right after a successful POST, the calmest way to update the list is…",
          choices: ["Ask the server again with a fresh GET", "Append an <li> by hand and skip the server", "Reload the whole page so it refetches", "Edit res.body and hope the server matches"],
          answer: 0, explain: "One source of truth: the server's store. Mutate → refetch → render can never drift out of sync, because you always draw what the server actually says. A full page reload would work too, but it throws away scroll position, focus and every bit of in-memory state — that's a sledgehammer, not calm. (Next unit you'll layer optimistic updates ON TOP of this — with rollback discipline.)" },
        { q: "`PATCH /api/notes/99` arrives, but no note has id 99. The well-behaved answer is…",
          choices: ["{ status: 200, body: { ok: true } } — fail quietly", "{ status: 500, body: { error: { code: \"server_error\" } } }", "{ status: 404, body: { error: { code: \"not_found\" } } }", "{ status: 201, body: { note: null, id: 99 } }"],
          answer: 2, explain: "Honest status + a structured envelope. Ship both fields: `code` for the client's if-checks and `message: \"No note 99\"` for the human reading the screen. 404 says \"that thing isn't here\" — a 500 would blame your server for the caller's bad id, and a silent 200 is how bugs hide for months." },
        { q: "Your form already refuses empty notes. Why must the SERVER check again?",
          choices: ["It shouldn't — checking twice is wasteful", "Anyone can call your API without the form", "JavaScript can't trim strings on the client", "It slows requests down, which throttles spam"],
          answer: 1, explain: "curl, a script, a rival app, or your own code with a typo can POST straight to `/api/notes` and never load your HTML at all. The server-side check is the only one you fully control — client checks are a courtesy that gives users fast feedback, server checks are the security guard. Every real backend validates every write, no matter what the UI promises." },
        { q: "What does this log?",
          code: "const res = api({ method: \"GET\", path: \"/api/notes\" });\nconsole.log(res.body);",
          lang: "js",
          choices: ["The notes array the server returned", "null, since the body is not ready yet", "A 404 envelope from the fake server", "undefined — res is a pending Promise"],
          answer: 3, explain: "Without `await`, `res` is the Promise object itself, and a Promise has no `.body` — reading a property that doesn't exist gives you `undefined`, not null and not an error. `api()` returns a Promise; `await` (inside an async function) unwraps it into the actual `{ status, body }` response. Forgetting the await is THE classic wire bug." }
      ]
    }
  ]
});
