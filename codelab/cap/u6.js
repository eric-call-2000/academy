/* Full-Stack Capstone — Unit 6: Ship it */
window.CODELAB.addUnit("cap", {
  id: "cap-u6",
  title: "Ship it",
  icon: "🚀",
  blurb: "The victory lap — make a page deploy-ready, then build NoteStream: your complete full-stack finale.",
  cheat: [
    { h: "Deploy-ready <head>", lang: "html", code: "<title>NoteStream</title>\n<meta name=\"viewport\"\n      content=\"width=device-width, initial-scale=1\">\n<meta name=\"description\"\n      content=\"A tiny full-stack notes app.\">", note: "Tab name, phone rendering, search snippet — three lines, huge difference." },
    { h: "One-file full stack", lang: "js", code: "/* ===== SERVER ===== */\nfunction handleRequest(req) { /* routes → { status, body } */ }\n\n/* ===== THE WIRE (20 ms) ===== */\nfunction api(req) {\n  return new Promise((resolve) =>\n    setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ===== CLIENT ===== */\nconst res = await api({ method: \"GET\", path: \"/api/notes\" });", note: "Same file, two machines in spirit — swap api() for fetch() and it is the real thing." },
    { h: "Optimistic add, honest rollback", lang: "js", code: "items.push(temp); render();              // instant\nconst res = await api({ method: \"POST\", path: \"/api/notes\",\n                        body: { text: text } });\nif (res.status === 201) {\n  items[items.indexOf(temp)] = res.body;  // confirm\n} else {\n  items.splice(items.indexOf(temp), 1);   // roll back\n  showError(res.body.error.message);\n}\nrender();" },
    { h: "Status codes you have shipped", lang: "js", code: "200 OK          // GET / DELETE success\n201 Created     // POST success\n400 Bad Request // body: { error: { code, message } }\n404 Not Found   // unknown route or id", note: "The envelope means the client can ALWAYS read error.message — no guessing." },
    { h: "Ship it with git", lang: "js", code: "git init          // start tracking the folder\ngit add .         // stage your changes\ngit commit -m \"feat: NoteStream v1\"\ngit push          // upload to GitHub", note: "Push to GitHub → enable Pages → your capstone is live at a real URL. Exactly how this course is hosted!" }
  ],
  lessons: [

    {
      id: "cap-u6-1",
      title: "Deploy-ready checklist",
      kind: "web", chip: "HTML", xp: 15, mins: 12,
      brief: "Your app runs — now make it *deployable*. Before any site goes public, pros walk a tiny checklist, and every line of it is structural HTML you already know:\n\n- **`<title>`** — names the browser tab, your bookmarks, and your search result\n- **viewport meta** — without `width=device-width`, phones render a zoomed-out desktop page\n- **`meta description`** — the sentence search engines print under your link\n- **a repo link** — your work, signed; recruiters really do click it\n\nNone of these change how the page looks on your laptop. All of them change how it lands everywhere else. NoteStream's landing page is missing all four — fix it.",
      steps: [
        { text: "Name the tab: a `<title>` in the `<head>` that says **NoteStream**.",
          test: "T.expect(T.$('head title'), 'Add <title>…</title> inside <head> — it names the browser tab.');\nT.expect((T.text('head title') || '').toLowerCase().indexOf('notestream') !== -1, 'Put the actual app name in it: NoteStream.');" },
        { text: "Add the two shipping metas: a viewport (`content` including `width=device-width`) and a real `meta name=\"description\"` sentence.",
          test: "var v = T.attr('meta[name=\"viewport\"]', 'content');\nT.expect(v !== null, 'Add <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"> to the head.');\nT.expect(v.indexOf('width=device-width') !== -1, 'The viewport content must include width=device-width — that is the phrase that stops phones from zooming way out.');\nvar d = T.attr('meta[name=\"description\"]', 'content');\nT.expect(d !== null && d.trim().length >= 20, 'Add <meta name=\"description\" content=\"…\"> with a real sentence (20+ characters) — search engines print it under your link.');" },
        { text: "Sign it: a `<footer>` with a link to your GitHub repo — the `href` must contain `github`, and the link needs visible text.",
          test: "T.expect(T.$('footer'), 'Add a <footer> near the end of <body>.');\nvar a2 = T.$('footer a[href*=\"github\"]');\nT.expect(a2, 'Inside the footer, link the repo: <a href=\"https://github.com/you/notestream\">…</a> — the check looks for github in the href.');\nT.expect((a2.textContent || '').trim().length > 0, 'Give the link visible text, e.g. Source on GitHub.');" }
      ],
      files: [
        { name: "index.html", content: "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n  <!-- 1) <title> — name the tab: NoteStream -->\n  <!-- 2) <meta name=\"viewport\" …> and <meta name=\"description\" …> -->\n</head>\n<body>\n  <main class=\"card\">\n    <h1>NoteStream 📡</h1>\n    <p>Full-stack notes — one page, both halves.</p>\n    <p><a class=\"cta\" href=\"#app\">Open the app</a></p>\n  </main>\n\n  <!-- 3) <footer> with a link to the GitHub repo -->\n</body>\n</html>\n" },
        { name: "styles.css", content: "body {\n  font-family: Arial, sans-serif;\n  background: #f1f5f9;\n  margin: 0;\n  min-height: 100vh;\n  display: grid;\n  place-items: center;\n}\n.card {\n  background: white;\n  padding: 32px 40px;\n  border-radius: 16px;\n  text-align: center;\n  box-shadow: 0 10px 30px rgba(15, 23, 42, 0.08);\n}\n.card h1 { margin: 0 0 8px; }\n.card p { color: #475569; margin: 0 0 12px; }\n.cta {\n  display: inline-block;\n  background: #0ea5e9;\n  color: white;\n  padding: 10px 18px;\n  border-radius: 10px;\n  text-decoration: none;\n  font-weight: bold;\n}\nfooter {\n  position: fixed;\n  bottom: 12px;\n  left: 0;\n  right: 0;\n  text-align: center;\n  font-size: 13px;\n  color: #64748b;\n}\nfooter a { color: #0ea5e9; }\n" }
      ],
      hints: [
        "The three head lines: `<title>NoteStream</title>`, `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">`, `<meta name=\"description\" content=\"NoteStream is a tiny full-stack notes app — one page, both halves.\">`",
        "Footer, right before `</body>`: `<footer><a href=\"https://github.com/you/notestream\">Source on GitHub</a></footer>` — any href containing github satisfies the check."
      ],
      solution: {
        "index.html": "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n  <title>NoteStream — full-stack notes</title>\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n  <meta name=\"description\" content=\"NoteStream is a tiny full-stack notes app: one page, a client, a server, and nothing to install.\">\n</head>\n<body>\n  <main class=\"card\">\n    <h1>NoteStream 📡</h1>\n    <p>Full-stack notes — one page, both halves.</p>\n    <p><a class=\"cta\" href=\"#app\">Open the app</a></p>\n  </main>\n\n  <footer>\n    Built start to finish by one dev —\n    <a href=\"https://github.com/you/notestream\">Source on GitHub</a>\n  </footer>\n</body>\n</html>\n"
      }
    },

    {
      id: "cap-u6-p",
      title: "Project: NoteStream — your full-stack notes app",
      kind: "web", chip: "WEB", xp: 60, mins: 50, project: true,
      brief: "This is the one the whole path was pointing at. **NoteStream** is a complete full-stack notes app on a single page: the SERVER section is exactly what you built in Back-End Foundations, `api()` is the 20 ms wire, and the CLIENT is your state-driven DOM craft — here they live in one file so you can see both halves at once.\n\nYou will wire: boot-from-GET rendering, optimistic add with a `pending` ghost, honest rollback when the server says no, optimistic delete, a live count, an `aria-live` announcer, pretty JSON export, and autosave on every render. The six checkpoints replay one realistic session, boot to export. Work the TODOs top to bottom — server first — and feel every course click into place. 🏁",
      example: { lang: "js", code: "/* ============ SERVER ============ */\nfunction handleRequest(req) { /* routes → { status, body } */ }\n\n/* ============ THE WIRE ============ */\nfunction api(req) {\n  return new Promise((resolve) =>\n    setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT ============ */\nconst res = await api({ method: \"GET\", path: \"/api/notes\" });" },
      steps: [
        { text: "**Boot.** The client GETs `/api/notes` and renders the two seed notes, count line included.",
          test: "await T.sleep(300);\nT.eq(T.count('#noteList li'), 2, 'boot() should GET /api/notes, map the body into items, and render() one li per note — then be CALLED at the bottom of the script.');\nvar txt = (T.text('#noteList') || '').toLowerCase();\nT.expect(txt.indexOf('welcome to notestream') !== -1 && txt.indexOf('pair the client') !== -1, 'Each li shows its note text.');\nT.expect((T.text('#countLine') || '').indexOf('2') !== -1, 'countLine reads items.length + \" notes\" — 2 right now.');\nT.expect(T.$('label[for=noteInput]'), 'Keep the label wired to the input — label[for] ships with the page.');\nvar res = handleRequest({ method: 'GET', path: '/api/notes', body: null });\nT.eq(res.status, 200, 'The tests can call your server directly: GET /api/notes must answer 200.');\nT.expect(Array.isArray(res.body) && res.body.length === 2, 'Its body is a copy of the notes array.');" },
        { text: "**Optimistic add.** Submitting shows the note *instantly* with class `pending`; ~20 ms later the server confirms — pending gone, status announced.",
          test: "T.type('#noteInput', 'Ship the capstone');\nT.submit('#noteForm');\nvar lis = T.$$('#noteList li');\nT.eq(lis.length, 3, 'The new note must appear IMMEDIATELY — push the temp item and render() BEFORE awaiting api().');\nT.expect(lis[2].classList.contains('pending'), 'Until the server confirms, the new li carries the class pending.');\nT.expect((lis[2].textContent || '').indexOf('Ship the capstone') !== -1, 'The ghost note shows its text right away.');\nT.eq(T.val('#noteInput'), '', 'Clear the input in the submit handler.');\nT.eq(T.attr('#statusLine', 'aria-live'), 'polite', 'Keep the aria-live=\"polite\" status region — screen readers listen to it.');\nawait T.sleep(200);\nT.eq(T.count('#noteList li.pending'), 0, 'The server answered 201 — swap the temp item for res.body and drop pending.');\nT.expect((T.text('#countLine') || '').indexOf('3') !== -1, 'Count line: 3 notes.');\nT.expect((T.text('#statusLine') || '').toLowerCase().indexOf('added ship the capstone') !== -1, 'Announce it: statusLine = \"Added \" + the note text.');\nT.eq(handleRequest({ method: 'GET', path: '/api/notes', body: null }).body.length, 3, 'The note really is in the server store.');" },
        { text: "**Rollback.** A whitespace-only submit is rejected *by the server* (400 envelope) — the ghost vanishes and `#errorBox` shows the server's message.",
          test: "T.type('#noteInput', '   ');\nT.submit('#noteForm');\nawait T.sleep(200);\nT.eq(T.count('#noteList li'), 3, 'The server said 400 — splice the optimistic ghost back OUT (rollback), leaving 3 notes.');\nT.expect(T.css('#errorBox', 'display') !== 'none', 'Show #errorBox by adding its class show.');\nT.expect((T.text('#errorBox') || '').toLowerCase().indexOf('required') !== -1, 'Render the server message itself: res.body.error.message.');\nT.expect((T.text('#countLine') || '').indexOf('3') !== -1, 'Count stays at 3.');\nvar bad = handleRequest({ method: 'POST', path: '/api/notes', body: { text: '' } });\nT.eq(bad.status, 400, 'Asked directly, the server must refuse empty text with 400 — validation lives on the SERVER, not the form.');\nT.expect(bad.body && bad.body.error && typeof bad.body.error.message === 'string', 'Answer with the envelope: { error: { code, message } }.');\nT.eq(handleRequest({ method: 'GET', path: '/api/notes', body: null }).body.length, 3, 'Nothing was created by the bad request.');" },
        { text: "**Recovery.** The next good add clears the banner, announces itself, and autosaves to localStorage.",
          test: "T.type('#noteInput', 'Deploy on Friday');\nT.submit('#noteForm');\nawait T.sleep(200);\nT.eq(T.count('#noteList li'), 4, 'A good add works again — 4 notes now.');\nT.expect(T.css('#errorBox', 'display') === 'none', 'Success must CLEAR the error banner — clearError() in the 201 branch.');\nT.expect((T.text('#statusLine') || '').toLowerCase().indexOf('added deploy on friday') !== -1, 'The announcer follows along.');\nvar stored = JSON.parse(localStorage.getItem('notestream-notes') || 'null');\nT.expect(Array.isArray(stored) && stored.length === 4, 'Autosave: every render() should end with save() — the key notestream-notes holds the JSON array.');\nT.eq(stored[3].text, 'Deploy on Friday', 'The newest note is in storage, text intact.');" },
        { text: "**Delete.** ✕ removes a note optimistically, announces the removal, and the server + storage agree.",
          test: "T.click('#noteList li .del');\nT.eq(T.count('#noteList li'), 3, 'Delete is optimistic too — splice + render() BEFORE awaiting the DELETE call.');\nawait T.sleep(200);\nT.expect((T.text('#noteList') || '').toLowerCase().indexOf('welcome to notestream') === -1, 'The first note (Welcome to NoteStream) is gone from the list.');\nT.expect((T.text('#statusLine') || '').toLowerCase().indexOf('removed welcome to notestream') !== -1, 'Announce removals as well: \"Removed \" + the note text.');\nT.eq(handleRequest({ method: 'GET', path: '/api/notes', body: null }).body.length, 3, 'The server store shrank to 3 — your DELETE route ran.');\nvar stored = JSON.parse(localStorage.getItem('notestream-notes') || 'null');\nT.expect(Array.isArray(stored) && stored.length === 3, 'Storage agrees: 3 notes.');" },
        { text: "**Export.** The button fills `#exportBox` with pretty-printed JSON that parses right back into the same notes.",
          test: "T.click('#exportBtn');\nvar raw = T.val('#exportBox') || '';\nT.expect(raw.indexOf('\\n') !== -1, 'Pretty-print it: JSON.stringify(items, null, 2) — the 2 adds real newlines and indentation.');\nvar data = null;\ntry { data = JSON.parse(raw); } catch (e) { }\nT.expect(Array.isArray(data) && data.length === 3, 'The textarea must parse straight back into your 3 notes — export items, nothing hand-built.');\nT.eq(data.map(function (n) { return n.text; }), ['Pair the client with the server', 'Ship the capstone', 'Deploy on Friday'], 'Same notes, same order, round-tripped through JSON.');\nT.eq(data[1].id, 3, 'Real server ids only — the optimistic temp id must have been swapped for the id the 201 returned.');" }
      ],
      files: [
        { name: "index.html", content: "<!DOCTYPE html>\n<html>\n<head>\n  <link rel=\"stylesheet\" href=\"styles.css\">\n  <title>NoteStream</title>\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n  <meta name=\"description\" content=\"NoteStream — a full-stack notes app on one page.\">\n</head>\n<body>\n  <header>\n    <h1>NoteStream 📡</h1>\n    <p class=\"tagline\">Notes that survive the network.</p>\n  </header>\n\n  <form id=\"noteForm\">\n    <label for=\"noteInput\">New note</label>\n    <input id=\"noteInput\" placeholder=\"Type a note…\" autocomplete=\"off\">\n    <button>Add</button>\n  </form>\n\n  <div id=\"errorBox\"></div>\n\n  <ul id=\"noteList\"></ul>\n\n  <p id=\"countLine\">0 notes</p>\n  <p id=\"statusLine\" aria-live=\"polite\"></p>\n\n  <section class=\"export\">\n    <button id=\"exportBtn\" type=\"button\">Export JSON</button>\n    <textarea id=\"exportBox\" rows=\"6\" aria-label=\"Exported notes as JSON\"></textarea>\n  </section>\n\n  <footer>\n    <a href=\"https://github.com/you/notestream\">Source on GitHub</a>\n  </footer>\n\n  <script src=\"script.js\"></script>\n</body>\n</html>\n" },
        { name: "styles.css", content: "body {\n  font-family: Arial, sans-serif;\n  background: #f1f5f9;\n  max-width: 520px;\n  margin: 0 auto;\n  padding: 20px;\n}\nheader h1 { margin: 8px 0 2px; }\n.tagline { color: #64748b; margin: 0 0 16px; }\n#noteForm { display: flex; gap: 8px; align-items: center; }\n#noteForm label { font-size: 14px; color: #334155; }\n#noteInput {\n  flex: 1;\n  padding: 10px 12px;\n  border: 2px solid #cbd5e1;\n  border-radius: 10px;\n  font-size: 15px;\n}\n#noteForm button {\n  min-height: 44px;\n  padding: 10px 16px;\n  border: none;\n  border-radius: 10px;\n  background: #0ea5e9;\n  color: white;\n  font-weight: bold;\n  cursor: pointer;\n}\n#errorBox {\n  display: none;\n  background: #fee2e2;\n  color: #b91c1c;\n  padding: 10px 12px;\n  border-radius: 10px;\n  margin-top: 12px;\n}\n#errorBox.show { display: block; }\n#noteList { list-style: none; padding: 0; }\n#noteList li {\n  background: white;\n  margin-top: 8px;\n  padding: 12px 14px;\n  border-radius: 10px;\n  display: flex;\n  justify-content: space-between;\n  gap: 8px;\n}\n#noteList li.pending { opacity: 0.5; }\n.del {\n  border: none;\n  background: none;\n  color: #ef4444;\n  font-weight: bold;\n  cursor: pointer;\n}\n#countLine { color: #64748b; font-size: 14px; margin-top: 14px; }\n#statusLine { color: #0369a1; font-size: 14px; min-height: 18px; margin: 4px 0 16px; }\n.export { border-top: 2px dashed #cbd5e1; padding-top: 12px; }\n#exportBtn {\n  min-height: 44px;\n  padding: 8px 14px;\n  border: 2px solid #cbd5e1;\n  border-radius: 10px;\n  background: white;\n  cursor: pointer;\n  font-size: 14px;\n}\n#exportBox {\n  width: 100%;\n  margin-top: 8px;\n  box-sizing: border-box;\n  font-family: monospace;\n  font-size: 12px;\n  border: 2px solid #cbd5e1;\n  border-radius: 10px;\n  padding: 8px;\n}\nfooter { margin-top: 20px; font-size: 13px; }\nfooter a { color: #64748b; }\n" },
        { name: "script.js", content: "/* ============ SERVER (imagine this on another machine) ============\n   Exactly what you built in Back-End Foundations — same request in,\n   same { status, body } out. It lives up here so you can watch both\n   halves of your stack at once. */\n\nlet notes = [\n  { id: 1, text: \"Welcome to NoteStream\" },\n  { id: 2, text: \"Pair the client with the server\" }\n];\nlet nextId = 3;\n\nfunction handleRequest(req) {\n  // TODO 1 — GET /api/notes\n  //   → { status: 200, body: JSON.parse(JSON.stringify(notes)) }\n  // TODO 2 — POST /api/notes — validate FIRST:\n  //   const text = (req.body && typeof req.body.text === \"string\") ? req.body.text.trim() : \"\";\n  //   empty → { status: 400, body: { error: { code: \"empty\", message: \"Note text is required.\" } } }\n  //   else  → push { id: nextId, text } (then bump nextId) → { status: 201, body: the new note }\n  // TODO 3 — DELETE /api/notes/<id>\n  //   const id = Number(req.path.slice(\"/api/notes/\".length));\n  //   unknown id → { status: 404, body: { error: { code: \"not-found\", message: \"No note \" + id } } }\n  //   found → splice it out → { status: 200, body: { ok: true } }\n  return { status: 404, body: { error: { code: \"no-route\", message: \"No route: \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\nconst statusLine = document.querySelector(\"#statusLine\");\nconst errorBox = document.querySelector(\"#errorBox\");\nconst exportBtn = document.querySelector(\"#exportBtn\");\nconst exportBox = document.querySelector(\"#exportBox\");\n\nlet items = [];       // client cache: { id, text, pending }\nlet tempCount = 0;    // deterministic ids for optimistic notes\n\n// TODO 4 — save(): items → localStorage key \"notestream-notes\" (JSON.stringify!)\n\n// TODO 5 — showError(message): put it in errorBox + add the class \"show\"\n//          clearError(): empty the box + remove the class\n\n// TODO 6 — render(): rebuild #noteList from items\n//   • one <li> per item: its text + a ✕ button (class \"del\") that calls removeNote(item)\n//   • item.pending === true → li.classList.add(\"pending\")\n//   • countLine: items.length + \" notes\"\n//   • last line: save()  ← every render autosaves\n\n// TODO 7 — async boot(): GET /api/notes, then\n//   items = res.body.map(n => ({ id: n.id, text: n.text, pending: false })); render();\n\n// TODO 8 — async addNote(text): the optimistic two-step\n//   a) tempCount += 1; push { id: \"tmp-\" + tempCount, text, pending: true }; render()\n//   b) await api POST { text } —\n//      201 → swap the temp for res.body (pending: false), clearError(),\n//            statusLine = \"Added \" + res.body.text\n//      else → splice the temp back OUT, showError(res.body.error.message)\n//   render() again either way\n\n// TODO 9 — async removeNote(item): optimistic too\n//   splice it out, render(), then await api DELETE \"/api/notes/\" + item.id\n//   200 → statusLine = \"Removed \" + item.text\n//   else → put it back, showError(res.body.error.message), render()\n\nnoteForm.addEventListener(\"submit\", (e) => {\n  e.preventDefault();\n  // TODO 10 — addNote(noteInput.value), then clear the input\n});\n\n// TODO 11 — exportBtn click → exportBox.value = JSON.stringify(items, null, 2)\n\n// TODO 12 — boot();\n" }
      ],
      hints: [
        "Server GET + POST: `if (req.method === \"GET\" && req.path === \"/api/notes\") return { status: 200, body: JSON.parse(JSON.stringify(notes)) };` — POST trims first: empty → the 400 envelope, otherwise `const note = { id: nextId, text: text }; nextId = nextId + 1; notes.push(note); return { status: 201, body: JSON.parse(JSON.stringify(note)) };`",
        "Server DELETE: `const id = Number(req.path.slice(\"/api/notes/\".length)); const at = notes.findIndex(function (n) { return n.id === id; });` — if `at === -1` answer the 404 envelope, else `notes.splice(at, 1); return { status: 200, body: { ok: true } };`",
        "render() is TaskMaster with one twist: `if (item.pending) li.classList.add(\"pending\");` — and its last line is `save();` so every redraw autosaves. The ✕ button: `del.className = \"del\"; del.addEventListener(\"click\", () => removeNote(item));`",
        "Optimistic add, step a: `tempCount = tempCount + 1; const temp = { id: \"tmp-\" + tempCount, text: text, pending: true }; items.push(temp); render();` — THEN `const res = await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });`",
        "Confirm or roll back: `const at = items.indexOf(temp);` — on 201 `items[at] = { id: res.body.id, text: res.body.text, pending: false }; clearError(); statusLine.textContent = \"Added \" + res.body.text;` — otherwise `items.splice(at, 1); showError(res.body.error.message);` — then `render();` either way.",
        "removeNote mirrors it: splice out + render() first, `await api({ method: \"DELETE\", path: \"/api/notes/\" + item.id, body: null })`, then `statusLine.textContent = \"Removed \" + item.text;` (or splice it back in and showError on failure). Boot: `const res = await api({ method: \"GET\", path: \"/api/notes\", body: null }); items = res.body.map(function (n) { return { id: n.id, text: n.text, pending: false }; }); render();` — and do not forget `boot();` at the very bottom."
      ],
      solution: {
        "script.js": "/* ============ SERVER (imagine this on another machine) ============\n   Exactly what you built in Back-End Foundations — same request in,\n   same { status, body } out. It lives up here so you can watch both\n   halves of your stack at once. */\n\nlet notes = [\n  { id: 1, text: \"Welcome to NoteStream\" },\n  { id: 2, text: \"Pair the client with the server\" }\n];\nlet nextId = 3;\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/notes\") {\n    return { status: 200, body: JSON.parse(JSON.stringify(notes)) };\n  }\n  if (req.method === \"POST\" && req.path === \"/api/notes\") {\n    const text = (req.body && typeof req.body.text === \"string\") ? req.body.text.trim() : \"\";\n    if (!text) {\n      return { status: 400, body: { error: { code: \"empty\", message: \"Note text is required.\" } } };\n    }\n    const note = { id: nextId, text: text };\n    nextId = nextId + 1;\n    notes.push(note);\n    return { status: 201, body: JSON.parse(JSON.stringify(note)) };\n  }\n  if (req.method === \"DELETE\" && req.path.indexOf(\"/api/notes/\") === 0) {\n    const id = Number(req.path.slice(\"/api/notes/\".length));\n    const at = notes.findIndex(function (n) { return n.id === id; });\n    if (at === -1) {\n      return { status: 404, body: { error: { code: \"not-found\", message: \"No note \" + id } } };\n    }\n    notes.splice(at, 1);\n    return { status: 200, body: { ok: true } };\n  }\n  return { status: 404, body: { error: { code: \"no-route\", message: \"No route: \" + req.method + \" \" + req.path } } };\n}\n\n/* ============ THE WIRE (fake network: 20 ms) ============ */\nfunction api(req) {\n  return new Promise((resolve) => setTimeout(() => resolve(handleRequest(req)), 20));\n}\n\n/* ============ CLIENT (the page) ============ */\nconst noteForm = document.querySelector(\"#noteForm\");\nconst noteInput = document.querySelector(\"#noteInput\");\nconst noteList = document.querySelector(\"#noteList\");\nconst countLine = document.querySelector(\"#countLine\");\nconst statusLine = document.querySelector(\"#statusLine\");\nconst errorBox = document.querySelector(\"#errorBox\");\nconst exportBtn = document.querySelector(\"#exportBtn\");\nconst exportBox = document.querySelector(\"#exportBox\");\n\nlet items = [];       // client cache: { id, text, pending }\nlet tempCount = 0;    // deterministic ids for optimistic notes\n\nfunction save() {\n  localStorage.setItem(\"notestream-notes\", JSON.stringify(items));\n}\n\nfunction showError(message) {\n  errorBox.textContent = message;\n  errorBox.classList.add(\"show\");\n}\n\nfunction clearError() {\n  errorBox.textContent = \"\";\n  errorBox.classList.remove(\"show\");\n}\n\nfunction render() {\n  noteList.innerHTML = \"\";\n  items.forEach((item) => {\n    const li = document.createElement(\"li\");\n    li.textContent = item.text;\n    if (item.pending) li.classList.add(\"pending\");\n\n    const del = document.createElement(\"button\");\n    del.type = \"button\";\n    del.className = \"del\";\n    del.textContent = \"✕\";\n    del.addEventListener(\"click\", () => removeNote(item));\n    li.appendChild(del);\n\n    noteList.appendChild(li);\n  });\n  countLine.textContent = items.length + \" notes\";\n  save();\n}\n\nasync function boot() {\n  const res = await api({ method: \"GET\", path: \"/api/notes\", body: null });\n  items = res.body.map(function (n) { return { id: n.id, text: n.text, pending: false }; });\n  render();\n}\n\nasync function addNote(text) {\n  tempCount = tempCount + 1;\n  const temp = { id: \"tmp-\" + tempCount, text: text, pending: true };\n  items.push(temp);\n  render();\n\n  const res = await api({ method: \"POST\", path: \"/api/notes\", body: { text: text } });\n  const at = items.indexOf(temp);\n  if (res.status === 201) {\n    items[at] = { id: res.body.id, text: res.body.text, pending: false };\n    clearError();\n    statusLine.textContent = \"Added \" + res.body.text;\n  } else {\n    items.splice(at, 1);\n    showError(res.body.error.message);\n  }\n  render();\n}\n\nasync function removeNote(item) {\n  const at = items.indexOf(item);\n  items.splice(at, 1);\n  render();\n\n  const res = await api({ method: \"DELETE\", path: \"/api/notes/\" + item.id, body: null });\n  if (res.status === 200) {\n    statusLine.textContent = \"Removed \" + item.text;\n  } else {\n    items.splice(at, 0, item);\n    showError(res.body.error.message);\n    render();\n  }\n}\n\nnoteForm.addEventListener(\"submit\", (e) => {\n  e.preventDefault();\n  addNote(noteInput.value);\n  noteInput.value = \"\";\n});\n\nexportBtn.addEventListener(\"click\", () => {\n  exportBox.value = JSON.stringify(items, null, 2);\n});\n\nboot();\n"
      }
    },

    {
      id: "cap-quiz-6",
      title: "Shipping quiz",
      kind: "quiz", xp: 10,
      questions: [
        { q: "NoteStream is done. Ship it — put the everyday git ritual in order:",
          code: "1. git ____   (stage your changes)\n2. git ____   (snapshot with a message)\n3. git ____   (upload to GitHub)",
          lang: "js",
          choices: ["commit → add → push", "push → commit → add", "add → commit → push", "add → push → commit"],
          answer: 2, explain: "Stage (add), snapshot with a message (commit), upload (push). You will type this trio thousands of times — it is the heartbeat of shipping." },
        { q: "What does GitHub Pages actually do?",
          choices: ["Runs your handleRequest function on GitHub's servers", "Serves your repo's static files (HTML/CSS/JS) at a public URL", "Compiles your JavaScript into a mobile app", "Backs up your localStorage to the cloud"],
          answer: 1, explain: "Pages is free static hosting: push files, flip a switch, get a URL. Your in-page \"server\" ships as plain JS to every visitor's browser — a real backend would need a server host." },
        { q: "You pushed a styles.css fix, but your phone still shows the old design. Most likely?",
          choices: ["Phones cannot load CSS files", "GitHub Pages only works on desktop browsers", "You forgot the viewport meta tag", "The phone's browser cached the old file — hard-refresh, and always re-test on a real device after deploying"],
          answer: 3, explain: "Browsers aggressively cache CSS and JS. Hard-refresh, or cache-bust (styles.css?v=2). This is exactly why pros test every deploy on a real phone, not just the laptop that wrote it." },
        { q: "Why does every `<img>` need real `alt` text before you ship?",
          choices: ["Screen readers speak it, and it appears if the image fails to load", "It makes the image download faster", "Browsers refuse to render images without it", "It is only required for decorative images"],
          answer: 0, explain: "alt is the image for anyone who cannot see it — screen-reader users, broken connections, search engines. (Purely decorative images get an EMPTY alt=\"\", not a missing one.)" },
        { q: "Your server answered this. What just happened?",
          code: "{ status: 201, body: { id: 5, text: \"…\" } }",
          lang: "js",
          choices: ["A request hit an unknown route", "The client sent invalid data", "Something was created — the classic POST success", "The server crashed"],
          answer: 2, explain: "The recap you shipped by heart: 200 OK, 201 Created (POST success), 400 bad input with an { error } envelope, 404 unknown route or id." },
        { q: "The one pattern under this entire course — which line-up is right?",
          choices: ["The DOM is the source of truth; state copies whatever it shows", "Events change state → render() redraws the UI from state — one source of truth", "render() changes the state, and events redraw the page", "localStorage is the source of truth while the app runs"],
          answer: 1, explain: "State drives everything: events mutate the array, render() repaints from it, storage just mirrors it. That loop is React and Vue in miniature — you now recognize it everywhere." },
        { q: "Where does localStorage data actually live?",
          choices: ["On your server, next to the notes array", "On GitHub, inside the repository", "In the cloud, synced to every device you own", "In that one browser on that one device, per-site — which is exactly why real apps add a backend"],
          answer: 3, explain: "localStorage is per-browser, per-site, per-device. Great for autosave; useless for your phone seeing your laptop's notes. The upgrade path is the server you already know how to build. 🎓" }
      ]
    }
  ]
});
