{
  "auth-u1-1": {
    "hash": "452be4fd07639794",
    "why": [
      "The starter builds the id from the user name, so anyone who has seen `token-ada` can type `token-bo`. A session id is a bearer credential: whoever presents it is that user. It has to come from a random source, and `randHex(16)` gives 128 bits nobody can derive or guess.",
      "The starter's row records only who the session belongs to. Without `created` and `lastSeen` the server can't tell how old a session is or when it was last used, so the timeouts later in this unit would have nothing to measure.",
      null,
      null
    ]
  },
  "auth-u1-2": {
    "hash": "5ac51d1e2ceefcc3",
    "why": [
      "The starter writes only `Path`, `Max-Age` and `HttpOnly`, so the cookie never gets `Secure` or `SameSite`. It also tests `opts.maxAge` for truthiness, which silently drops `maxAge: 0`. Collecting the parts in an array and checking `!= null` writes every attribute that was set, including a zero.",
      null,
      "The starter puts any name and value straight into the header. A value like `abc; Domain=evil.example` then adds an attribute the server never chose. Checking both against the cookie grammar, and throwing a `TypeError` when they don't match, keeps the header exactly what the caller asked for.",
      "`part.split(\"=\")` splits on every `=`, and destructuring keeps only the first two pieces, so `next=/a?b=c` loses its `=c`. Cutting at the first `=` with `indexOf` keeps the rest of the value intact. Skipping pieces with no `=` and names already seen stops odd headers from overwriting a real cookie."
    ]
  },
  "auth-u1-3": {
    "hash": "ef510c72e66934bd",
    "why": [
      null,
      "The starter finds the session the visitor arrived with and marks it as logged in. If an attacker planted that id, the attacker's copy now opens the victim's account. Deleting the old row and creating a fresh one at login leaves the planted id pointing at nothing.",
      "The starter's login answers with a brand-new session and an empty cart, so anything the visitor added before signing in is lost. Carrying `cart` from the old row into the new one, and sending that new id in `Set-Cookie`, makes the rotation invisible to the shopper.",
      null
    ]
  },
  "auth-u1-4": {
    "hash": "9a7674bd0168322a",
    "why": [
      "The starter's `touchSession` returns any row it finds, however old, and never updates it. A session nobody checks against the clock never ends, and one whose `lastSeen` never moves can't have an idle timeout. Read `now()` once, end the session when either limit is reached, and only then slide `lastSeen` forward.",
      null,
      null,
      null
    ]
  },
  "auth-u1-5": {
    "hash": "419b4c01e136b29b",
    "why": [
      "The starter's clearing cookie is `sid=; Max-Age=0` with no `Path`. Browsers match a cookie by name and path, so that header can leave the original `sid` in place. Building it with `serializeCookie` and the same `Path=/` the session cookie was set with actually replaces it.",
      "The starter only tells the browser to forget the cookie; the row stays on the server. Anyone holding a copy of the id (a proxy log, malware, the fixation attacker) keeps using it until it times out. Deleting the row is what makes logout real.",
      null,
      "The starter has no `/api/logout-all` route, so it answers 404. Signing out everywhere means finding every row with the caller's `userName` and deleting them all, which is only possible because a session is a row the server can search."
    ]
  },
  "auth-u4-1": {
    "hash": "98d87a197feb340d",
    "why": [
      "The starter's encoder never reads the bytes, so it returns an empty string. base64url takes the bytes three at a time as one 24-bit number and writes four 6-bit characters, or two or three for a short final group, with no `=` padding.",
      null,
      "The starter's decoder returns an empty array for every input. Decoding runs the encoder backwards: each character is worth 6 bits, and a byte comes out whenever 8 bits have built up. It also has to throw for a character outside the alphabet and for a length that leaves one stray character.",
      null
    ]
  },
  "auth-u4-2": {
    "hash": "ebecfab62cb6f162",
    "why": [
      "The starter's `sign` returns only the encoded payload, with no signature, so anyone could write their own token. Appending a dot and the base64url HMAC of the payload means only a holder of `SECRET` can produce a token that matches.",
      null,
      "The starter's `verify` decodes whatever payload it gets and never checks a signature, so an edited `role: \"admin\"` goes straight through. Recompute the HMAC and refuse a mismatch first; only then parse the payload, inside a `try`, so a malformed token is `null` rather than an exception.",
      null
    ]
  },
  "auth-u4-3": {
    "hash": "a14718ef1f00ec71",
    "why": [
      null,
      "The starter's loop returns at the first byte that differs, so a signature wrong in byte 0 is rejected sooner than one wrong in byte 31. Measured over many requests, that difference gives a valid signature away byte by byte. OR-ing every byte's XOR into one number and checking it once at the end costs the same whatever the input.",
      "The starter's `verify` still compares base64url strings with `!==`, which stops at the first different character and never calls `safeEqual`. Decoding the given signature to bytes (refusing one that won't decode) and comparing with `safeEqual` closes the leak where it matters.",
      null
    ]
  },
  "auth-u4-4": {
    "hash": "29d89accd3b1424a",
    "why": [
      null,
      "The starter ignores the `kid` and tries every key until one matches, so the label means nothing: a token that says `2026-09` but was signed with August's key still passes, and an unknown `kid` costs one HMAC per key. Looking the `kid` up with `hasOwnProperty` and checking only that key fixes both.",
      "The starter's `rotateKey` does nothing, so `sign` keeps using the old key. Rotating adds the new secret and switches `currentKid` to it, and it refuses a `kid` that already exists, because silently replacing a secret breaks every token it signed.",
      "The starter's `retireKey` does nothing, so a retired key keeps verifying its tokens forever. Deleting it from `KEYS` ends them, and refusing to delete `currentKid` stops every newly signed token from failing at once."
    ]
  },
  "auth-u5-1": {
    "hash": "5d862413c48504c2",
    "why": [
      "The starter's `decodeJwt` returns `null` for every token, so it never shows what a JWT carries. A JWT is three base64url parts: the first two are JSON anyone can read with `readPart`, and the third is the signature, kept as text. Reading them inside a `try` makes a malformed token `null` instead of an exception.",
      null,
      "The starter copies the whole user record into the token, so the email, password hash and reset token travel with every request, readable by anyone who decodes it. Building the claims by hand keeps the token to what the server needs next time: who (`sub`), what they may do (`role`) and when (`iat`, `exp`).",
      null
    ]
  },
  "auth-u5-2": {
    "hash": "675d0a20d5669fe5",
    "why": [
      "The starter's `forgeNone` returns an empty string, which isn't a JWT at all. An unsecured JWT is still three parts: a header saying `alg: \"none\"`, the claims, and an empty signature after the last dot.",
      null,
      "The starter keeps the legacy rule that returns the payload whenever the header says `none`, so the token decides how it gets checked. Allowing exactly `HS256`, and refusing a header that isn't an object, puts that decision back on the server: `none` in any spelling, `HS512` and a missing `alg` all fall out.",
      null
    ]
  },
  "auth-u5-3": {
    "hash": "4f5e025aab195624",
    "why": [
      "The starter's `acceptToken` returns any correctly signed token, so an expired one, one not valid yet, one from another issuer and one meant for another API all get in. Each claim needs its own check, in seconds (`Math.floor(now() / 1000)`) with `LEEWAY` for clock drift. All four checks live in this one block, which is why the later checkpoints pass once this one does.",
      null,
      null,
      null
    ]
  },
  "auth-u5-4": {
    "hash": "3d7d407236c8b9c5",
    "why": [
      "The starter stores the raw refresh token as the Map key and records no family or `used` flag. A leaked table would hand out working tokens, and there would be nothing to rotate or revoke. Keying by `sha256(refresh)` and recording the family with `used: false` gives each refresh token a row the server can reason about.",
      "The starter hands back the same refresh token every time and never notices one being used twice. Rotation marks the old row `used` and issues a new token in the same family, and a used token that comes back means someone copied it, so every token in that family is deleted. The lookup also has to go by `sha256`, since that is how the rows are stored.",
      null,
      null
    ]
  },
  "auth-u5-p": {
    "hash": "79eaf2f6042e816e",
    "why": [
      "The starter returns the claims for any token whose header says `alg: none`, so an unsigned token claiming to be admin gets in. The server allows exactly `HS256` and refuses everything else.",
      "The starter checks `exp` and `iss` but never `aud`, so a token the same auth server issued for `billing-api` opens `notes-api` too. Normalise `aud` to an array and refuse a token that doesn't name this API.",
      "The starter's `LEEWAY` is 60000, a millisecond-sized number used in a check where every time is in seconds. That is almost 17 hours of drift allowance, so a token that expired two hours ago still passes. JWT times are seconds, so the leeway is 60.",
      "The starter compares base64url strings with `!==`, which stops at the first different character, so rejecting a bad signature takes longer the more of it is right. Decode the signature to bytes (a bad encoding is `null`) and compare with `safeEqual`, which reads every byte whatever it finds.",
      "The starter falls back to the first key in `KEYS` when it doesn't recognise the token's `kid`, so a token naming a key that doesn't exist is still checked, and accepted, against some other key. Refuse a `kid` that isn't one of `KEYS`' own properties before any HMAC runs."
    ]
  },
  "auth-u7-1": {
    "hash": "947f4d16c782a1f6",
    "why": [
      "The starter's `hotp` returns an empty string, so there is no code to compare. HOTP writes the counter as 8 bytes using division (shifts stop at 32 bits), takes `hmac(\"sha1\", ...)`, reads 4 bytes at the offset given by the last byte's low 4 bits with the top bit cleared, and keeps the last `digits` digits as a zero-padded string. Checkpoints 2 and 3 exercise this same code, which is why they pass once this one does.",
      null,
      null,
      "The starter passes `now()` straight to `totp`, but `now()` counts milliseconds and TOTP counts seconds. The step comes out a thousand times too large, so the code never matches the one on the phone. Convert with `Math.floor(now() / 1000)` first."
    ]
  },
  "auth-u7-2": {
    "hash": "b5b292220c92308d",
    "why": [
      "The starter compares only against the current code and never records a use. A phone clock 30 seconds off is locked out, and a code that just worked can be sent again. Checking every step within `WINDOW`, and accepting only a step newer than `lastStep`, fixes both; the later checkpoints test the same loop.",
      null,
      null,
      null
    ]
  },
  "auth-u7-3": {
    "hash": "857123c319981316",
    "why": [
      "The starter answers 404 \"No account with that email\" for unknown addresses and names the address for known ones, so the form tells anyone which emails have accounts. Send the email only when the account exists, but return one identical response either way.",
      "The starter keys `resetTokens` by the raw token, so anyone who can read the table can reset any account with a pending link. Store `sha256(token)` with an expiry and a `used` flag. The token is 128 random bits, so a single SHA-256 is enough.",
      "The starter's `resetPassword` never marks a token used or checks its age, so one link resets the password again and again for as long as the server runs. Look the row up by `sha256`, refuse it when it is used or past `expires`, and mark it used before changing the password. The expiry check here is what checkpoint 4 tests.",
      null
    ]
  },
  "auth-u7-4": {
    "hash": "395bf78239a09648",
    "why": [
      "The starter stores the recovery codes themselves, so a leaked table is a list of working codes. Store `sha256` of each code instead: the codes are shown to the user once and never kept.",
      "The starter checks the typed text as-is against the stored set, so a code copied with spaces or in capitals fails, and a code that does match keeps working forever. Trim and lowercase, compare by `sha256`, and delete the hash once it has been used.",
      "The starter trusts any assertion whose signature verifies, so a reused challenge, a lookalike origin, a key scoped to another site and a cloned authenticator all get through, and the new `signCount` is never recorded. Check the owner, the type, the one-time challenge, the origin, `rpIdHash` and the counter around the signature, then store the counter. Checkpoint 4 tests the rest of these same checks.",
      null
    ]
  },
  "auth-u2-1": {
    "hash": "81decacf31486d9a",
    "why": [
      "The starter's session line has `Domain=app.example`, and a Domain cookie goes to that domain and every subdomain, so `blog.app.example` (another team's server) receives the session. Leaving Domain off makes the cookie host-only: it only ever goes back to `app.example`.",
      "The starter's theme line has no Domain, so the theme is host-only and never reaches the blog. A preference every subdomain should share is exactly what `Domain=app.example` is for.",
      "The starter scopes `docs-lang` to `Path=/doc`. Paths match only at a `/` boundary, so `/doc` covers `/doc` and `/doc/...` but not `/docs`. The cookie belongs under `Path=/docs`.",
      null
    ]
  },
  "auth-u2-2": {
    "hash": "f9aee4ed24370a91",
    "why": [
      "The starter gives `__Host-sid` a `Domain` attribute. A `__Host-` name promises the cookie came from exactly this host, so the browser refuses any `__Host-` line that has a Domain at all. Removing it (keeping `Secure` and `Path=/`) makes the line acceptable.",
      "The starter's `__Secure-pref` line has no `Secure` attribute, and the `__Secure-` prefix is a promise that it does. The browser refuses the line until `Secure` is added.",
      "The starter scopes `__Host-csrf` to `Path=/account`. A `__Host-` cookie must have exactly `Path=/`, so the browser refuses the line; with `Path=/` it is stored and sent to every path, `/account` included.",
      null
    ]
  },
  "auth-u2-3": {
    "hash": "6cb74c46ffa5ffc3",
    "why": [
      "The starter's logout sends an `Expires` date in the year 9999, which does not delete anything: the browser stores an empty `sid` that lasts for centuries. It also has no `Path`, so from `/account/logout` it would be filed under `/account` as a separate cookie. `Path=/` with `Max-Age=0` replaces the real `sid` and deletes it at once, which is why checkpoint 2 passes with the same line.",
      null,
      "The starter multiplies by 1000, but `Max-Age` counts seconds and `DAY` already is seconds, so the cookie lasts about 82 years instead of 30 days. `Max-Age` is `30 * DAY`.",
      "The starter never sends the remember cookie again after login, so it expires 30 days after sign-in however often ada visits. Sending it again on each visit that carries it slides the 30 days forward, the way Unit 1 slid `lastSeen`."
    ]
  },
  "auth-u2-4": {
    "hash": "e28bc2d8e1e5e509",
    "why": [
      null,
      "The starter's session is `SameSite=None`, so the browser attaches it to every cross-site request, a form on `evil.example` included, and the forged transfer goes through. `Lax` keeps cross-site top-level GET navigations (the email link) and drops cross-site POSTs and fetches; checkpoint 3 tests the fetch half of the same change.",
      null,
      "The starter's widget line asks for `SameSite=None` without `Secure`, and browsers refuse that combination, so the flag is never stored and the embedded button never sees it. A cookie meant to ride cross-site requests has to be `Secure`."
    ]
  },
  "auth-u3-1": {
    "hash": "41708011ed5b9eeb",
    "why": [
      "The starter's `forgeTransfer` does nothing, so no request is made and the balance stays put. The attack is a single cross-site form POST from a page on `evil.example` to the bank's `/transfer`. It needs no token and no password: the browser attaches ada's session cookie to the request on its own. The later checkpoints inspect that same request.",
      null,
      null,
      null
    ]
  },
  "auth-u3-2": {
    "hash": "51d6b242971a4019",
    "why": [
      "The starter returns the fixed string \"token\", so every session shares one guessable value. Minting `randHex(16)` on first use and storing it per session gives each session its own unguessable token, stable across the requests that read it.",
      "The starter's `checkCsrf` allows every request, so the forged POST goes through. Requiring `req.body.csrf` to equal this session's token refuses a forgery, which arrives without the token because the attacker's page can't read it off your origin's form.",
      null,
      null
    ]
  },
  "auth-u3-3": {
    "hash": "010e7976c4f6c45b",
    "why": [
      "The starter returns a bare nonce as the token and, in `tokenValid`, accepts any string, which is the naive double-submit that a sibling subdomain defeats by setting both halves. Signing the nonce with `hmac(\"sha256\", CSRF_KEY, sid + \"!\" + nonce)` binds the token to the session, and verifying by recomputing that signature from the request's own `sid` rejects any value the attacker invents, since they lack the key. The other checkpoints exercise this same pair.",
      null,
      null,
      null
    ]
  },
  "auth-u3-4": {
    "hash": "0aaa89de83fa0035",
    "why": [
      "The starter's `csrfGuard` allows everything, so a cross-site POST still lands. Blocking when `Sec-Fetch-Site` is `cross-site`, and falling back to an `Origin` allow-list when that header is absent, refuses the forgery while leaving same-origin and same-site requests alone. The remaining checkpoints test this same guard through the browser.",
      null,
      null,
      null
    ]
  },
  "auth-u6-1": {
    "hash": "176f8b9d28427cb1",
    "why": [
      "The starter's `buildAuthorizeUrl` returns the bare endpoint with no query, so the IdP has nothing to act on. The authorize URL has to name the flow (`response_type=code`), the app (`client_id`), where to send the code (`redirect_uri`, URL-encoded) and what it wants (`scope=openid`). Checkpoint 2's redirect chain depends on this same URL being right.",
      null,
      "The starter's `/callback` returns a placeholder and never exchanges the code, so no session is created. Calling `idpToken` with the code on the back channel, then storing `tokens.sub` in a session cookie, is what actually signs the user in.",
      null
    ]
  },
  "auth-u6-2": {
    "hash": "34b57de276628867",
    "why": [
      "The starter's `/login` sends no `state` at all, so there is nothing to check on the way back. Minting a state, setting it in a `SameSite=Lax` `oauth_state` cookie, and putting the same value on the authorize URL gives the callback something bound to this browser to compare against.",
      null,
      "The starter's `/callback` exchanges any code it is handed, so an attacker's code injected via a lured link logs the victim into the attacker's account. Refusing the request unless `req.query.state` equals the `oauth_state` cookie — before calling `idpToken` — rejects a callback the app didn't start.",
      null
    ]
  },
  "auth-u6-3": {
    "hash": "b71104160a3998bf",
    "why": [
      "The starter's `challengeFor` returns the verifier unchanged, so the challenge equals the verifier and PKCE proves nothing. The S256 challenge is `base64url(SHA-256(verifier))`: a one-way hash, so the value in the authorize URL can't be turned back into the verifier the exchange needs.",
      "The starter's authorize URL omits the challenge, so the IdP stores none and can't bind the code to this app. Adding `code_challenge` (the hash) and `code_challenge_method=S256`, while keeping the verifier out of the URL, is what makes a stolen code useless. Checkpoint 3's exchange relies on this challenge being present.",
      "The starter's exchange sends no `code_verifier`, so the IdP's PKCE check fails and no session is created. Passing `code_verifier: CODE_VERIFIER` on the back channel proves the app that redeems the code is the one that started the flow.",
      null
    ]
  },
  "auth-u6-4": {
    "hash": "88df6b2c3e84bb85",
    "why": [
      "The starter matches `redirect_uri` with a prefix check, so a genuine sign-in still works but the door is open. Comparing the whole string against the registered `REDIRECT_URI` keeps the honest case working while it closes checkpoint 2's attack.",
      "The prefix check accepts `https://app.example.evil.example/callback`, because it begins with `https://app.example`, and the IdP then redirects a live code to the attacker's host. An exact `!==` comparison rejects any redirect_uri that isn't the registered one, so the code is never issued.",
      null,
      null
    ]
  },
  "auth-u6-5": {
    "hash": "556973b6655c50fa",
    "why": [
      null,
      "The starter's `verifyIdToken` just decodes the payload, checking neither the signature nor the claims, so a token forged for another client, a tampered one, an expired one, or even an opaque access token would all be accepted. Verifying the HS256 signature and then requiring the right `iss`, `aud`, `nonce` and a future `exp` is what makes the token proof of who signed in. The other checkpoints exercise these same checks.",
      null,
      null
    ]
  }
}
