/* Deploying Your App — Unit 7: Ship it for real */
window.CODELAB.addUnit("ship", {
  id: "ship-u7",
  title: "Ship it for real",
  icon: "🚀",
  blurb: "The synthesis: one file replays a complete deploy — preflight, hashed build, base-path serving, CORS answers, rollback — plus the field guides for the real Pages and wrangler screens.",
  cheat: [
    { h: "Ship a static site to GitHub Pages — the real clicks", lang: "js", code: "// NOT runnable in this sandbox — this is the real-world walk.\n// 1. push the site to GitHub (index.html at the root, or in /docs)\n// 2. repo → Settings → Pages\n// 3. Source: \"Deploy from a branch\" → main + /(root)  (or /docs)\n// 4. first deploy takes MINUTES — watch the Actions tab, don't re-push\n// 5. site: https://<user>.github.io/<repo>/   ← note the base path!\n// custom domain: same panel — and COMMIT the CNAME file it creates\n// Jekyll eating _folders? add an empty .nojekyll at the site root", note: "The dropdowns move with every redesign; the model doesn't: one branch + one folder in, static files out." },
    { h: "Ship a Worker with wrangler — the real commands", lang: "js", code: "npm create cloudflare@latest my-api   // scaffold + wrangler.toml\nwrangler dev                          // run LOCALLY on :8787, hot reload\nwrangler deploy                       // bundle + publish to the edge\nwrangler secret put STRIPE_KEY        // secret → encrypted on Cloudflare\nwrangler tail                         // live production logs\n// local secrets: .dev.vars (KEY=value, gitignored) — dev reads it,\n// deploy ignores it. Secrets NEVER go in [vars].", note: "Not runnable here — no network, no account. The config file (wrangler.toml/jsonc) is the source of truth: deploys overwrite dashboard edits." },
    { h: "wrangler.toml — the shape that matters", lang: "js", code: "name = \"notestream-api\"        # renaming = a brand-NEW Worker\nmain = \"src/index.js\"\ncompatibility_date = \"2026-08-01\"\n\n[vars]                         # plain config — commits with the repo\nALLOWED_ORIGIN = \"https://me.github.io\"\n\n[env.staging]                  # bindings are NOT inherited —\n[env.staging.vars]             # re-declare them per environment\nALLOWED_ORIGIN = \"https://staging.me.dev\"", note: "env.NAME reads a [vars] value and a `wrangler secret put` secret the same way — the difference is only where the value lives." },
    { h: "The pre-flight checklist, now executable", lang: "js", code: "preflight(repo)        // [] or, sorted: absolute-asset-path ·\n                       // missing-index · secret-in-repo · uppercase-index\nbuild(src, env)        // __API_URL__ → env value, app.js → app.<hash>.js\nserveStatic(url, dist) // base path + per-file cache headers:\n  // index.html    → cache-control: no-cache\n  // app.<hash>.js → public, max-age=31536000, immutable\ndeploy(state, release) // only a HEALTHY release becomes current\nrollback(state)        // re-point at the previous healthy build", note: "The whole course in six calls — the checklist you can actually run, and just did." },
    { h: "One deploy, end to end (the habit)", lang: "js", code: "// 1. preflight passes locally — no red, no push\n// 2. build — a fresh, content-hashed dist\n// 3. git push  /  wrangler deploy\n// 4. smoke test the LIVE urls:  /  and  /api/health\n// 5. broken? rollback FIRST, diagnose second\n// 6. leaked a secret at ANY point? rotate the key — history remembers", note: "Steps 1, 2 and 5 are code you wrote in this course; steps 3–4 are the two field guides above." }
  ],
  lessons: [

    {
      id: "ship-u7-p",
      title: "Project: ship-it.js — build, serve, guard",
      kind: "js", chip: "SHIP", xp: 50, mins: 45, project: true,
      brief: "The graduation deploy: everything this course taught, replayed as **one file**. `preflight()` reports the repo's three problems, your `fixRepo()` pass clears them, `build()` emits a content-hashed dist and rewrites `index.html`, `serveStatic()` serves it under the `/notestream/` base path with the right `cache-control` per file, a **real** Cloudflare Worker — `export default { async fetch }`, byte-for-byte deployable — answers the page cross-origin, and `deploy()`/`rollback()` make sure a release that fails its smoke test never goes live.\n\nThe givens are marked and ready-made: the djb2 `hash()`, the broken `REPO`, `NOTES`, `ENV`, and the `BASE` path. You write the pipeline. Nothing here touches the real network, git, or DNS — the actual clicks and commands live in this unit's field-guide quizzes and cheat sheet.",
      steps: [
        { text: "`preflight(REPO)` reports the repo's three problems as a **sorted** array of failure codes — and stays quiet on a clean repo.",
          test: "T.expect(typeof preflight === 'function', 'Define preflight(repo) — it returns a SORTED array of failure codes.');\nvar out = preflight(REPO);\nT.eq(out, ['absolute-asset-path', 'secret-in-repo', 'uppercase-index'], 'REPO ships three problems: an href/src that ignores ' + BASE + ' (absolute-asset-path), an sk_live key in .env (secret-in-repo), and a stray Index.html (uppercase-index) — report all three, sorted');\nT.eq(preflight({ 'index.html': '<h1>hi</h1>' }), [], 'A clean repo reports [] — a checker that cries wolf gets ignored');\nT.eq(preflight({ 'app.js': 'x' }), ['missing-index'], 'No index.html at the repo root means Pages has nothing to serve at / — that is its own failure code');" },
        { text: "`fixHrefs(html, base)` repairs root-absolute paths; `fixRepo(repo)` returns a **new** map that passes preflight clean.",
          test: "T.expect(typeof fixHrefs === 'function' && typeof fixRepo === 'function', 'Define fixHrefs(html, base) and fixRepo(repo).');\nT.eq(fixHrefs('<a href=\"/styles.css\">', BASE), '<a href=\"/notestream/styles.css\">', 'A root-absolute href gets the base path spliced in — the Unit 1 fix');\nT.eq(fixHrefs('<img src=\"logo.png\">', BASE), '<img src=\"logo.png\">', 'Relative paths already resolve against the page — leave them untouched');\nT.eq(fixHrefs('<a href=\"/notestream/about.html\">', BASE), '<a href=\"/notestream/about.html\">', 'A path already under the base must not be double-prefixed');\nvar fixed = fixRepo(REPO);\nT.eq(preflight(fixed), [], 'After fixRepo, preflight signs off: hrefs carry the base, .env is gone, the stray Index.html is gone');\nT.expect(fixed['.env'] === undefined, 'The secret file must NOT ship — that key belongs in the Worker env, not the repo');\nT.expect(REPO['.env'] !== undefined, 'fixRepo builds a NEW map — the original REPO object stays untouched');" },
        { text: "`build(src, env)` fills `__API_URL__`, emits `app.<hash>.js`, and rewrites `index.html` to point at it — different env, different hash.",
          test: "var dist = build(fixRepo(REPO), ENV);\nvar bundleName = Object.keys(dist).find(function (k) { return /^app\\.[0-9a-z]+\\.js$/.test(k); });\nT.expect(!!bundleName, 'build must emit a content-hashed bundle named app.<hash>.js — use the given hash() on the FINISHED file text');\nT.expect(dist['app.js'] === undefined, 'The unhashed app.js must not be in dist — shipping both invites the stale copy');\nT.expect(dist[bundleName].indexOf(ENV.API_URL) !== -1, 'The __API_URL__ placeholder is filled at BUILD time, from env');\nT.expect(dist[bundleName].indexOf('__API_URL__') === -1, 'The placeholder itself must be GONE from the built bundle');\nT.expect(dist['index.html'].indexOf(bundleName) !== -1, 'index.html must reference the hashed filename it shipped with');\nT.expect(dist['index.html'].indexOf('app.js\"') === -1, 'The old app.js reference must be rewritten — otherwise every visitor 404s on the bundle');\nvar dist2 = build(fixRepo(REPO), { API_URL: 'https://staging-api.me.workers.dev' });\nvar bundle2 = Object.keys(dist2).find(function (k) { return /^app\\.[0-9a-z]+\\.js$/.test(k); });\nT.expect(bundle2 !== bundleName, 'A different env means different bytes, and different bytes must mean a different name — hash the content AFTER replacing the placeholder');" },
        { text: "`serveStatic(urlPath, dist)` serves under the `/notestream/` base path, with `no-cache` HTML and the year-long `immutable` header on the hashed bundle.",
          test: "var dist = build(fixRepo(REPO), ENV);\nvar bundleName = Object.keys(dist).find(function (k) { return /^app\\.[0-9a-z]+\\.js$/.test(k); });\nvar home = serveStatic('/notestream/', dist);\nT.eq(home.status, 200, \"'/notestream/' must serve — a folder URL resolves to that folder's index.html\");\nT.eq(home.file, 'index.html', \"serveStatic('/notestream/', dist) resolves to index.html\");\nT.eq(home.headers['cache-control'], 'no-cache', 'index.html is the entry point: no-cache, so the next deploy is visible immediately');\nvar asset = serveStatic('/notestream/' + bundleName, dist);\nT.eq(asset.status, 200, 'The hashed bundle must serve under the base path');\nT.eq(asset.file, bundleName, 'And it serves the hashed file itself');\nT.eq(asset.headers['cache-control'], 'public, max-age=31536000, immutable', 'A hashed file can never change under its name — cache it for a year, immutable');\nT.eq(serveStatic('/app.js', dist).status, 404, 'The site lives UNDER /notestream/ — the domain root is not yours, so root requests miss');\nT.eq(serveStatic('/notestream/ghost.js', dist).status, 404, 'Unknown files under the base still 404');" },
        { text: "The Worker answers `/api/health` and `/api/notes`, echoes the **allowed** origin in `access-control-allow-origin` (config via `env.ALLOWED_ORIGIN`), gives strangers no CORS header, and 204s the OPTIONS preflight.",
          test: "var res = await worker.fetch(new Request('https://notestream-api.me.workers.dev/api/health'), {});\nT.eq(res.status, 200, 'GET /api/health answers 200 — the smoke test hits this first');\nT.eq(await res.json(), { ok: true }, '/api/health answers { ok: true } as JSON');\nvar good = await worker.fetch(new Request('https://notestream-api.me.workers.dev/api/notes', { headers: { origin: 'https://me.github.io' } }), {});\nT.eq(good.status, 200, 'GET /api/notes from the Pages origin answers 200');\nT.eq(good.headers.get('access-control-allow-origin'), 'https://me.github.io', 'The allowed origin gets itself echoed in access-control-allow-origin — an allowlist, never *');\nT.eq(await good.json(), NOTES, '/api/notes serves the NOTES fixture as JSON');\nvar bad = await worker.fetch(new Request('https://notestream-api.me.workers.dev/api/notes', { headers: { origin: 'https://evil.example' } }), {});\nT.eq(bad.headers.get('access-control-allow-origin'), null, 'A stranger origin gets NO CORS header — the server stays polite, the BROWSER does the blocking');\nvar pre = await worker.fetch(new Request('https://notestream-api.me.workers.dev/api/notes', { method: 'OPTIONS', headers: { origin: 'https://me.github.io' } }), {});\nT.eq(pre.status, 204, 'The OPTIONS preflight answers 204 No Content');\nT.expect((pre.headers.get('access-control-allow-methods') || '').indexOf('GET') !== -1, 'The preflight names its allowed methods');\nvar moved = await worker.fetch(new Request('https://notestream-api.me.workers.dev/api/notes', { headers: { origin: 'https://preview.me.dev' } }), { ALLOWED_ORIGIN: 'https://preview.me.dev' });\nT.eq(moved.headers.get('access-control-allow-origin'), 'https://preview.me.dev', 'The allowlist reads env.ALLOWED_ORIGIN (default https://me.github.io) — config, not code');\nvar miss = await worker.fetch(new Request('https://notestream-api.me.workers.dev/nope'), {});\nT.eq(miss.status, 404, 'Unknown routes answer 404');\nT.eq(await miss.json(), { error: { code: 'not_found', message: 'no such route' } }, \"The 404 envelope — Course 7's shape, unchanged at the edge\");" },
        { text: "`deploy(state, release)` records every release but only a **healthy** one becomes current; `rollback(state)` re-points at the previous healthy build.",
          test: "T.expect(typeof deploy === 'function' && typeof rollback === 'function', 'Define deploy(state, release) and rollback(state).');\nvar s = deploy({ current: null, history: [] }, { v: 1, healthy: true });\nT.eq(s.current.v, 1, 'A healthy release becomes current');\nT.eq(s.history.length, 1, 'And it is recorded in history');\ns = deploy(s, { v: 2, healthy: true });\nT.eq(s.current.v, 2, 'The next healthy release takes over');\ns = deploy(s, { v: 3, healthy: false });\nT.eq(s.current.v, 2, 'A release that FAILS its smoke test must never become current — this gate is the whole point');\nT.eq(s.history.length, 3, 'The failed release is still recorded — you will want that log tomorrow');\ns = rollback(s);\nT.eq(s.current.v, 1, 'rollback re-points current at the healthy release BEFORE the current one — no rebuild, no waiting on CI');" }
      ],
      files: [
        { name: "script.js", content: "// ===== ship-it.js — one file, one complete deploy =====\n//\n// The pipeline you built across this course, wired end to end:\n//   preflight(REPO)   → reports the repo's 3 problems\n//   fixRepo(REPO)     → preflight signs off\n//   build(src, env)   → content-hashed dist, index.html rewritten\n//   serveStatic(...)  → the dist under /notestream/, right cache headers\n//   worker.fetch      → the API answering the page cross-origin\n//   deploy/rollback   → a failing smoke test never goes live\n//\n// GIVENS are marked — leave them as-is. Everything else is yours.\n\n// ---------- GIVEN: deterministic djb2 hash (Unit 3's helper) ----------\nfunction hash(str) {\n  let h = 5381;\n  for (let i = 0; i < str.length; i++) h = ((h * 33) ^ str.charCodeAt(i)) >>> 0;\n  return h.toString(36);\n}\n\n// ---------- GIVEN: the GitHub Pages base path ----------\nconst BASE = \"/notestream/\";\n\n// ---------- GIVEN: the repo as it sits on your laptop (3 problems) ----------\nconst REPO = {\n  \"index.html\": \"<!doctype html>\\n<html>\\n<head>\\n  <link rel=\\\"stylesheet\\\" href=\\\"/styles.css\\\">\\n</head>\\n<body>\\n  <h1>NoteStream</h1>\\n  <ul id=\\\"notes\\\"></ul>\\n  <script src=\\\"/app.js\\\"></script>\\n</body>\\n</html>\",\n  \"Index.html\": \"<!doctype html><h1>NoteStream (old draft)</h1>\",\n  \"styles.css\": \"body { font-family: system-ui; margin: 2rem; }\",\n  \"app.js\": \"const API_URL = \\\"__API_URL__\\\";\\nfetch(API_URL + \\\"/api/notes\\\");\",\n  \".env\": \"STRIPE_KEY=sk_live_51abc\\n\"\n};\n\n// ---------- GIVEN: what the API serves ----------\nconst NOTES = [\n  { id: 1, text: \"write the notes app\" },\n  { id: 2, text: \"deploy it for real\" },\n  { id: 3, text: \"ship it\" }\n];\n\n// ---------- GIVEN: the build-time config ----------\nconst ENV = { API_URL: \"https://notestream-api.me.workers.dev\" };\n\n// ========================= YOUR PIPELINE =========================\n\n// 1) preflight(repo) → SORTED array of failure codes:\n//    \"missing-index\"        no index.html at the repo root\n//    \"uppercase-index\"      a stray Index.html (Linux is case-sensitive)\n//    \"absolute-asset-path\"  an href/src in an .html file starting \"/\"\n//                           but not with BASE\n//    \"secret-in-repo\"       \"sk_live\" anywhere in any file\nfunction preflight(repo) {\n  // TODO\n}\n\n// 2a) fixHrefs(html, base) → root-absolute href/src get base spliced in;\n//     relative paths and paths already under base stay untouched\nfunction fixHrefs(html, base) {\n  // TODO\n}\n\n// 2b) fixRepo(repo) → a NEW map: hrefs fixed, .env dropped, Index.html dropped\nfunction fixRepo(repo) {\n  // TODO\n}\n\n// 3) build(src, env) → dist:\n//    app.js: __API_URL__ replaced with env.API_URL, then emitted as\n//    app.<hash(content)>.js — the unhashed app.js does NOT ship;\n//    index.html: every \"app.js\" reference rewritten to the hashed name;\n//    everything else copied through\nfunction build(src, env) {\n  // TODO\n}\n\n// 4) headersFor(filename) → { \"cache-control\": ... }\n//      app.<hash>.js   → \"public, max-age=31536000, immutable\"\n//      everything else → \"no-cache\"\n//    serveStatic(urlPath, files) → { status, file, headers } | { status: 404 }\n//      \"/notestream/\" → index.html; outside BASE → 404\nfunction headersFor(filename) {\n  // TODO\n}\nfunction serveStatic(urlPath, files) {\n  // TODO\n}\n\n// 5) deploy(state, release) → NEW state { current, history }:\n//    every release lands in history; only a healthy one becomes current.\n//    rollback(state) → current re-points at the healthy release before it\nfunction deploy(state, release) {\n  // TODO\n}\nfunction rollback(state) {\n  // TODO\n}\n\n// 6) The API — a real Cloudflare Worker, byte-for-byte:\n//    GET /api/health → 200 { ok: true }     GET /api/notes → 200 NOTES\n//    OPTIONS → 204 (+ access-control-allow-methods)\n//    anything else → 404 { error: { code, message } }\n//    CORS: the allowed origin (env.ALLOWED_ORIGIN, default\n//    https://me.github.io) gets access-control-allow-origin echoed back;\n//    strangers get NO CORS header at all\nexport default {\n  async fetch(request, env, ctx) {\n    // TODO\n  }\n};\n" }
      ],
      hints: [
        "preflight: collect codes into an array and `return codes.sort()`. The path check scans `.html` files with `/(?:href|src)=\"([^\"]+)\"/g` — a captured value that starts with `\"/\"` but not with `BASE` is the Pages killer. The secret check is just `repo[k].indexOf(\"sk_live\") !== -1` over every file.",
        "build: fill the placeholder FIRST, then hash the finished text: `const bundled = src[\"app.js\"].split(\"__API_URL__\").join(env.API_URL); const bundleName = \"app.\" + hash(bundled) + \".js\";` — then rewrite the HTML with `src[\"index.html\"].split(\"app.js\").join(bundleName)` and skip copying the raw `app.js`.",
        "The Worker: compute CORS once per request — `const allowed = (env && env.ALLOWED_ORIGIN) || \"https://me.github.io\";` — echo `request.headers.get(\"origin\")` only when it equals `allowed`, and merge that header into EVERY response, the OPTIONS 204 included."
      ],
      solution: {
        "script.js": "// ===== ship-it.js — one file, one complete deploy =====\n\n// ---------- GIVEN: deterministic djb2 hash (Unit 3's helper) ----------\nfunction hash(str) {\n  let h = 5381;\n  for (let i = 0; i < str.length; i++) h = ((h * 33) ^ str.charCodeAt(i)) >>> 0;\n  return h.toString(36);\n}\n\n// ---------- GIVEN: the GitHub Pages base path ----------\nconst BASE = \"/notestream/\";\n\n// ---------- GIVEN: the repo as it sits on your laptop (3 problems) ----------\nconst REPO = {\n  \"index.html\": \"<!doctype html>\\n<html>\\n<head>\\n  <link rel=\\\"stylesheet\\\" href=\\\"/styles.css\\\">\\n</head>\\n<body>\\n  <h1>NoteStream</h1>\\n  <ul id=\\\"notes\\\"></ul>\\n  <script src=\\\"/app.js\\\"></script>\\n</body>\\n</html>\",\n  \"Index.html\": \"<!doctype html><h1>NoteStream (old draft)</h1>\",\n  \"styles.css\": \"body { font-family: system-ui; margin: 2rem; }\",\n  \"app.js\": \"const API_URL = \\\"__API_URL__\\\";\\nfetch(API_URL + \\\"/api/notes\\\");\",\n  \".env\": \"STRIPE_KEY=sk_live_51abc\\n\"\n};\n\n// ---------- GIVEN: what the API serves ----------\nconst NOTES = [\n  { id: 1, text: \"write the notes app\" },\n  { id: 2, text: \"deploy it for real\" },\n  { id: 3, text: \"ship it\" }\n];\n\n// ---------- GIVEN: the build-time config ----------\nconst ENV = { API_URL: \"https://notestream-api.me.workers.dev\" };\n\n// ========================= THE PIPELINE =========================\n\n// 1) The gate: nothing ships until this returns [].\nfunction preflight(repo) {\n  const codes = [];\n  const keys = Object.keys(repo);\n  if (repo[\"index.html\"] === undefined) codes.push(\"missing-index\");\n  if (keys.some(k => k !== \"index.html\" && k.toLowerCase() === \"index.html\")) {\n    codes.push(\"uppercase-index\");\n  }\n  let badPath = false;\n  for (const k of keys) {\n    if (!k.endsWith(\".html\")) continue;\n    const attrRe = /(?:href|src)=\"([^\"]+)\"/g;\n    let m;\n    while ((m = attrRe.exec(repo[k])) !== null) {\n      if (m[1].charAt(0) === \"/\" && m[1].indexOf(BASE) !== 0) badPath = true;\n    }\n  }\n  if (badPath) codes.push(\"absolute-asset-path\");\n  if (keys.some(k => repo[k].indexOf(\"sk_live\") !== -1)) codes.push(\"secret-in-repo\");\n  return codes.sort();\n}\n\n// 2) The fixes: base-path the hrefs, drop the secret and the case-twin.\nfunction fixHrefs(html, base) {\n  return html.replace(/(href|src)=\"(\\/[^\"]*)\"/g, function (m, attr, p) {\n    if (p.indexOf(base) === 0) return m;          // already under the base\n    return attr + '=\"' + base + p.slice(1) + '\"'; // splice the base in\n  });\n}\n\nfunction fixRepo(repo) {\n  const out = {};\n  for (const k of Object.keys(repo)) {\n    if (k !== \"index.html\" && k.toLowerCase() === \"index.html\") continue; // stray case-twin\n    if (k === \".env\") continue; // the key moves to the Worker env, not the repo\n    out[k] = k.endsWith(\".html\") ? fixHrefs(repo[k], BASE) : repo[k];\n  }\n  return out;\n}\n\n// 3) The build: fill the placeholder, hash the RESULT, rewrite the HTML.\nfunction build(src, env) {\n  const bundled = src[\"app.js\"].split(\"__API_URL__\").join(env.API_URL);\n  const bundleName = \"app.\" + hash(bundled) + \".js\";\n  const dist = {};\n  for (const k of Object.keys(src)) {\n    if (k === \"app.js\") continue; // the unhashed copy never ships\n    dist[k] = src[k];\n  }\n  dist[bundleName] = bundled;\n  dist[\"index.html\"] = src[\"index.html\"].split(\"app.js\").join(bundleName);\n  return dist;\n}\n\n// 4) The host: base-path resolution + the cache policy that makes\n//    deploys visible (no-cache HTML, immutable hashed assets).\nfunction headersFor(filename) {\n  if (/\\.[0-9a-z]+\\.js$/.test(filename)) {\n    return { \"cache-control\": \"public, max-age=31536000, immutable\" };\n  }\n  return { \"cache-control\": \"no-cache\" };\n}\n\nfunction serveStatic(urlPath, files) {\n  if (urlPath.indexOf(BASE) !== 0) return { status: 404 }; // the root is not yours\n  let rel = urlPath.slice(BASE.length);\n  if (rel === \"\" || rel.charAt(rel.length - 1) === \"/\") rel += \"index.html\";\n  if (files[rel] === undefined) return { status: 404 };\n  return { status: 200, file: rel, headers: headersFor(rel) };\n}\n\n// 5) The safety net: every release is recorded, only healthy ones go live.\nfunction deploy(state, release) {\n  const history = state.history.concat([release]);\n  return { current: release.healthy ? release : state.current, history: history };\n}\n\nfunction rollback(state) {\n  const ix = state.history.lastIndexOf(state.current);\n  for (let i = ix - 1; i >= 0; i--) {\n    if (state.history[i].healthy) return { current: state.history[i], history: state.history };\n  }\n  return { current: state.current, history: state.history }; // no earlier healthy build — hold the floor (Unit 6's rule)\n}\n\n// 6) The API — a real Cloudflare Worker, byte-for-byte deployable.\nexport default {\n  async fetch(request, env, ctx) {\n    const allowed = (env && env.ALLOWED_ORIGIN) || \"https://me.github.io\";\n    const requestOrigin = request.headers.get(\"origin\");\n    const cors = {};\n    if (requestOrigin === allowed) cors[\"access-control-allow-origin\"] = requestOrigin;\n\n    if (request.method === \"OPTIONS\") {\n      return new Response(null, {\n        status: 204,\n        headers: Object.assign({ \"access-control-allow-methods\": \"GET, OPTIONS\" }, cors)\n      });\n    }\n\n    const url = new URL(request.url);\n    const json = function (body, code) {\n      return new Response(JSON.stringify(body), {\n        status: code,\n        headers: Object.assign({ \"content-type\": \"application/json\" }, cors)\n      });\n    };\n\n    if (url.pathname === \"/api/health\") return json({ ok: true }, 200);\n    if (url.pathname === \"/api/notes\") return json(NOTES, 200);\n    return json({ error: { code: \"not_found\", message: \"no such route\" } }, 404);\n  }\n};\n"
      }
    },

    {
      id: "ship-quiz-pages",
      title: "Field guide quiz: GitHub Pages, for real",
      kind: "quiz", xp: 10,
      brief: "Symptom → cause on the real GitHub Pages settings — no screenshots needed. 80% to pass.",
      questions: [
        { q: "You push a fix to `main` and refresh for ten minutes — nothing changes. Settings → Pages shows 'Deploy from a branch: `gh-pages` /(root)'. Why?",
          choices: ["Pages publishes the `gh-pages` branch — commits to `main` never reach the site", "The Pages CDN caches every page for 24 hours no matter what you deploy", "GitHub batches Pages rebuilds and runs them once per hour on a schedule", "The fix needs a version bump committed to `package.json` before Pages will rebuild the site"],
          answer: 0, explain: "Pages serves exactly one source, picked in Settings → Pages: a branch plus a folder. If that dropdown says `gh-pages`, commits to `main` never reach the published site — either switch the source to `main`, or keep a workflow that builds `main` into `gh-pages` on every push. Nothing rebuilds on an hourly schedule, and the CDN picks up a real deploy within a minute or two." },
        { q: "Your built app lives in `docs/` on `main`, but the live site renders your README instead. Which setting fixes it?",
          choices: ["Rename `docs/` to `public/` — Pages only publishes a folder named `public/`", "Switch the Pages folder dropdown from `/(root)` to `/docs`", "Add a `homepage` field to `package.json` pointing at the `docs/` folder", "Move the README out of the repo so Pages falls back to serving `docs/` instead"],
          answer: 1, explain: "Branch deploys offer exactly two folder choices: `/(root)` and `/docs`. On `/(root)` Pages publishes the repo root — and with no `index.html` there, you get the README rendered as the site. `/docs` exists precisely for committed build output; `public/` means nothing to Pages, and `package.json` is a Node convention it never reads." },
        { q: "Your Actions deploy works, but every push resets your custom domain — `mysite.com` stops working until you re-enter it in Settings. Why?",
          choices: ["GitHub re-verifies domain ownership after every deploy and disables it meanwhile", "Custom domains only work with 'Deploy from a branch' — Actions deploys reset them by design", "Your DNS record's TTL is shorter than the time between your deploys", "The domain lives in a `CNAME` file in the published output — your build overwrites it"],
          answer: 3, explain: "Setting a custom domain in the UI quietly writes a file literally named `CNAME` into the published branch — the setting IS that file. A workflow that regenerates the output from scratch deletes it, so the domain reverts on every deploy. The fix: commit a `CNAME` file (containing just the domain) into whatever your build publishes, so it ships every time." },
        { q: "You rename the repo `notes` → `notestream`. The old URL now 404s and the site's CSS is missing at the new one. What happened?",
          choices: ["Renaming a repository disables GitHub Pages entirely until you re-enable it in the Settings panel and manually trigger a fresh first deploy", "GitHub keeps the old URL alive but wipes the asset cache as part of any rename", "A project site's URL contains the repo name — the site moved to `/notestream/`, and paths baked for `/notes/` now miss", "The rename created a fresh default branch that doesn't contain your files yet"],
          answer: 2, explain: "A project site's URL is `username.github.io/<repo>/` — the repo name IS the base path, so renaming the repo moves the entire site, with no redirect left behind. Any hardwired `/notes/...` path or absolute link now points at nothing. It's Unit 1's base-path trap wearing a new hat: keep asset paths relative, and expect old shared links to die on a rename." },
        { q: "In the Pages source dropdown, what actually changes when you switch 'Deploy from a branch' to 'GitHub Actions'?",
          choices: ["Nothing user-visible — it's the same pipeline wearing a different label", "Who builds: branch mode publishes committed files nearly as-is; Actions mode runs your own build workflow first", "Actions mode is mandatory for public repositories, branch mode for private ones", "Branch mode keeps custom domains and automatic HTTPS certificates; Actions mode trades both away in exchange for control of the build"],
          answer: 1, explain: "'Deploy from a branch' publishes what's already committed (after a legacy Jekyll pass) — your build output must live in the repo. 'GitHub Actions' hands the pipeline to your workflow: build from source, upload the artifact, nothing generated ever committed. Custom domains and HTTPS work identically in both modes." },
        { q: "Brand-new repo, first ever push, Pages enabled — two minutes later the URL still 404s. The healthiest next move?",
          choices: ["Delete the repo and re-create it from scratch — brand-new first deploys hang more often than they succeed", "Force-push an empty commit to kick the build queue back into motion", "Wait it out, watching the `pages-build-deployment` run in the Actions tab", "Open a support ticket — a Pages URL should be live within seconds of the push"],
          answer: 2, explain: "A first deploy provisions the whole site — build, artifact upload, CDN and DNS fan-out — and commonly takes a few minutes, occasionally ten. Every Pages deploy, branch mode included, appears as a run in the Actions tab, so that's your real status page: a green `pages-build-deployment` run plus patience beats any amount of re-pushing. Later deploys are much faster because the site already exists." },
        { q: "Branch deploy: the site works except your `_lib/` folder of JS modules, which 404s in production while every neighboring asset serves fine. The fix?",
          choices: ["Add an empty `.nojekyll` file", "Rename `_lib/` — Pages reserves underscore-prefixed folders for its own internals", "List `_lib/` in a `.pages.yml` manifest so the CDN knows to include it", "Re-push underscore folders after every deploy; they're pruned by default"],
          answer: 0, explain: "Branch-mode Pages still pipes your files through Jekyll by default, and Jekyll treats underscore-prefixed folders as internal — it silently drops `_lib/` from the output. One empty file named `.nojekyll` at the site root switches that pass off and publishes your files verbatim. There's no manifest to maintain, and nothing is 'reserved' about underscores once Jekyll is out of the way." },
        { q: "Repo `me.github.io` serves at your domain root; your friend's repo `blog` serves under `/blog/`. What's the actual rule?",
          choices: ["The first repository an account ever publishes claims the domain root; every later repository is pushed down into a repo-named subpath automatically", "Root hosting is a paid-plan feature; free accounts always get subpaths", "Repos with a custom domain get the root; the rest get subpaths", "Only a repo named exactly `<username>.github.io` is a user site at the root; every other repo is a project site under `/<repo>/`"],
          answer: 3, explain: "Each account gets one special repo — named exactly `<username>.github.io` — that publishes at the root of the subdomain; every other repo is a project site under `/<repo>/`. That's why base paths matter for project sites and silently don't for user sites: code that works at the root can 404 under a subpath. Plans and custom domains change none of this." }
      ]
    },

    {
      id: "ship-quiz-wrangler",
      title: "Field guide quiz: Cloudflare Workers & wrangler, for real",
      kind: "quiz", xp: 10,
      brief: "Symptom → cause on wrangler and the Workers dashboard — the commands you'll actually type. 80% to pass.",
      questions: [
        { q: "You add `GREETING` in the Worker's dashboard Settings and it works — until the next `wrangler deploy`, when it vanishes. What happened?",
          choices: ["Dashboard vars expire after 24 hours unless you mark them persistent", "`wrangler deploy` restarts the Worker, wiping the runtime memory vars live in", "The wrangler config file is the source of truth — deploys overwrite dashboard edits with what the file says", "Vars added in the dashboard apply only to the preview deployment, never to the production Worker your deploys create"],
          answer: 2, explain: "`wrangler deploy` pushes your config file's view of the world — vars included — so hand-edits made in the dashboard are replaced by whatever `wrangler.toml`/`wrangler.jsonc` declares. Pick one home for configuration and make it the file: it's versioned, reviewable, and survives every deploy. Secrets are the deliberate exception — uploaded once with `wrangler secret put`, deploys leave them alone." },
        { q: "You're mid-feature and want to iterate on the Worker without touching production. What's the actual difference between `wrangler dev` and `wrangler deploy`?",
          choices: ["`dev` runs the Worker locally with hot reload; `deploy` bundles and publishes it to the edge", "`dev` deploys to a temporary staging URL on the edge; `deploy` promotes that staging build to production", "`dev` only type-checks the Worker without running it; `deploy` actually executes it", "They differ only in log verbosity — both commands publish your code to Cloudflare"],
          answer: 0, explain: "`wrangler dev` runs the actual Workers runtime (workerd) on your machine at `localhost:8787`, reloading on save and reading local files like `.dev.vars` — nothing leaves your laptop. `wrangler deploy` bundles the Worker and publishes it to the edge. There's no implicit staging step between them; if you want staging, you model it yourself with `[env.staging]`." },
        { q: "Your Worker needs the live Stripe key in production. Which is the right home for it?",
          choices: ["`[vars]` in `wrangler.toml`, alongside the rest of the configuration", "Hardcoded in the Worker source, since the repository is private anyway", "A `# SECRETS` comment block that wrangler parses at deploy time", "`wrangler secret put STRIPE_KEY`"],
          answer: 3, explain: "`[vars]` lives in the config file, and the config file lives in your repo — putting a live key there is Unit 2's secret-in-repo failure with extra steps. `wrangler secret put NAME` prompts for the value, encrypts it, and stores it with the deployed Worker; code reads it as `env.NAME` exactly like a var, but it never touches the codebase. Private repos leak too — forks, laptops, CI logs." },
        { q: "`env.STRIPE_KEY` works in production but comes back `undefined` under `wrangler dev`. Where does the local value belong?",
          choices: ["In `[vars]` — dev merges uploaded secrets into vars automatically after a first deploy", "In a gitignored `.dev.vars` file next to your wrangler config", "In your shell profile, exported before starting the dev server", "In `wrangler secret put --local`, re-run at the start of every dev session"],
          answer: 1, explain: "Secrets uploaded with `wrangler secret put` live encrypted on Cloudflare, and `wrangler dev` runs on your machine — it can't see them, by design. The local counterpart is `.dev.vars`: a `KEY=value` file beside the config that dev loads into `env`, and it belongs in `.gitignore` for the same reason `.env` did in Unit 2. Shell exports aren't read into Worker `env` at all." },
        { q: "`wrangler deploy --env staging` succeeds, but the staging Worker crashes: `env.GREETING` is `undefined`. Top-level `[vars]` clearly defines `GREETING`. What gives?",
          choices: ["`[env.staging]` starts from a blank slate — bindings aren't inherited, so it needs its own `[env.staging.vars]`", "Staging environments can't read vars until the Worker has been opened once in the dashboard to initialize its settings store", "`--env` renames the Worker under the hood, and vars never survive a rename", "`GREETING` collides with a name wrangler reserves inside named environments"],
          answer: 0, explain: "Named environments inherit a few top-level settings (like `compatibility_date`) but never bindings: vars, KV, D1 and friends must be re-declared inside each `[env.*]` block. It's deliberate — staging shouldn't silently reach production's data just because you forgot to override a binding. The duplication is the feature: every environment's bindings are spelled out where a review can see them." },
        { q: "`api.mysite.com` should hit your Worker. Cloudflare offers both routes and custom domains — what's the real difference?",
          choices: ["Routes are the free-plan variant of custom domains, with identical behavior", "Routes are the legacy mechanism; custom domains fully replaced them years ago", "A custom domain creates the DNS record and certificate for you; a route only matches URLs on a zone you already manage", "Custom domains attach per-path so several Workers can share one hostname; routes can only cover an entire hostname at once"],
          answer: 2, explain: "A Workers custom domain means 'this hostname IS this Worker' — Cloudflare creates the DNS record and TLS certificate for you. A route is only a URL pattern (`mysite.com/api/*`) matched against traffic already flowing through a zone you've configured, which is why a route pointing at a hostname with no DNS record simply never fires. Route for a path on an existing site; custom domain for a whole hostname." },
        { q: "`MY_KEY` works under `wrangler dev` (it's in `.dev.vars`), but after `wrangler deploy` the production Worker sees `env.MY_KEY` as `undefined`. Why?",
          choices: ["`.dev.vars` uploads with the deploy bundle, but Cloudflare only decrypts it for accounts on the paid Workers plan", "`.dev.vars` never leaves your machine — production needs the value uploaded with `wrangler secret put`", "Secrets attach on the second deploy; the first deploy always clears them", "Production `env` is read-only at runtime; values must be fetched from KV instead"],
          answer: 1, explain: "`.dev.vars` is a local convenience for `wrangler dev` — gitignored, never bundled, never uploaded. The deployed Worker's `env` is assembled from `[vars]` in the config plus secrets stored on Cloudflare, so a value that exists only in `.dev.vars` is simply absent in production. One secret, two homes: `.dev.vars` locally, `wrangler secret put` for the edge." },
        { q: "Tidying `wrangler.toml`, you change `name = \"notes-api\"` to `name = \"notestream-api\"` and deploy. The dashboard now lists two Workers. What did that deploy do?",
          choices: ["Renamed the Worker in place, carrying its routes, secrets and logs across", "Published a temporary preview alias that expires by itself after seven days", "Forked the Worker into two copies that now split incoming traffic between the old and new names roughly at random", "Created a brand-new Worker under the new name — the old one is still live, still holding the routes and secrets"],
          answer: 3, explain: "`name` is the Worker's identity: wrangler deploys to whatever the file says, and a new name means a brand-new Worker with empty secrets and its own `workers.dev` URL. The old Worker keeps serving its routes until you migrate them and delete it. A real rename is a checklist: deploy under the new name, re-`put` the secrets, move routes and domains, then retire the old Worker." }
      ]
    },

    {
      id: "ship-quiz-final",
      title: "Final quiz: Deploying your app",
      kind: "quiz", xp: 10,
      brief: "The whole course, one last pass — six units, eight symptoms. 80% to pass.",
      questions: [
        { q: "This works on your Windows laptop and 404s on Pages — the committed file is `photos/header.jpg`. Why?",
          code: "<img src=\"photos/Header.jpg\">",
          lang: "html",
          choices: ["Pages recompresses images and lowercases their filenames during deploy", "The server's filesystem is case-sensitive — `Header.jpg` and `header.jpg` are different names there", "JPEGs need an entry in a site manifest before Pages will serve them from subfolders", "The browser cached an old 404 for that path and will keep replaying it until the cache is manually cleared"],
          answer: 1, explain: "Windows and macOS filesystems are case-insensitive, so the wrong-case path finds the file on your laptop anyway; the Linux machines behind Pages match names exactly, the way Unit 1's `serveStatic` did a strict key lookup. `Header.jpg` and `header.jpg` are simply different keys there. The durable habit: lowercase filenames, referenced character-for-character." },
        { q: "Deployed to `me.github.io/notestream/`, the page loads unstyled and the console shows a 404 for `https://me.github.io/styles.css`. The bug?",
          choices: ["GitHub strips `<link>` tags from project sites as a security measure", "styles.css tripped the Pages file-size limit and was skipped during upload", "Stylesheets on project sites need the full absolute URL, domain included, before the CDN will agree to serve them", "`href=\"/styles.css\"` is root-absolute — it ignores the `/notestream/` base path and asks the domain root"],
          answer: 3, explain: "A leading `/` means 'from the domain root', and a project site doesn't own the domain root — it owns `/notestream/`. So `href=\"/styles.css\"` asks for a file one level above your entire site and 404s. Relative paths or base-prefixed ones both land; this is exactly the bug your `fixHrefs()` pass repaired in the project." },
        { q: "Production logs are drowning in debug output even though the dashboard sets `DEBUG` to `false`. Why does this fire?",
          code: "if (env.DEBUG) enableVerboseLogging();",
          lang: "js",
          choices: ["env values arrive as strings, and the string `\"false\"` is truthy", "The dashboard only accepts uppercase `FALSE` and ignored the value", "`DEBUG` is a platform-reserved name that always reports as enabled", "The deploy pipeline cached last week's `DEBUG` value and keeps re-applying it on every request"],
          answer: 0, explain: "Everything that crosses the env boundary arrives as a string — `\"false\"`, `\"0\"`, `\"8080\"` — and every non-empty string is truthy in JavaScript, so `if (env.DEBUG)` fires. That's Unit 2's whole lesson: parse at the config edge (`bool()`, `num()`) once, and pass real types through the rest of the program." },
        { q: "You update `API_URL` in your static host's dashboard and hard-refresh the live site — it still calls the old URL. What's actually missing?",
          choices: ["A cache purge — the CDN pinned the old value and will serve it until every edge node is flushed", "A `VITE_` prefix — dashboard variables are invisible to bundles without one", "A rebuild — the URL was baked in at build time, and an env change can't reach bytes that already exist", "Nothing is missing — dashboard variables simply take up to 24 hours to propagate through the CDN to the running site"],
          answer: 2, explain: "The URL was substituted into the bundle when `build()` ran — the artifact is frozen bytes with a content-hashed name, and nothing on a static host re-runs your build at request time. The dashboard variable feeds the NEXT build, not the current files. Two clocks: env read at build time is baked; only a rebuild-and-redeploy moves it." },
        { q: "You ship v2; a day later half your users still see v1. Every file, `index.html` included, ships with `cache-control: public, max-age=31536000, immutable`. Which header change makes deploys visible again?",
          choices: ["`no-store` on every file, so no browser ever caches anything again", "`no-cache` on `index.html`", "`max-age=3600` everywhere, as a compromise between speed and freshness", "Keep the year-long cache but append a `?v=` query to every asset URL on each deploy"],
          answer: 1, explain: "The entry point has to be revalidated on every visit: `no-cache` makes browsers check for a fresh `index.html`, which then references the new hashed bundles by name. The hashed files themselves stay safe to cache for a year precisely because new bytes always arrive under a new name. A year-long-cached `index.html` is the deploy nobody can see — users keep the old map to your old files." },
        { q: "Your Pages site calls your Worker API and the console reports the request was blocked by CORS policy. Where does the fix belong?",
          choices: ["In the Worker — it must send `access-control-allow-origin` for your Pages origin", "In the fetch call — `mode: \"no-cors\"` tells the browser to skip the check", "In the browser — CORS is a per-user security setting your docs should tell visitors to disable", "In DNS — hosting the API on the same IP address makes the two origins match"],
          answer: 0, explain: "CORS is granted by the server: the browser blocks a cross-origin response unless the response carries `access-control-allow-origin` naming the caller's origin. `mode: \"no-cors\"` doesn't fix anything — it hands you an opaque response you can't read. And origins are scheme + host + port, so `me.github.io` and your `workers.dev` hostname stay different forever, whatever the DNS says." },
        { q: "Ten minutes after a deploy, checkout is broken in production. The fastest correct move?",
          choices: ["Debug it live — fixing forward beats moving backwards once users are affected", "`git revert` the bad commit, then wait for CI to rebuild and ship the corrected version", "Roll back: re-point production at the previous known-good build", "Disable the CDN so users reach the origin servers and bypass the broken cached copy"],
          answer: 2, explain: "The previous build is a complete, already-smoke-tested artifact sitting in history — re-pointing production at it is seconds of low-risk recovery, which is exactly why `deploy()` keeps every release. Fix-forward and revert-through-CI both leave production broken for as long as a pipeline run takes, with a stressed human typing. Roll back first, diagnose calmly second." },
        { q: "Three weeks ago someone committed an `sk_live` key. Today you deleted the file and gitignored `.env`. Is the incident closed?",
          choices: ["Yes — the file is gone from the branch, so the key is no longer reachable", "No — but running `git gc` scrubs the unreachable blobs and closes it out", "Yes, provided the repository has stayed private for the whole three weeks and no forks exist", "No — the key survives in git history, so it must be rotated at the provider"],
          answer: 3, explain: "Deleting a file changes the newest commit, not the past — anyone who can clone the repo can check out the three-week-old commit and read the key. Once a secret has ever been committed, treat it as burned: rotate it at the provider, update the deployed env, and only then worry about scrubbing history. Private repos still leak via forks, laptops, CI logs and future open-sourcing, and `git gc` doesn't rewrite pushed history." }
      ]
    }
  ]
});
