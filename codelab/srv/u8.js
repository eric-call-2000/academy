/* Back-End Foundations — Unit 8: Capstone: Blog API with auth */
window.CODELAB.addUnit("srv", {
  id: "srv-u8",
  title: "Capstone: Blog API with auth",
  icon: "📰",
  blurb: "The graduation build: a blog API with tokens, drafts, publishing, filters and pagination — every unit in one handler.",
  cheat: [
    { h: "The capstone request, fully grown", lang: "js", code: "{ method: \"POST\", path: \"/api/posts\",\n  query: {},                                // ?a=b, already parsed (Unit 3)\n  headers: { authorization: \"token-ada\" },  // who is asking (Unit 5)\n  body: { title: \"Hi!\", postBody: \"…\" } }   // what they sent", note: "Four units of request anatomy in one object. The response is still just { status, body }." },
    { h: "The error envelope (every failure, one shape)", lang: "js", code: "return { status: 404, body: { error: { code: \"not_found\",     message: \"Not found\" } } };\nreturn { status: 401, body: { error: { code: \"unauthorized\", message: \"Token required\" } } };\nreturn { status: 400, body: { error: { code: \"invalid\",       message: \"postBody is required\" } } };", note: "Clients write ONE error handler. Consistency is a feature." },
    { h: "Auth guard in three lines", lang: "js", code: "const userName = req.headers && TOKENS[req.headers.authorization];\nif (!userName) return fail(401, \"unauthorized\", \"Token required\");\n// userName is now VERIFIED — stamp it on everything you create", note: "Identity comes from the token, never from the body." },
    { h: "Publish workflow = PATCH a flag", lang: "js", code: "// PATCH /api/posts/7   body: { published: true }\nif (\"published\" in req.body) post.published = req.body.published;", note: "Merge only the keys the client sent — that is PATCH's whole personality (Unit 2)." },
    { h: "Pagination meta", lang: "js", code: "const total = visible.length;\nconst pages = Math.ceil(total / perPage);\nconst items = visible.slice((pageNum - 1) * perPage, pageNum * perPage);\nreturn { status: 200, body: { items, total, page: pageNum, pages } };", note: "7 items, perPage 3 → pages 3 + 3 + 1. slice just returns whatever is left." },
    { h: "How this looks in Express (real Node.js)", lang: "js", code: "app.post(\"/api/posts\", requireAuth, (req, res) => {\n  const post = createDraft(req.user, req.body);\n  res.status(201).json(post);\n});", note: "requireAuth is middleware (Unit 4). Everything you built here transfers 1:1." }
  ],
  lessons: [

    {
      id: "srv-u8-1",
      title: "Posts & authors",
      kind: "js", chip: "SERVER", xp: 15, mins: 14,
      brief: "Welcome to the capstone. Eight units ago a server was one humble function — now you will build the backend behind a real blog with that very same function: `handleRequest(req) → res`.\n\nFirst, the read side. Posts carry `authorName`, `title`, `postBody` and a `published` flag. Two public routes:\n\n- `GET /api/posts` → **only published** posts — drafts stay private\n- `GET /api/posts/<id>` → that post, drafts included (authors preview by direct link)\n\nUnknown ids and unknown routes both answer the Unit 6 **error envelope**, via the prewritten `notFound()` helper. One failure shape, everywhere — your future clients thank you.",
      steps: [
        { text: "`GET /api/posts` → 200 with ONLY the published posts (the draft must not leak).",
          test: "var res = handleRequest({ method: 'GET', path: '/api/posts', body: null });\nT.expect(res && typeof res === 'object', 'handleRequest should return a response object.');\nT.eq(res, { status: 200, body: [ { id: 1, authorName: 'ada', title: 'Hello, backend', postBody: 'Servers are just functions.', published: true }, { id: 3, authorName: 'bo', title: 'Status codes speak', postBody: '2xx success, 4xx client, 5xx server.', published: true } ] }, 'GET /api/posts should list ONLY published posts (ids 1 and 3, in store order) — the draft with id 2 must stay hidden');" },
        { text: "`GET /api/posts/<id>` → 200 with that exact post — drafts included (direct-link preview).",
          test: "var res = handleRequest({ method: 'GET', path: '/api/posts/1', body: null });\nT.eq(res, { status: 200, body: { id: 1, authorName: 'ada', title: 'Hello, backend', postBody: 'Servers are just functions.', published: true } }, 'GET /api/posts/1 should answer 200 with post 1 — yours returned status ' + (res && res.status));\nvar draft = handleRequest({ method: 'GET', path: '/api/posts/2', body: null });\nT.eq(draft, { status: 200, body: { id: 2, authorName: 'bo', title: 'Middleware ideas', postBody: 'Wrap functions in functions.', published: false } }, 'GET /api/posts/2 — the draft IS reachable by direct link (preview mode)');" },
        { text: "Unknown post ids AND unknown routes → the 404 envelope from `notFound()`.",
          test: "var res = handleRequest({ method: 'GET', path: '/api/posts/99', body: null });\nT.eq(res, { status: 404, body: { error: { code: 'not_found', message: 'Not found' } } }, 'GET /api/posts/99 should answer the 404 envelope — yours returned ' + JSON.stringify(res));\nT.eq(handleRequest({ method: 'GET', path: '/api/tacos', body: null }), { status: 404, body: { error: { code: 'not_found', message: 'Not found' } } }, 'Unknown routes get the SAME envelope — one failure shape everywhere');" }
      ],
      files: [
        { name: "script.js", content: "// Capstone data model: a blog post\n//   { id, authorName, title, postBody, published }\n// Error envelope (Unit 6): { status, body: { error: { code, message } } }\n\nlet posts = [\n  { id: 1, authorName: \"ada\", title: \"Hello, backend\", postBody: \"Servers are just functions.\", published: true },\n  { id: 2, authorName: \"bo\",  title: \"Middleware ideas\", postBody: \"Wrap functions in functions.\", published: false },\n  { id: 3, authorName: \"bo\",  title: \"Status codes speak\", postBody: \"2xx success, 4xx client, 5xx server.\", published: true }\n];\n\nfunction notFound() {\n  return { status: 404, body: { error: { code: \"not_found\", message: \"Not found\" } } };\n}\n\nfunction handleRequest(req) {\n  // 1) GET /api/posts      → 200, ONLY posts with published: true\n  // 2) GET /api/posts/<id> → 200, that post (drafts included — preview by link)\n  //    hint: const id = Number(req.path.split(\"/\")[3]);\n  // 3) anything else       → notFound()\n}\n\nconsole.log(handleRequest({ method: \"GET\", path: \"/api/posts\", body: null }));\n" }
      ],
      hints: [
        "Published-only is a filter: `return { status: 200, body: posts.filter(p => p.published) };` — the flag is already a boolean.",
        "Detail route: `if (req.method === \"GET\" && req.path.startsWith(\"/api/posts/\")) { const id = Number(req.path.split(\"/\")[3]); const post = posts.find(p => p.id === id); … }` — when find comes back empty, `return notFound();`",
        "The catch-all `return notFound();` is the LAST line of handleRequest — every request gets an answer."
      ],
      solution: {
        "script.js": "// Capstone data model: a blog post\n//   { id, authorName, title, postBody, published }\n// Error envelope (Unit 6): { status, body: { error: { code, message } } }\n\nlet posts = [\n  { id: 1, authorName: \"ada\", title: \"Hello, backend\", postBody: \"Servers are just functions.\", published: true },\n  { id: 2, authorName: \"bo\",  title: \"Middleware ideas\", postBody: \"Wrap functions in functions.\", published: false },\n  { id: 3, authorName: \"bo\",  title: \"Status codes speak\", postBody: \"2xx success, 4xx client, 5xx server.\", published: true }\n];\n\nfunction notFound() {\n  return { status: 404, body: { error: { code: \"not_found\", message: \"Not found\" } } };\n}\n\nfunction handleRequest(req) {\n  if (req.method === \"GET\" && req.path === \"/api/posts\") {\n    return { status: 200, body: posts.filter(p => p.published) };\n  }\n  if (req.method === \"GET\" && req.path.startsWith(\"/api/posts/\")) {\n    const id = Number(req.path.split(\"/\")[3]);\n    const post = posts.find(p => p.id === id);\n    if (!post) return notFound();\n    return { status: 200, body: post };\n  }\n  return notFound();\n}\n\nconsole.log(handleRequest({ method: \"GET\", path: \"/api/posts\", body: null }));\n"
      }
    },

    {
      id: "srv-u8-2",
      title: "Lock the writes",
      kind: "js", chip: "SERVER", xp: 15, mins: 14,
      brief: "Anyone can read the blog — writing is members-only. Reuse the Unit 5 token pattern: a fixed `TOKENS` map (`\"token-ada\"` → `\"ada\"`) stands in for the session store.\n\nThe rules:\n\n- `POST /api/posts` and `DELETE /api/posts/<id>` demand a valid `req.headers.authorization` — anything else gets the **401 envelope**\n- new posts are born as drafts: `published: false`\n- `authorName` is stamped **from the token**, never from the body — a request claiming to be `bo` while holding ada's token is lying, and the server decides\n\nThat last rule is the entire security mindset in one line: trust what you verified, not what you were sent.",
      steps: [
        { text: "Write `authUser(req)` — the userName behind `req.headers.authorization`, or `null` (missing headers object, missing header, unknown token).",
          test: "T.expect(typeof authUser === 'function', 'Define authUser(req).');\nT.eq(authUser({ method: 'GET', path: '/', headers: { authorization: 'token-ada' } }), 'ada', 'token-ada should map to ada');\nT.eq(authUser({ method: 'GET', path: '/', headers: { authorization: 'token-bo' } }), 'bo', 'token-bo should map to bo');\nT.eq(authUser({ method: 'GET', path: '/', headers: { authorization: 'token-zz' } }), null, 'Unknown tokens are null, not undefined');\nT.eq(authUser({ method: 'GET', path: '/', headers: {} }), null, 'No authorization header means null');\nT.eq(authUser({ method: 'GET', path: '/' }), null, 'No headers object AT ALL must not crash — check req.headers first');" },
        { text: "`POST /api/posts` without a valid token → the 401 envelope, and NOTHING gets created.",
          test: "var res = handleRequest({ method: 'POST', path: '/api/posts', headers: {}, body: { title: 'Sneaky', postBody: 'no token, no post' } });\nT.eq(res, { status: 401, body: { error: { code: 'unauthorized', message: 'Token required' } } }, 'POST with no token must answer the 401 envelope — yours returned status ' + (res && res.status));\nvar res2 = handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-fake' }, body: { title: 'Sneaky', postBody: 'bad token' } });\nT.eq(res2.status, 401, 'An UNKNOWN token is just as rejected as no token');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts/1', headers: {}, body: null }).status, 404, 'The rejected posts must NOT exist — GET /api/posts/1 should still 404');" },
        { text: "With a valid token, `POST /api/posts` → **201** and the new draft — `authorName` stamped from the TOKEN, ids from the counter.",
          test: "var res = handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-ada' }, body: { title: 'Hello', postBody: 'First post!', authorName: 'bo' } });\nT.eq(res, { status: 201, body: { id: 1, authorName: 'ada', title: 'Hello', postBody: 'First post!', published: false } }, 'The body CLAIMED authorName bo — but ada holds the token, so ada is the author. Never trust the client.');\nvar res2 = handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-bo' }, body: { title: 'Second', postBody: 'Mine.' } });\nT.eq(res2, { status: 201, body: { id: 2, authorName: 'bo', title: 'Second', postBody: 'Mine.', published: false } }, 'Second create: id 2, author bo, still a draft');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts', headers: {}, body: null }).body, [], 'Both posts are drafts, so the public list is STILL empty');" },
        { text: "`DELETE /api/posts/<id>` — token required (401 envelope), 404 envelope for missing ids, `{ ok: true }` on success.",
          test: "T.eq(handleRequest({ method: 'DELETE', path: '/api/posts/1', headers: {}, body: null }).status, 401, 'DELETE with no token → 401');\nvar del = handleRequest({ method: 'DELETE', path: '/api/posts/1', headers: { authorization: 'token-bo' }, body: null });\nT.eq(del, { status: 200, body: { ok: true } }, 'DELETE /api/posts/1 with a valid token should answer 200 { ok: true }');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts/1', headers: {}, body: null }).status, 404, 'Post 1 is gone — its detail route now 404s');\nT.eq(handleRequest({ method: 'DELETE', path: '/api/posts/99', headers: { authorization: 'token-ada' }, body: null }), { status: 404, body: { error: { code: 'not_found', message: 'Not found' } } }, 'Deleting a missing id gets the 404 envelope');" }
      ],
      files: [
        { name: "script.js", content: "// Reads are public — writes need a token.\n// TOKENS maps a bearer token → the userName it belongs to.\n\nconst TOKENS = { \"token-ada\": \"ada\", \"token-bo\": \"bo\" };\n\nlet posts = [];\nlet nextId = 1;\n\nfunction notFound() {\n  return { status: 404, body: { error: { code: \"not_found\", message: \"Not found\" } } };\n}\nfunction unauthorized() {\n  return { status: 401, body: { error: { code: \"unauthorized\", message: \"Token required\" } } };\n}\n\n// 1) authUser(req) → the userName for req.headers.authorization, or null\n//    (no headers object? no header? unknown token? → null)\n\nfunction handleRequest(req) {\n  // ---- public reads (done — from last lesson) ----\n  if (req.method === \"GET\" && req.path === \"/api/posts\") {\n    return { status: 200, body: posts.filter(p => p.published) };\n  }\n  if (req.method === \"GET\" && req.path.startsWith(\"/api/posts/\")) {\n    const id = Number(req.path.split(\"/\")[3]);\n    const post = posts.find(p => p.id === id);\n    return post ? { status: 200, body: post } : notFound();\n  }\n\n  // ---- protected writes (yours) ----\n  // 2) POST /api/posts → unauthorized() without a valid token; else 201 with\n  //    { id: nextId++, authorName: <from the token>, title, postBody, published: false }\n  // 3) DELETE /api/posts/<id> → unauthorized() without a valid token;\n  //    notFound() for missing ids; else remove it → { status: 200, body: { ok: true } }\n\n  return notFound();\n}\n\nconsole.log(handleRequest({ method: \"GET\", path: \"/api/posts\", headers: {}, body: null }));\n" }
      ],
      hints: [
        "authUser is a lookup with guard rails: `if (!req.headers || !req.headers.authorization) return null; return TOKENS[req.headers.authorization] || null;`",
        "In the POST branch, check auth FIRST: `const userName = authUser(req); if (!userName) return unauthorized();` — then build `{ id: nextId++, authorName: userName, title: req.body.title, postBody: req.body.postBody, published: false }`, push it, answer 201.",
        "DELETE: token check, then `const id = Number(req.path.split(\"/\")[3]);` — use `posts.some(p => p.id === id)` to decide 404 vs success, and reassign with `posts = posts.filter(p => p.id !== id);` (that is why the store is `let`)."
      ],
      solution: {
        "script.js": "// Reads are public — writes need a token.\n// TOKENS maps a bearer token → the userName it belongs to.\n\nconst TOKENS = { \"token-ada\": \"ada\", \"token-bo\": \"bo\" };\n\nlet posts = [];\nlet nextId = 1;\n\nfunction notFound() {\n  return { status: 404, body: { error: { code: \"not_found\", message: \"Not found\" } } };\n}\nfunction unauthorized() {\n  return { status: 401, body: { error: { code: \"unauthorized\", message: \"Token required\" } } };\n}\n\nfunction authUser(req) {\n  if (!req.headers || !req.headers.authorization) return null;\n  return TOKENS[req.headers.authorization] || null;\n}\n\nfunction handleRequest(req) {\n  // ---- public reads ----\n  if (req.method === \"GET\" && req.path === \"/api/posts\") {\n    return { status: 200, body: posts.filter(p => p.published) };\n  }\n  if (req.method === \"GET\" && req.path.startsWith(\"/api/posts/\")) {\n    const id = Number(req.path.split(\"/\")[3]);\n    const post = posts.find(p => p.id === id);\n    return post ? { status: 200, body: post } : notFound();\n  }\n\n  // ---- protected writes ----\n  if (req.method === \"POST\" && req.path === \"/api/posts\") {\n    const userName = authUser(req);\n    if (!userName) return unauthorized();\n    const post = { id: nextId++, authorName: userName, title: req.body.title, postBody: req.body.postBody, published: false };\n    posts.push(post);\n    return { status: 201, body: post };\n  }\n  if (req.method === \"DELETE\" && req.path.startsWith(\"/api/posts/\")) {\n    const userName = authUser(req);\n    if (!userName) return unauthorized();\n    const id = Number(req.path.split(\"/\")[3]);\n    if (!posts.some(p => p.id === id)) return notFound();\n    posts = posts.filter(p => p.id !== id);\n    return { status: 200, body: { ok: true } };\n  }\n\n  return notFound();\n}\n\nconsole.log(handleRequest({ method: \"GET\", path: \"/api/posts\", headers: {}, body: null }));\n"
      }
    },

    {
      id: "srv-u8-p",
      title: "Project: Blog API",
      kind: "js", chip: "SERVER", xp: 60, mins: 45, project: true,
      brief: "The full editorial session — every unit in one handler.\n\n- `POST /api/login` → `{ token: \"token-<userName>\" }`; wrong creds → 401 `bad_credentials`\n- `GET /api/posts` → published only; `?authorName=` filters; `?page=&perPage=` answers `{ items, total, page, pages }` (paginate AFTER the filter — and query values arrive as **strings**)\n- `GET /api/posts/<id>` → any post, else the 404 envelope\n- `POST /api/posts` *(auth)* → 400 `invalid` unless title is ≥ 3 chars and postBody is present; creates a draft\n- `PATCH /api/posts/<id>` *(auth)* → merges `title` / `postBody` / `published`\n\nEnvelope on every failure. Ship this and you have built a genuine backend — Express just adds the socket.",
      steps: [
        { text: "**Log in.** `POST /api/login` checks `USERS`: success → 200 `{ token: \"token-<userName>\" }`, wrong creds → 401 `bad_credentials` envelope.",
          test: "var res = handleRequest({ method: 'POST', path: '/api/login', body: { userName: 'ada', password: 'mainframe' } });\nT.eq(res, { status: 200, body: { token: 'token-ada' } }, 'ada + mainframe should log in and receive token-ada — yours returned ' + JSON.stringify(res));\nT.eq(handleRequest({ method: 'POST', path: '/api/login', body: { userName: 'bo', password: 'hunter2' } }), { status: 200, body: { token: 'token-bo' } }, 'bo logs in too');\nT.eq(handleRequest({ method: 'POST', path: '/api/login', body: { userName: 'ada', password: 'wrong' } }), { status: 401, body: { error: { code: 'bad_credentials', message: 'Wrong userName or password' } } }, 'A wrong password gets the bad_credentials envelope');" },
        { text: "**The gate.** Writes without a token → 401 envelope. Bad drafts → 400 `invalid`: short title → `\"title must be at least 3 characters\"`, missing postBody → `\"postBody is required\"`.",
          test: "T.eq(handleRequest({ method: 'POST', path: '/api/posts', headers: {}, body: { title: 'Fine title', postBody: 'text' } }), { status: 401, body: { error: { code: 'unauthorized', message: 'Token required' } } }, 'No token, no post');\nT.eq(handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-ada' }, body: { title: 'Hi', postBody: 'text' } }), { status: 400, body: { error: { code: 'invalid', message: 'title must be at least 3 characters' } } }, 'A 2-character title fails validation with the exact message');\nT.eq(handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-ada' }, body: { title: 'Fine title' } }), { status: 400, body: { error: { code: 'invalid', message: 'postBody is required' } } }, 'A missing postBody fails validation with the exact message');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts', body: null }).body, [], 'None of those rejected requests created anything');" },
        { text: "**Draft three posts.** 201s with counter ids, `authorName` from the token, `published: false` — the public list stays empty.",
          test: "var p1 = handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-ada' }, body: { title: 'Servers are functions', postBody: 'Request in, response out.' } });\nT.eq(p1, { status: 201, body: { id: 1, authorName: 'ada', title: 'Servers are functions', postBody: 'Request in, response out.', published: false } }, 'First draft: id 1, author ada, unpublished');\nvar p2 = handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-ada' }, body: { title: 'Middleware onions', postBody: 'Layers all the way down.' } });\nT.eq(p2.body.id, 2, 'Counter ids: the second draft is id 2');\nvar p3 = handleRequest({ method: 'POST', path: '/api/posts', headers: { authorization: 'token-bo' }, body: { title: 'Status code poetry', postBody: 'Every number tells a story.' } });\nT.eq(p3.body, { id: 3, authorName: 'bo', title: 'Status code poetry', postBody: 'Every number tells a story.', published: false }, 'Third draft belongs to bo — the token decides authorship');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts', body: null }).body, [], 'Three drafts, zero published — the public list is still empty');" },
        { text: "**Publish (and edit) via PATCH.** Merge only the keys sent; token required; missing ids → 404 envelope.",
          test: "T.eq(handleRequest({ method: 'PATCH', path: '/api/posts/1', headers: {}, body: { published: true } }).status, 401, 'PATCH without a token → 401');\nvar pub = handleRequest({ method: 'PATCH', path: '/api/posts/1', headers: { authorization: 'token-ada' }, body: { published: true } });\nT.eq(pub, { status: 200, body: { id: 1, authorName: 'ada', title: 'Servers are functions', postBody: 'Request in, response out.', published: true } }, 'PATCH merges { published: true } and answers 200 with the whole updated post');\nhandleRequest({ method: 'PATCH', path: '/api/posts/2', headers: { authorization: 'token-ada' }, body: { published: true, title: 'Middleware onions, peeled' } });\nhandleRequest({ method: 'PATCH', path: '/api/posts/3', headers: { authorization: 'token-bo' }, body: { published: true } });\nT.eq(handleRequest({ method: 'PATCH', path: '/api/posts/99', headers: { authorization: 'token-ada' }, body: { published: true } }), { status: 404, body: { error: { code: 'not_found', message: 'Not found' } } }, 'PATCH on a missing id → 404 envelope');\nvar live = handleRequest({ method: 'GET', path: '/api/posts', body: null });\nT.eq(live.body.length, 3, 'All three posts are now live');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts/2', body: null }).body.title, 'Middleware onions, peeled', 'The title edit merged in alongside the publish');" },
        { text: "**Filter by author.** `?authorName=` narrows the published list; unknown authors get an empty 200.",
          test: "var byAda = handleRequest({ method: 'GET', path: '/api/posts', query: { authorName: 'ada' }, body: null });\nT.eq(byAda, { status: 200, body: [ { id: 1, authorName: 'ada', title: 'Servers are functions', postBody: 'Request in, response out.', published: true }, { id: 2, authorName: 'ada', title: 'Middleware onions, peeled', postBody: 'Layers all the way down.', published: true } ] }, '?authorName=ada → both ada posts, store order');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts', query: { authorName: 'bo' }, body: null }).body.length, 1, '?authorName=bo → just one post');\nT.eq(handleRequest({ method: 'GET', path: '/api/posts', query: { authorName: 'zed' }, body: null }), { status: 200, body: [] }, 'Unknown author: empty list, still a 200 — no results is not an error');" },
        { text: "**Paginate the front page.** `?page=&perPage=` → `{ items, total, page, pages }` — recomputed live when a post is unpublished.",
          test: "var pg1 = handleRequest({ method: 'GET', path: '/api/posts', query: { page: '1', perPage: '2' }, body: null });\nT.eq(pg1, { status: 200, body: { items: [ { id: 1, authorName: 'ada', title: 'Servers are functions', postBody: 'Request in, response out.', published: true }, { id: 2, authorName: 'ada', title: 'Middleware onions, peeled', postBody: 'Layers all the way down.', published: true } ], total: 3, page: 1, pages: 2 } }, 'Page 1 of 2: the first two posts, total 3 — remember query values are STRINGS, Number them');\nvar pg2 = handleRequest({ method: 'GET', path: '/api/posts', query: { page: '2', perPage: '2' }, body: null });\nT.eq(pg2.body, { items: [ { id: 3, authorName: 'bo', title: 'Status code poetry', postBody: 'Every number tells a story.', published: true } ], total: 3, page: 2, pages: 2 }, 'Page 2 holds the leftover post');\nhandleRequest({ method: 'PATCH', path: '/api/posts/2', headers: { authorization: 'token-bo' }, body: { published: false } });\nvar after = handleRequest({ method: 'GET', path: '/api/posts', query: { page: '1', perPage: '2' }, body: null });\nT.eq(after.body, { items: [ { id: 1, authorName: 'ada', title: 'Servers are functions', postBody: 'Request in, response out.', published: true }, { id: 3, authorName: 'bo', title: 'Status code poetry', postBody: 'Every number tells a story.', published: true } ], total: 2, page: 1, pages: 1 }, 'Unpublish post 2 and the math recomputes: total 2, one page');" }
      ],
      files: [
        { name: "script.js", content: "// ===== Capstone: Blog API =====\n// request  = { method, path, query?, headers?, body? }\n// response = { status, body }\n// Every failure: { status, body: { error: { code, message } } } — use fail() below.\n//\n// ROUTES\n//   POST  /api/login             { userName, password } → 200 { token: \"token-<userName>\" }\n//                                wrong creds → 401 fail(401, \"bad_credentials\", \"Wrong userName or password\")\n//   GET   /api/posts             → 200, published posts only (public)\n//         ?authorName=X          → filter by author (before pagination)\n//         ?page=N&perPage=M      → { items, total, page, pages } — pages = Math.ceil(total / perPage)\n//   GET   /api/posts/<id>        → 200 that post (drafts included) | 404 envelope\n//   POST  /api/posts   (auth)    → 201 draft { id, authorName: <token user>, title, postBody, published: false }\n//                                title < 3 chars → 400 \"title must be at least 3 characters\"\n//                                no postBody     → 400 \"postBody is required\"\n//   PATCH /api/posts/<id> (auth) → merge title / postBody / published → 200 | 404 envelope\n//   anything else                → 404 envelope\n\nconst USERS = [\n  { userName: \"ada\", password: \"mainframe\" },\n  { userName: \"bo\",  password: \"hunter2\" }\n];\nconst TOKENS = { \"token-ada\": \"ada\", \"token-bo\": \"bo\" };\n\nlet posts = [];\nlet nextId = 1;\n\nfunction fail(statusCode, errCode, errMsg) {\n  return { status: statusCode, body: { error: { code: errCode, message: errMsg } } };\n}\n\nfunction handleRequest(req) {\n  // Suggested build order:\n  // 1) POST /api/login\n  // 2) GET /api/posts  (+ authorName filter, + pagination when req.query.page is present)\n  // 3) GET /api/posts/<id>\n  // 4) the auth guard for writes: req.headers.authorization → TOKENS\n  // 5) POST /api/posts with validation\n  // 6) PATCH /api/posts/<id> merge\n}\n" }
      ],
      hints: [
        "Login: `const found = USERS.find(u => req.body && u.userName === req.body.userName && u.password === req.body.password);` — found ? `{ status: 200, body: { token: 'token-' + found.userName } }` : `fail(401, 'bad_credentials', 'Wrong userName or password')`.",
        "Start GET /api/posts with `const qry = req.query || {};` so queryless requests never crash. Then `let visible = posts.filter(p => p.published);` and `if (qry.authorName) visible = visible.filter(p => p.authorName === qry.authorName);`",
        "Pagination only when asked: `if (qry.page) { const pageNum = Number(qry.page); const perPage = Number(qry.perPage); … }` — `items: visible.slice((pageNum - 1) * perPage, pageNum * perPage)`, `pages: Math.ceil(total / perPage)`.",
        "Compute auth ONCE before the write branches: `const userName = req.headers && req.headers.authorization ? TOKENS[req.headers.authorization] : null;` — then each write starts `if (!userName) return fail(401, 'unauthorized', 'Token required');`",
        "Validate in order with the EXACT messages: title first (`typeof b.title !== 'string' || b.title.trim().length < 3`), then postBody. PATCH merges with `if ('title' in b) post.title = b.title;` and friends."
      ],
      solution: {
        "script.js": "// ===== Capstone: Blog API =====\n\nconst USERS = [\n  { userName: \"ada\", password: \"mainframe\" },\n  { userName: \"bo\",  password: \"hunter2\" }\n];\nconst TOKENS = { \"token-ada\": \"ada\", \"token-bo\": \"bo\" };\n\nlet posts = [];\nlet nextId = 1;\n\nfunction fail(statusCode, errCode, errMsg) {\n  return { status: statusCode, body: { error: { code: errCode, message: errMsg } } };\n}\n\nfunction handleRequest(req) {\n  const qry = req.query || {};\n\n  if (req.method === \"POST\" && req.path === \"/api/login\") {\n    const found = USERS.find(u => req.body && u.userName === req.body.userName && u.password === req.body.password);\n    if (!found) return fail(401, \"bad_credentials\", \"Wrong userName or password\");\n    return { status: 200, body: { token: \"token-\" + found.userName } };\n  }\n\n  if (req.method === \"GET\" && req.path === \"/api/posts\") {\n    let visible = posts.filter(p => p.published);\n    if (qry.authorName) visible = visible.filter(p => p.authorName === qry.authorName);\n    if (qry.page) {\n      const pageNum = Number(qry.page);\n      const perPage = Number(qry.perPage);\n      const total = visible.length;\n      const pages = Math.ceil(total / perPage);\n      const items = visible.slice((pageNum - 1) * perPage, pageNum * perPage);\n      return { status: 200, body: { items: items, total: total, page: pageNum, pages: pages } };\n    }\n    return { status: 200, body: visible };\n  }\n\n  if (req.method === \"GET\" && req.path.startsWith(\"/api/posts/\")) {\n    const id = Number(req.path.split(\"/\")[3]);\n    const post = posts.find(p => p.id === id);\n    if (!post) return fail(404, \"not_found\", \"Not found\");\n    return { status: 200, body: post };\n  }\n\n  // everything below changes data → verified identity required\n  const userName = req.headers && req.headers.authorization ? TOKENS[req.headers.authorization] : null;\n\n  if (req.method === \"POST\" && req.path === \"/api/posts\") {\n    if (!userName) return fail(401, \"unauthorized\", \"Token required\");\n    const b = req.body || {};\n    if (typeof b.title !== \"string\" || b.title.trim().length < 3) {\n      return fail(400, \"invalid\", \"title must be at least 3 characters\");\n    }\n    if (typeof b.postBody !== \"string\" || b.postBody.trim() === \"\") {\n      return fail(400, \"invalid\", \"postBody is required\");\n    }\n    const post = { id: nextId++, authorName: userName, title: b.title, postBody: b.postBody, published: false };\n    posts.push(post);\n    return { status: 201, body: post };\n  }\n\n  if (req.method === \"PATCH\" && req.path.startsWith(\"/api/posts/\")) {\n    if (!userName) return fail(401, \"unauthorized\", \"Token required\");\n    const id = Number(req.path.split(\"/\")[3]);\n    const post = posts.find(p => p.id === id);\n    if (!post) return fail(404, \"not_found\", \"Not found\");\n    const b = req.body || {};\n    if (\"title\" in b) post.title = b.title;\n    if (\"postBody\" in b) post.postBody = b.postBody;\n    if (\"published\" in b) post.published = b.published;\n    return { status: 200, body: post };\n  }\n\n  return fail(404, \"not_found\", \"Not found\");\n}\n"
      }
    },

    {
      id: "srv-quiz-8",
      title: "Final quiz: Back-end foundations",
      kind: "quiz", xp: 10,
      brief: "Methods, statuses, middleware, tokens, envelopes, pagination — the whole course in seven questions. 80% to pass.",
      questions: [
        { q: "You want to create a new blog post. Which request is RESTfully correct?",
          choices: ["GET /api/posts/create", "PATCH /api/posts", "POST /api/posts", "PUT /api/posts/new"],
          answer: 2, explain: "POST to the collection creates a member of it, with the new post's fields riding in the request body — success answers 201 Created. Verbs never belong in the path, so `/create` and `/new` are both un-RESTful, and PATCH aimed at a collection has no single record to patch." },
        { q: "A request hits a protected route with no token at all. The right status?",
          choices: ["400 Bad Request", "404 Not Found", "200 with an error inside", "401 Unauthorized"],
          answer: 3, explain: "401 Unauthorized = missing or invalid credentials, which is exactly what \"no token at all\" means. 400 is for malformed input, 404 is for a missing resource — and burying a failure inside a 200 is a classic API crime, because every client that checks the status will think it worked." },
        { q: "Which function sees the request FIRST?",
          code: "const wrapped = withLogging(requireBody(coreHandler));\nwrapped(req);",
          lang: "js",
          choices: ["withLogging — the outermost wrapper", "coreHandler — the core always goes first", "requireBody — guards always go first", "Each layer runs at the same moment"],
          answer: 0, explain: "Wrapping composes outside-in: the outermost wrapper runs first and then delegates inward. withLogging logs, calls requireBody, which checks and then calls coreHandler. The core handler runs LAST, guards get no special priority, and nothing runs concurrently. Express middleware chains work exactly like this." },
        { q: "Where does the client carry its token on every authenticated request in this course?",
          choices: ["In the body, as { token }", "In req.headers.authorization", "In the path, like /api/posts/token-ada", "Nowhere — the server remembers you"],
          answer: 1, explain: "The Authorization header travels with each request — that's the standard slot for credentials, and GET requests have no body to hide a token in. HTTP is stateless, so \"the server remembers the last login\" is exactly what does NOT happen, and a token in the path leaks into every log, proxy and browser history." },
        { q: "Every failure in this course answers the same shape. Which one?",
          choices: ["{ status, body: \"oops, something went wrong\" }", "{ error: { message } } with status 200", "{ code, message } and no status at all", "{ status, body: { error: { code, message } } }"],
          answer: 3, explain: "Status on the outside, `{ error: { code, message } }` on the inside. One envelope for every failure means clients write ONE error handler: branch on `code`, show `message`. A bare string gives them nothing to branch on, a failure dressed as 200 is a lie, and dropping the status throws away the coarse signal HTTP already gives you. Consistency is a feature you ship." },
        { q: "A store has 7 published posts and perPage is 3. What does page 3 look like?",
          choices: ["It does not exist — only 2 pages", "The last page, holding just 1 item", "3 items, like every page", "An error — 7 isn't divisible by 3"],
          answer: 1, explain: "`Math.ceil(7 / 3)` = 3 pages, split 3 + 3 + 1, so page 3 exists and holds the single leftover item. `slice` simply returns whatever is left past the start index — a short last page is normal, never an error, and never padded out to a full 3." },
        { q: "After eight units, `handleRequest(req) → res` is…",
          choices: ["What Express, Django and Rails do underneath", "A toy — real servers work completely differently", "A pattern that only suits small blogs", "A stripped-down database engine"],
          answer: 0, explain: "This is exactly what Express, Django and Rails do underneath their routing sugar — it is not a toy, and it is not tied to any one kind of app. Real frameworks add the network socket and nicer packaging, but the mental model is the one you just built: request in, response out, with routing, middleware, auth, validation and pagination in between. A server CALLS a database; it never is one." }
      ]
    }
  ]
});
